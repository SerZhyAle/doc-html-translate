{"check":"scripts/test.ps1","class":"gate","runner":"scripts/check.ps1","decided":"2026-09-24","ticket":"2026-09-23_contract-automated-checks-sync","basis":"judged","reason":"the Go suite (unit, parity, placement, typography, full-corpus integration) - the core of the local gate every build-local / /build runs"}
{"check":"scripts/test-extension.ps1","class":"gate","runner":"scripts/check.ps1","decided":"2026-09-24","ticket":"2026-09-23_contract-automated-checks-sync","basis":"judged","reason":"the extension edition's node --test suite; Node is on the dev machine - until 2026-09-24 it sat in no runner at all, and the publish workflows build the extension without it"}
{"check":"scripts/lint.ps1","class":"gate","runner":"scripts/check.ps1","decided":"2026-09-24","ticket":"2026-09-23_contract-automated-checks-sync","basis":"judged","reason":"golangci-lint over the Go source; cheap, deterministic, same inputs as the test run"}
{"check":"scripts/typo.ps1","class":"gate","runner":"scripts/check.ps1","decided":"2026-09-24","ticket":"2026-09-23_contract-automated-checks-sync","basis":"judged","reason":"typos over the whole tree, including docs and site pages that no other check reads"}
{"check":"scripts/parity-check.ps1","class":"gate","runner":"scripts/check.ps1","decided":"2026-09-24","ticket":"2026-09-23_contract-automated-checks-sync","basis":"judged","reason":"structural Go<->JS drift on the change set; advisory (exit 3) inside the gate because a one-sided change is often intentional and is acknowledged by editing docs/PARITY.md"}
{"check":"scripts/verify-exe-version.ps1","class":"build","runner":"scripts/build.ps1, scripts/build-ui.ps1","decided":"2026-09-24","ticket":"2026-09-23_contract-automated-checks-sync","basis":"judged","reason":"judges a produced artifact, so it runs where the artifact is produced - right after each local build, before the copy to the deploy folder; for a release it is a free local step in scripts/release.ps1 against the downloaded assets, because the tag workflow is paid and stays as it is"}
{"check":"scripts/verify-html.ps1","class":"hand-run","runner":"/verify-view","decided":"2026-09-24","ticket":"2026-09-23_contract-automated-checks-sync","basis":"judged","reason":"its input is a converted book the caller names; there is no fixed input a per-change runner could pass it, and it drives a headless browser for seconds per page"}
{"check":"tools/ocrlab verify","class":"hand-run","runner":"go run ./tools/ocrlab verify","decided":"2026-09-24","ticket":"2026-09-23_contract-automated-checks-sync","basis":"judged","reason":"the corpus media is third-party work kept in the gitignored test_doc/ocrlab; on a clone without it the check can only say COULD NOT VERIFY, so it belongs to the OCR work that has the corpus"}
{"check":"tools/ocrlab gate","class":"hand-run","runner":"go run ./tools/ocrlab gate","decided":"2026-09-24","ticket":"2026-09-23_contract-automated-checks-sync","basis":"judged","reason":"judges a scored run directory produced by ocrlab run + score, which takes minutes and a browser; it is the acceptance step of an OCR change, not of every change"}
{"check":"sza canon compliance","class":"hand-run","runner":"sza plugin, adopt-canon skill","decided":"2026-09-24","ticket":"2026-09-23_contract-automated-checks-sync","basis":"judged","reason":"the canon's own compliance check ships with the sza plugin, outside this repository; it is run when the canon is adopted or re-synced, and the plugin's hooks enforce the always-on part at the tool call"}
{"check":"scripts/doc-registry.ps1","class":"gate","runner":"scripts/check.ps1","decided":"2026-09-25","ticket":"31_2026-09-25_canon-resync-new-duties","basis":"judged","reason":"the documentation registry against the tree in both directions, the SEO block of every announced page and the generated sitemap (canon DOCUMENTATION_CONCEPT section 6); its subject is the whole tree and it reads only files, so it runs in the gate every build and every release passes through"}
{"check":"scripts/security-posture.ps1","class":"gate","runner":"scripts/check.ps1","decided":"2026-09-25","ticket":"31_2026-09-25_canon-resync-new-duties","basis":"judged","reason":"the permission and network-surface inventories against the manifests, the code, the dependency set and every rendered privacy text (canon SECURITY_AND_PRIVACY section 7 item 6 asks for the pre-release sweep); here that sweep is the gate whose evidence scripts/release.ps1 requires, so it runs there"}
{"check":"scripts/contract-gate.ps1","class":"release","runner":"scripts/release.ps1","decided":"2026-09-25","ticket":"31_2026-09-25_canon-resync-new-duties","basis":"judged","reason":"reads the shared contracts catalog, which only the owner's machine has; in the gate it would turn every clone's build into COULD NOT VERIFY, so it runs where the release is cut - scripts/release.ps1 prints its verdict before the tag step and blocks on FAIL or UNVERIFIED"}
{"check":"scripts/audit-slices.ps1","class":"hand-run","runner":"ticket 34 phases (/spec-dev), by hand","decided":"2026-09-26","ticket":"34_2026-09-25_full-code-audit-pre-release","basis":"judged","reason":"measures a sliced audit campaign against its ticket folder; it is open by design for as long as slices are unread, so in the gate it would fail every build - it is run when a slice starts and closes, and before the release that waits on the campaign"}
