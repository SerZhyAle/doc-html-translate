# DEV CHANGELOG

## Project Links
- GitHub Pages: https://serzhyale.github.io/doc-html-translate/
- GitHub Repository: https://github.com/SerZhyAle/doc-html-translate

| Timestamp | Path | Target | Description |
|---|---|---|---|
| 2026-08-13 17:14:15 | internal/ocr/overlay.go, extension/src/ocr-overlay.css, tests/parity_test.go, DEV/research/ocr_plate_coverage_2026-08-13.md, DEV/plan/2026-08-13_ocr-sweep-plate-composition.md, DEV/CHANGELOG.md | ocrlab: the concealment gate goes back to green, and the padding that decides it | The lab run P47 said it had not made. `ocrlab run -split dev`, 46 scenes, 13 annotated, Edge 151 / Tesseract 5.4: worst residual ink **0.9996 → 0.2705** against the recorded 0.28 bound, so all three concealment gate rows go FAIL → **PASS**, and the self-diagnosis over all 46 scenes goes from 93 % of the source lettering still showing to **19 % mean, 55 % worst** (17 % / 56 % when the box carrier was last measured). Everything else is identical to the run this work started from, to the last digit: recall 0.6154, IoU 0.7756, worst IoU 0.3489, merges 1, splits 0, cross-group 6, clipped 0, drift 0. **The run also found a real defect in the change, which is why it was worth making.** The first run scored **0.2841** and missed the gate by 0.0041 - on a scene whose four plate rectangles are **byte-identical** to the reference run''s, same sampled paper, same boxes. So it was not geometry: the residual metric asks whether the *rendered* page still has ink where the source had it, which means a plate''s own replacement lettering counts against it wherever the two coincide - and the plate''s internal padding is what decides how large the runtime fit grows that lettering. Moving the paper to the box had also moved the padding from the old span''s `0.08em 0.28em` to the box''s `0.05em 0.15em`, which grew one plate''s font from 56.4 px to 60.0 px. Restoring the measured value takes the scene to **0.2705**, a hair under the reference run''s own 0.2707. Both editions carry it and `TestParityOCRFontFit` now pins it, because a value that reads as cosmetic turned out to move a gated number. Two things are recorded rather than smoothed over. **`worstHalo` went up** (0.3503 → 0.6532), all of it one scene, `synth-text-on-halftone`, with an identical plate rect - the sampled pair was **inverted in the reference run and is correct now** (the scene''s own generator draws black lettering on cream under a grey dot screen, and the old pair was light-on-grey). Halo counts ink in a ring inside the group''s bounds, so a correctly light plate on a screened ground scores worse than an inverted dark one that blends into the screen: the metric is penalising the right answer, and it carries no gate. **The gate still exits 1 on four rows, none of them this work''s**: `merges`/`crossGroup` are red in the reference run too (`synth-two-columns`, the known open grouping defect the hard gate fixes at 0 by fiat); `recall` and `review` fail on arithmetic - the corpus grew from 11 annotated scenes to 13 after `thresholds.json` was derived and both new scenes score 0, so it is 8 of 13 against 8 of 11; `cost` is the rescue ladder running every rung since 2026-08-13. Re-deriving those three belongs to a dated baseline run, not to a ticket that would like them green (lab rule 2). P47''s residual criterion flips to met; the ticket''s status stays **Partial** on the one criterion that is blocked on the owner by design. The exchange spec in FastMediaSorter Lite gets §14.7 with the same numbers and the padding lesson, which is the transferable part: their S2 changes exactly that quantity. |
| 2026-08-13 16:01:31 | internal/ocr/tesseract.go, internal/ocr/overlay.go, internal/ocr/script.go, internal/ocr/script_test.go, internal/ocr/cluster_test.go, internal/ocr/rescue_test.go, internal/ocr/batch_test.go, internal/pipeline/pipeline.go, internal/pdf/extract.go, internal/pdf/extract_test.go, extension/src/ocr-cluster.js, extension/src/ocr-overlay.js, extension/src/ocr-overlay.css, extension/test/ocr-cluster.test.mjs, tests/parity_test.go, docs/PARITY.md, docs/ocr-pipeline.md, DEV/research/ocr_plate_coverage_2026-08-13.md, DEV/plan/2026-08-13_ocr-sweep-plate-composition.md, DEV/CHANGELOG.md | ocr: a plate stops being a page, and starts concealing what it covers | P47, the 21-document sweep''s composition defects. **The measure the ticket asked for was measured and it does not work**, which is the useful part: how much of a plate''s box its own lines fill *by area* puts the defect (`accounts.jpg`, 0.5891) **above** four scenes that must never be broken - a legitimate balloon at 0.4582, a cartoon caption at 0.3621 - because area fill mixes leading with ragged right edges. What separates the corpus is a conjunction on two other axes, and both halves are needed: a finished cluster is **released into one plate per line** when its box covers more than `ocrMaxPlateCoverage` **0.52** of the image *and* its own line boxes account for less than `ocrMinPlateLineFill` **0.72** of that box''s height. Size alone would release `samson-and-delilah-03-scroll` (one annotated caption over 0.6087 of its crop); looseness alone would release `synth-uniform-paper` (three body lines, 0.6667 fill). Both bounds are geometric middles of brackets measured from opposite directions over the 46 lab scenes and the 13 hand-drawn annotations, and the thin one (line fill, ~9 % each way) is named as thin rather than hidden. **Released, not refused:** each line keeps its own text and box and skips `isTranslatable`, because the assembled text already passed it and re-testing short rows would trade a composition fix for a recall regression. `accounts.jpg` goes from **one plate over 80.6 % of the picture** to **seven, largest 7.67 %** - measured on the built binary, not predicted. Second: **the opaque paper moves back to the plate box** in both editions and `.ocr-ink` is gone. The string carrier lasted one day; the corpus''s answer was 93 % of the source lettering still showing against 17 % for the box, against a recorded bound of 0.28, so it was a regression rather than a trade. Verified by rendering, headless Edge 1280x1800: the Lincoln broadside - the sweep''s clearest "both layers legible at once" case - now reads as one layer, and the Soldier''s Creed plate disappears into the poster''s own near-black panel. The cost the string carrier existed for (paper beside a short last line) is real, is recorded in `docs/PARITY.md`, and is now bounded from the other side by this coverage rule and by the type-size rule. Third: **the OCR language follows the document** when the reader did not choose one (new `internal/ocr/script.go`). Tesseract''s `--psm 0` pass runs **once per book**, on its first image - 0.43 s, against four minutes if it ran per page on a 480-page comic - and where data for the detected script is installed the language becomes `rus+eng`, never a replacement, so a wrong verdict costs a slower pass and not an unreadable page; where it is not, the book gets **no plates** and a line naming the script, the `-ocr-download` and the `-ocr-lang` override. The floor of **6.4** is set by how bad the detector is, measured: over the same 46 scenes it calls an English Archie cover Cyrillic at 3.81 and an 1887 English cartoon Arabic at 5.00, says nothing at all on 19 files including two of the corpus''s three Cyrillic scenes, and is right about a non-Latin script exactly twice - at 8.24 and 8.15. On `test_doc/image.png` the sweep''s `Katanoru-nonyyarenn` over a readable interface becomes `0 image(s) overlaid` plus the sentence that fixes it. Fourth, honesty: the no-raster PDF fallback stated *"OCR is disabled"* as a literal - a run with `-ocr` printed it too - and now states the fact that decides the outcome and holds either way (no text layer **and** no extractable page image). The `page_*.html` orphan half of that defect was already gone: the single-page merge removes the pages it absorbs. **Not done, and named:** the three sweep scenes do not enter the lab corpus - annotation truth and `licenceVerifiedBy` are human-only by the lab''s own rules, and `accounts.jpg` should stay out of a public repository entirely (a private account list; its geometry survives in both editions'' test fixtures with the row texts redacted). The lab was not re-run, so no new residual-ink number is claimed - only plate counts on four sweep cases before and after (none lost a plate) and two rendered pages. Extension parity: the grouping rule and the carrier are ported with mirrored tests; the script rule is not, because its language is an explicit popup choice and the pass needs a 10 MB `osd.traineddata` - stated as a divergence with its consequence in `docs/PARITY.md`. Gates: `scripts/test.ps1`, `scripts/lint.ps1`, `scripts/typo.ps1`, `scripts/check.ps1` green; `npm test` 136/136. |
| 2026-08-13 15:13:51 | internal/ocr/tesseract.go, internal/ocr/cluster_test.go, extension/src/ocr-cluster.js, extension/test/ocr-cluster.test.mjs, tests/parity_test.go, docs/PARITY.md, docs/ocr-pipeline.md, DEV/research/ocr_display_lettering_2026-08-12.md, DEV/plan/2026-08-12_ocr-misses-display-lettering-on-saturated-art.md, DEV/plan/2026-08-12_ocr-misses-display-lettering-on-saturated-art/INDEX.md, DEV/plan/2026-08-12_ocr-misses-display-lettering-on-saturated-art/PHASE_07__type-size-break.md, DEV/plan/2026-08-13_ocr-rescue-floor-drops-genuine-lettering.md, DEV/plan/2026-08-13_ocr-sweep-plate-composition.md, DEV/plan/ROADMAP.md, DEV/CHANGELOG.md | ocr: a headline stops being glued to the text under it | Closes the last open done-criterion of the display-lettering ticket, and the useful part is that the plan''s own diagnosis was wrong. The six lines the rescue ladder recovers on `poster-display-type-on-flat-colour` landed in **one plate spanning 62% of the poster''s height**, matching neither annotated reading group, so the lab scored the scene a miss while a reader could see seven words. That was recorded as a pitch problem and handed to P47. Measuring the line boxes the app actually gets refutes it: the step a reader would cut at - headline to body - is **335 px**, and a step *inside* the body, over a line the confidence floor dropped, is **381 px**. The cut a reader wants is the smaller of the two, so **no pitch bound separates this poster in the right place**, and any threshold that split the headline off would tear the body into three. What separates them is what a reader uses - **type size**: 281 px of display ink over a 155 px body median. `clusterLines` therefore gains a second condition beside pitch and shared column - a line joins a cluster only if its ink height is within `ocrTypeSizeRatio` of that cluster''s own median, either way round (`sameTypeSize`). **1.6, and where it comes from:** the value has to clear the spread one text shows on its own and stay under the step between two texts, and both bands are measured on the corpus''s hand-drawn line boxes rather than on any recognizer output - widest within-group spread 1.42x (`samson-and-delilah-03-scroll`, 19 lines of one caption, ink 23-34 px), narrowest across-group step 1.86x (this poster), next narrowest 2.57x (`synth-display-lettering`). 1.6 is the geometric middle of 1.42 and the recognized 1.81, about 13% of margin each side; a value next to either measurement would have had 2%. `ocrClusterPitchFactor` was **not** moved. Measured, dev split, `temp/ocrlab/p46b` against `temp/ocrlab/p46`: twelve of the thirteen annotated scenes identical to the digit on detection, grouping and placement; the thirteenth reads nothing under the lab''s default `eng` and was re-run with `-lang rus`, where it goes from recall **0.00 to 0.50**, tp/fp/fn 0/1/2 to 1/1/1, and from a plate crossing an unrelated reading group in **every** stress case to **none**. Its body plate matches the annotated group at IoU 0.93 and its transcript is now compared against the annotation. An isolation run with the rule neutralised (`temp/ocrlab/p46b-nosize`) confirms the attribution and shows the cost: the headline plate is small enough that three of the six stress translations clip in it where the merged plate had room. Both editions, hand-ported; `TestParityOCRClustering` grew to pin the constant **and** its meaning - that the candidate is weighed against the *cluster''s* median and not the page''s, and that the ratio multiplies the smaller of the two heights - because the previous fix in this ticket drifted in exactly the way equal constants cannot see. The rule separates sizes, not regions: re-converting the sweep''s worst case, `accounts.jpg` splits its header off and drops from one plate over 80.6% of the image to 68.3%, and same-size separated regions stay P47. Left open and now its own ticket (P49): the headline''s first word `ЗАЧЕМ` is read correctly at 69.2 and discarded by a rescue floor of 80, which is the whole difference between recall 0.50 and 1.00 here. Found while measuring and **not** this change: residual ink over 46 scenes is a mean of 93% against a bound of 0.28, from the 2026-08-13 plate-shape change - proven by `synth-uniform-paper`, which cannot trigger the size rule and whose plate is byte-identical across the two runs while its residual moves 0.085 to 0.998. Recorded in P47. `go test ./internal/ocr/` green, `npm test` 134/134, `go test ./tests/ -run TestParityOCR` 10/10. |
| 2026-08-13 14:18:18 | extension/src/ocr-overlay.js, internal/ocr/overlay.go, docs/PARITY.md, tests/parity_test.go, scripts/add_to_dev_log.ps1, DEV/plan/2026-08-12_ocr-misses-display-lettering-on-saturated-art.md, DEV/plan/2026-08-12_ocr-misses-display-lettering-on-saturated-art/INDEX.md, DEV/plan/2026-08-12_ocr-misses-display-lettering-on-saturated-art/PHASE_06__docs-cleanup.md, DEV/CHANGELOG.md | ocr: close P46 at Partial, and fix the parity drift its own colour fix left behind | Closing pass over the display-lettering ticket. Phase 06 had been executed the day before without its checkboxes flipped, so the plan read 5/6 while the work was on disk; every predicate was re-verified against the tree rather than against the note. **Two real defects turned up while verifying, and neither was bookkeeping.** First, the ring test that decides which sampled colour is the paper had drifted between the editions: `blockColors` in the extension used one variable for two different heights - the line itself and the 1.3-line strip the ink is sampled in - and handed the strip to `ringNearerInk`, so the extension weighed a band 30 % wider than the desktop app on the same block (pad `0.43 x` line height against `0.33 x`). Both sides now derive the ring from the raw line height and name the ink strip separately (`firstBand`); `TestParityOCRPlateColourOrientation` grew three pins the old one could not see - the band divisor on each side, the two heights being kept apart by name, and the ink being a median. Second, `docs/PARITY.md` still described the ink as the **mean** of the deviating pixels, which both editions stopped doing on 2026-08-13: the row now says median and carries the measurement behind it (a caption of rgb(17,17,17) on rgb(253,253,253) gives mean rgb(61,61,61), median rgb(7,7,7)), and the ring is described as a third of a line rather than a line - the code comments said "a line tall" beside a `lh / 3`. Third, and found by trying to describe the shipped plate to another project: the **"plate shape is per line"** bullet in `docs/PARITY.md` named `padStyles`, `padRects` and `.ocr-pad` - three symbols that exist in no edition and in no commit (`git log -S padStyles` is empty). What ships is the simpler thing `TestParityOCRFontFit` actually pins: a transparent `.ocr-box` with the sampled paper on an inline `.ocr-ink` span, `box-decoration-break:clone`, so the browser paints it once per **rendered** line. The bullet now describes that, records why paper cut to the *source* line boxes was rejected (the rendered string wraps where the source did not, so paper and words come apart), and names the open consequence measured in the 21-document sweep - a plate whose text is shorter than its region leaves the source lettering showing around it. The doc was the only thing wrong; no code moved for this one. Also fixed the helper this row was written with: `add_to_dev_log.ps1` used `Add-Content`, so every row it wrote landed at the bottom of a table that is read newest-first - this ticket's own entry sat below rows from 2026-07-01. It now splices under the table rule and refuses to guess when that rule is missing; the two rows it had misfiled were moved into place. Gate: `scripts/test.ps1`, `scripts/lint.ps1`, `scripts/typo.ps1`, `scripts/check.ps1` all green, `npm test` 131/131, `tests/ -run TestParityOCR` 10/10. The ticket stays **Partial** by decision: six of its seven done-criteria are met, and the seventh (the reported scene scored against its annotation) needs the grouping work that is P47 - closing it here would mean moving `ocrClusterPitchFactor`, a shared invariant tuned on balloon scenes, until one poster passes. |
| 2026-08-13 12:41:40 | docs/PARITY.md, extension/_locales/ar/messages.json, extension/_locales/bn/messages.json, extension/_locales/de/messages.json, extension/_locales/en/messages.json, extension/_locales/es/messages.json, extension/_locales/fr/messages.json, extension/_locales/hi/messages.json, extension/_locales/it/messages.json, extension/_locales/pt/messages.json, extension/_locales/ru/messages.json, extension/_locales/uk/messages.json, extension/_locales/ur/messages.json, extension/_locales/zh_CN/messages.json, extension/src/ocr-overlay.css, extension/src/viewer.html, extension/src/viewer.js, internal/comic/comic_test.go, internal/comic/extract.go, internal/htmlgen/navbar.go, internal/htmlgen/singlepage.go, internal/htmlgen/singlepage_test.go, internal/i18n/i18n_reader.go, internal/img/extract.go, internal/ocr/overlay.go | fix(comic): valid page structure, page anchors and an OCR layer toggle | A converted comic (`cb7-tiny_Nyoka-the-Jungle-Girl-071.cb7`, 37 pages) exposed five defects in the image-page output, all fixed across both editions. **1. Invalid HTML:** each page wrapper was a `<main>`, so the single-page merge nested **37 `<main>` elements inside the merged document's own** - reader mode and screen-reader landmark navigation break on that. The wrapper is now `<section class="dht-page" id="page_NNN">` (`internal/comic`, `internal/img`); measured after: `<main>` count 38 -> **1**, `main main` present -> **absent**. **2. No page anchors:** zero ids and no way to reach or link a page in a 37-page scroll; the ids now survive the merge (**37 anchors**) and an image-page book gets a `#dht-page-sel` jump box (**37 options**) that tracks scroll - jumping to page 20 puts it at viewport `top=0`. **3. Orphan page files:** 37 `page_NNN.html` survived the merge unreferenced, carrying no navbar, no theme and no OCR plates; they are now removed after the merged file is safely on disk (**37 -> 0**). **4. Alt text:** `alt` repeated the book title on all 37 images (**1 unique**) - a screen reader read 37 identical objects; now `"<title> - page N of M"` (**37 unique**), and the hard-coded `background: #f0f0f0` that overrode the reader's dark theme is gone. **5. No way to see the artwork:** 483 plates covered the lettering with no control to lift them. Added an OCR-layer toggle, hidden on documents without plates, persisted like the theme, hiding via `display:none` so plates take no pointer events, are not read out, and offer the page translator nothing: Go `#dht-ocr-toggle` -> `html.dht-ocr-off` (localStorage `dht_ocr`), extension `#btn-ocr` -> `html.ocr-layer-off` (`viewerPrefs.ocrLayer`). Both new strings landed in **all 13 locales** (Go `i18n_reader.go` + 13 `_locales/*/messages.json`); docs/PARITY.md records the toggle's three shared rules. Chrome-checked: `index.html total=37 render=37 broken=0`; CDP probe confirmed the toggle moves plates `flex -> none -> flex` with `aria-pressed` following, in the app and against the extension's own stylesheet. New tests cover all of it, including the first coverage `GenerateSinglePage` has ever had (4 tests, mutation-checked: reverting the separator fix fails `TestGenerateSinglePageSeparatorMatchesBookKind`). Note: **OCR accuracy was not touched** - the confidence floors are deliberate and already tuned; small print like the copyright block stays garbled by design rather than by defect. |
| 2026-08-13 02:40:00 | internal/ocr/tesseract.go, internal/ocr/overlay.go, internal/ocr/colors_test.go, internal/ocr/screen_test.go, extension/src/ocr-cluster.js, extension/src/ocr-overlay.js, extension/src/ocr-overlay.css, tests/parity_test.go, docs/PARITY.md | ocr: the plate takes the shape of the text it replaces, and its ink stops being grey | Reported from use on a phone screenshot: the overlay reads as a white patch laid over the picture rather than as the caption it replaces. Three defects behind one complaint, all measured on that image before anything was changed. **The plate was drawn as the block's bounding rectangle, and a bounding rectangle is not the shape of centred copy.** The source caption is 984 px wide on its first two lines and 759 on its third; the plate covered 941 px throughout, so 91 px of paper sat on the photograph on either side of the last line. The opaque backing is now one pad per recognized line (`padStyles` / `padRects`), with the text as a single transparent node over them so the string stays one unit for the translator - pads widened by a quarter line height because paper extends past the ink box, meeting in the middle of the gap between neighbouring lines so no sliver of picture shows between them, and falling back to the bounding box for any block with no line list. That needed the line boxes to reach the renderer at all: `Block.Lines` and the JS block's `lines` are new, filled in `clusterLines` and scaled with everything else in `scaleDown`. **The ink was a mean where it should have been a median.** The background was already a median; the ink averaged the pixels that stand out from it, and the deviation test admits most of a glyph's antialiased edge - so the answer sat between the ink and the paper by construction. Measured on the same caption, rgb(17,17,17) on rgb(253,253,253): mean rgb(61,61,61), median rgb(7,7,7), and the shipped plate now samples rgb(22,22,22) where it used to sample rgb(61,61,61). **The runtime fit could only shrink.** It now grows as well, capped at 1.15x the compile-time size - a bound rather than "fill the box", because a block box includes the leading between its lines and filling it would print a translation larger than the words it covers. Corner radius moved from a fixed 2px to 0.35em so a caption plate and a full-page plate round in proportion. Both editions, hand-ported; `TestParityOCRFontFit` grew to pin the per-line draw, the fallback, the line-box carriers, the median ink and the grow ceiling on both sides - a side reverting to one rectangle would pass every constant check without it. `go test ./internal/...` green, `npm test` 131/131, `scripts/lint.ps1` passed. Corpus re-measure running; the previous run over the same 13 annotated scenes moved nothing in recognition or geometry (recall 0.62, mean IoU 0.78, merges 1, cross-group 6, clipped 0 - all unchanged), which is expected of a change that only draws. |
| 2026-08-12 20:50:00 | internal/ocr/exif.go, internal/ocr/exif_test.go, internal/ocr/tesseract.go, internal/ocr/overlay.go, internal/ocr/upscale_test.go, internal/htmlgen/navbar.go, internal/htmlgen/navbar_test.go, docs/PARITY.md, docs/ocr-pipeline.md, DEV/research/ocr_positioning_exchange_2026-08-12.md, DEV/research/RESEARCH_INDEX.md, DEV/plan/2026-08-12_ocr-exchange-followups.md, DEV/plan/ROADMAP.md | ocr: two ways the overlay was in the wrong place, both found by answering someone else's questions | Written to answer the open section of the FastMediaSorter Lite positioning exchange from the code rather than from memory; the answers are the research artifact, and these two defects are what checking them turned up. **A page script was moving every plate off its text.** The navbar's image-aspect guard writes an *inline* `width` on every `<img>` it watches, and an inline width beats `.ocr-fig>img{width:100%}` - so on any page whose picture is narrower than the text column the image fell back to its natural size while the plate container kept the column's, and the plates, positioned in percent of the container, spread across a picture no longer under them. Measured in headless Chrome on a 640x320 scene in a 1216 px column: the plate belonged at x=48, 405 px wide, and rendered at x=91, 770 px wide - 1.9x too large and off its text. **It drifted 0 px between viewports**, which is why nothing caught it: the acceptance gate grades the position dimension on drift alone, and a systematic offset is invisible to a stability measure. The guard now skips images inside `.ocr-fig` at both entry points (`TestImageAspectGuardSkipsOCROverlay` fails if the exemption is dropped), and the missing absolute-position bound is item 1 of the new ticket. **EXIF orientation was ignored, and it broke recognition before it broke geometry.** A browser paints an `<img>` through its orientation tag; tesseract reads the file's stored pixels. For the ordinary portrait phone shot of a page those are two different pictures, and no OSD runs in either PSM this app uses - so the lettering arrives on its side and comes back as its upside-down transcript (measured: `"duin{ seiqaz yep 2INb A\bulxen MoH"` for a legible line), while anything that does read lands in a space the plates are not in (measured before the fix: a 1216x2432 container with its plate 480 px down a picture whose text runs across the top). Both are answered in one place - `stageForOCR` now turns the copy handed to tesseract, so recognition sees what the reader sees and every coordinate arrives in display space; the decoded image the plate colours are sampled from is turned with it. The same photo now recognizes its full line and produces **byte-identical geometry to the unrotated PNG of the same scene** (`x0=48 y0=63 x1=453 y1=167 lineH=20`, same inline style). This is the one row of their §11 comparison where their pipeline was right and ours was not: they feed OCR an EXIF-corrected snapshot, we fed the file. Rotation is read from JPEG only (PNG `eXIf` and WebP are a documented gap, not a planned parser), and the pixel map is a bijection rather than a clamped point map - the first draft clamped mirrored coordinates and silently lost the top-left pixel, which is a pixel a plate samples its paper colour from, caught by its own test. Verified after both fixes at ten browser states - device scale and page zoom at 100/125/150/200 %, plus tablet and phone - worst plate-edge drift **0 px of the source image** on all three example pages. `go test ./internal/...` green, `tests/ -run TestParity` 15/15 green; the full-corpus conversion test still dies with `out of memory` on the big CBZ samples, reproduced on a clean checkout of HEAD, so it is the standing 386-build ceiling and not this change. |
| 2026-08-12 17:05:00 | extension/scripts/_ocrlab-image.mjs, extension/scripts/ocrlab.mjs, extension/test/ocrlab-image.test.mjs, extension/package.json, docs/PARITY.md, DEV/plan/2026-08-12_extension-crashes-the-tab-on-a-detailed-scan.md, DEV/research/ocrlab/2026-08-11__baseline.md, DEV/plan/ROADMAP.md | ocrlab: the extension never crashed anything - the lab dropped its own connection, and the two entries below are corrected | **This corrects the 13:40 and 14:10 entries.** The baseline's largest single result, "the extension kills the whole browser process on 17 of 45 scenes", is false. The browser survives every one of them: driven for two minutes with the console, exceptions and target lifecycle subscribed, the page answers, the browser answers, `child.exitCode` stays null, no crash dump is written and Windows logs no application error. What died was the lab's own websocket. `Page.captureScreenshot` returns its PNG as base64 in a **single** DevTools message, and this runner asked for the clip rendered at the source's natural size - often an upscale - so the reply's size tracked the source's pixel count *and* its detail, which is exactly why the failures correlated with megapixels without megapixels ever being the rule. Measured ceiling: **4 121 988 bytes of base64 arrived, the next step up did not**, and a failing call received nothing at all (largest frame ~1 KB), so it is the reply and not a partial transfer. A **third** hypothesis fell on the way - the connected-components one this ticket was opened to test: five probes at an identical 2.52 Mpx separate cleanly by PNG size and not by component count, since the surviving halftone has 70 200 components after Otsu and the dying engraving has 7 083. All three refuted mechanisms were about the recognizer, and a fix drawn from any of them would have changed shipped code to work around a defect that was not there. The repair is the extension runner rejoining a contract the Go runner already kept: capture at the viewport's own resolution and map into image space locally (`CropToImage` does this in `browser.go`). New `_ocrlab-image.mjs` splits the capture into bands of at most `CaptureBandPx` = 1 500 000 pixels - **derived**, since pure noise encodes to about 2.0 MB of base64 per megapixel, worst-casing a band to ~3.0 MB against a limit that took 4.12 MB - and stitches them with boundaries computed from the whole rather than accumulated, so rounding cannot drift down the page and shift the bottom of the image against the source it is compared with. The extension needs bands and the Go runner does not, because one clips to the image and the other to the viewport; recorded in `docs/PARITY.md` as an intentional difference alongside the new "Screenshot space" row. **Result: 45 of 45 scenes produce a result, zero errored, zero browser relaunches** (was 28 of 45), and no gate check moved from PASS to FAIL - every failure was already failing on the baseline. The cost is stated rather than hidden: both editions now measure a resampled render, which moved mean residual ink 0.1170 -> 0.1185 and mean minimum contrast 91.7 -> 82.8 over the 28 scenes common to both runs, so the extension's concealment and contrast columns in the baseline are superseded by `temp/ocrlab/fix02`. `thresholds.json` is unaffected - it came from the desktop run. **No shipped code changed**; `extension/src` is untouched. `npm test` 129/129, `scripts/lint.ps1` passed. |
| 2026-08-12 16:32:01 | internal/ocr/tesseract.go, internal/ocr/strength.go, internal/ocr/strength_test.go, internal/ocr/rescue_test.go, internal/ocr/overlay.go, internal/ocr/colors_test.go, extension/src/ocr-overlay.js, extension/src/ocr-cluster.js, extension/test/ocr-cluster.test.mjs, tests/parity_test.go, docs/PARITY.md, DEV/ocrlab/corpus.json, DEV/ocrlab/annotations/ | fix(ocr): the rescue ladder keeps its strongest rung, and a sparse rung reads posters | A standalone poster - display lettering over flat saturated colour - used to come back with one stray word on it. Three changes, all measured on the reported scene and on its sibling, both now in the ocrlab corpus with hand-measured annotations: the grey rescue ladder no longer stops at the first rung that produced any plate at all but runs every rung and keeps the strongest (`resultStrength` counts words placed, `strictlyBetter` gives a tie to the incumbent); a third rung asks Tesseract for sparse text (PSM 11) instead of a page, which is what a poster is; and plate colours stopped coming back as the negative of the word they cover - the paper is now decided by the band just outside the block rather than by the median over it, which assumed the text is the minority of its own box and is wrong for heavy display capitals. Measured end to end with rus data on the reported poster: transcript `\| МОЖЕМ` (2 words) becomes `ТРАХАТЬСЯ: МЫ ЖЕ ЛЮДИ, МОЖЕМ ПРОСТО ПОГОВОРИТЬ` (7 words), plate colours go from rgb(47,42,32)/rgb(198,174,139) to rgb(226,185,143)/rgb(164,56,36), wall clock 3.8s to 6.1s. An image that reads normally never enters the ladder and pays nothing. Ported to the extension and pinned by TestParityOCRRungComparator and TestParityOCRPlateColourOrientation. Ticket: DEV/plan/2026-08-12_ocr-misses-display-lettering-on-saturated-art.md. Corpus outcome, dev split, `temp/ocrlab/p46` against the named baseline `temp/ocrlab/base06-desktop`: nothing regressed - all eleven scenes the baseline scored keep their recall to the digit, and the two hard gate rules (`reading groups merged` 1, `plates crossing another group` 6) come out identical - while the sibling scene `a-propaganda-poster-from-the-soviet-union-in-the-1920s`, read with its own language, went from no plates at all to recall 1.00 at CER 0.00. Left open and recorded in DEV/research/ocr_display_lettering_2026-08-12.md: the six recovered lines land in one plate, because with three lines still missing the page's median line pitch is wide enough to join the headline to the body. |
| 2026-08-12 13:40:00 | DEV/research/ocrlab/2026-08-11__baseline.md, DEV/ocrlab/thresholds.json, DEV/research/RESEARCH_INDEX.md, DEV/plan/2026-08-11_ocr-visual-fidelity-lab/PHASE_06__baseline-and-thresholds.md, DEV/plan/2026-08-11_ocr-visual-fidelity-lab/INDEX.md | ocrlab: the first measured baseline of both editions, and the bounds it can honestly support | Phase 06 of the OCR visual-fidelity ticket, steps 06.1-06.3 and 06.5. Both editions ran the whole 45-scene corpus at the three pinned viewports and the six stress cases, and the report states all eight of the strategic table's dimensions per edition, per category and per split, with the coverage caveat as its headline rather than a footnote: **11 scored scenes desktop, 10 extension, and no holdout at all**. The two editions were confirmed to load byte-identical language data (`sha256:7d4322bd2a774972`), so a divergence between them is the pipeline, never the training data. **The largest single result is the extension's, and it is a user-visible failure rather than a quality score: 17 of 45 scenes crash the browser tab**, and the boundary is sharp - 15 of 15 scenes above 2.8 Mpx died, 28 of 30 below it survived, while the desktop app completed all 45 including a 17 Mpx newspaper page. A reader who opens an ordinary comic or newspaper scan in the extension loses the tab. The desktop app's own worst result is that **11 scenes get no overlay at all** - the reader is shown the untranslated original - concentrated in posters and non-Latin display lettering. `thresholds.json` is deliberately small and says so in its own `caveat` field: the four hard gates at zero as the spec fixes them, overall bounds set **at** the measured value rather than above it (`cost` is the one exception, at ~1.5x, because it measures this machine and not the product), per-category bounds only for `comic` and `texture` because every other category has one or two scored scenes, and **no `regressionTolerance` on any dimension** - it compares holdout against holdout and there is no holdout, so the field could only ever be decorative. `ocrlab gate` reproduces the report's verdict on both runs: FAIL on exactly the hard gates §6.1 names. Strategic §9's four research items are answered in §10 - three stay open with the scene count that would close each, and §9.3 is answered "do not add the font-recognition dependency", since none of the five measured findings is a typeface problem. No shipped code changed; no rendering decision moved. |
| 2026-08-12 14:10:00 | DEV/plan/2026-08-12_extension-crashes-the-tab-on-a-detailed-scan.md, DEV/plan/ROADMAP.md, DEV/research/ocrlab/2026-08-11__baseline.md, extension/scripts/ocrlab.mjs | ocrlab: the extension's tab-killing defect gets its own ticket, and two plausible causes are refuted before any code is written | The baseline's largest single result was that the extension **kills the whole browser process** on 17 of 45 scenes, so it was taken as the next work item ahead of the visual-fidelity ticket's Phase 07 - replacement quality on a page that never renders is not a question. Diagnosis first, and it changed the answer twice. The corpus correlates crashes with image size (15 of 15 above 2.8 Mpx died, 28 of 30 below survived), but a controlled probe - six downscaled copies of an 11.3 Mpx crosshatched engraving, registered as derived scenes and removed again afterwards so they would not inflate the `cartoon` count - **dies at 2.0 Mpx while corpus scenes live at 8.2**. That refuted the size rule, and then refuted both mechanisms behind it: the 2x pre-OCR upscale is not the cause (the 1.0 Mpx probe is upscaled to 4.7 Mpx and **survives**, while the 2.0 Mpx probe is fed at native size and dies - the larger staged image is the safe one), and neither is the grey rescue ladder (both surviving probes returned a plate, so their first pass read and the ladder never ran; two corpus scenes that did run it to the end, at 1.9 and 1.6 Mpx, survived). What remains is a hypothesis and is labelled one: cost tracks the number of connected components rather than area - every scene that dies is an engraving, a halftone or photographic grain, every large scene that lives is a flat cartoon or poster. **No shipped code was changed**, because a pixel ceiling chosen from the refuted correlation would degrade recognition on every large flat page to protect a class it may not be about. The lab gained one thing it lacked: when the browser dies its stderr tail is now recorded on the scene, since "the DevTools connection errored" says a browser died and not why. The baseline report's own §5 was corrected rather than left standing - it had stated the size boundary as a rule before the probe refuted it. |
| 2026-08-12 13:05:00 | extension/scripts/_ocrlab-cdp.mjs, extension/scripts/ocrlab.mjs, extension/test/ocrlab-cdp.test.mjs | ocrlab: the extension runner could not finish a run, and the reason was a one-sided gap in a shared contract | The lab's browser transport hung permanently on the seventh scene of a full-corpus run - no evidence file, no failed scene, no output at all, 0.4 s of browser CPU per 30 s. The cause was not the browser: `waitFor` read its deadline only *after* its predicate returned, and the predicate called `CDP.send`, which had **no timeout whatsoever**, so a wedged renderer hung `Runtime.evaluate` for ever inside a wait that could never fire. `tools/ocrlab/runner/cdp.go` has bounded every call at 90 s since it was written, which makes this precisely the class of one-sided divergence the parity contract exists to catch, and the reason the desktop edition finished while the extension did not. Three repairs, all in the lab and none touching shipped extension code: `CDP.send` now bounds every call at `CallTimeoutMs = 90_000`, the Go side's value; a socket `close` or `error` fails every pending call **and marks the client dead** so later calls reject at once, because without that a crash on scene 7 cost 38 further scenes at 90 s each of grinding through a browser that was not there; and the runner starts a fresh browser when one dies and continues with the next scene, so a crash costs the scene that caused it rather than every scene after it - the crashed scene keeping its recorded error, never dropped from the count. A fourth, smaller fix: the launch re-reads `DevToolsActivePort` on each attempt instead of once, because a browser starting on a brand-new profile can write a port, exit and be replaced by a second process on a different one, and connecting to the first then fails with `ECONNREFUSED` for a browser that is running perfectly. Without these the strategic spec's "a missing dependency is an explicit failure, never a silently reduced scene count" was unenforceable in this edition: the failure mode was not a reduced count but an unbounded silence. `npm test` 121/121. |
| 2026-08-12 05:20:00 | internal/ocr/screen.go, internal/ocr/tesseract.go, internal/ocr/screen_test.go, extension/src/ocr-screen.js, extension/src/ocr-overlay.js, extension/test/ocr-screen.test.mjs, tests/parity_test.go, docs/PARITY.md, DEV/plan/2026-08-12_ocr-screen-pass-for-pages-that-already-read.md, DEV/plan/2026-08-12_ocr-screen-pass-for-pages-that-already-read/ | ocr: the screen pass now reaches a page whose balloons already read | The halftone rung shipped last cycle sits where the whole rescue ladder sits - it fires only for an image that produced **no plates at all** - and that position, chosen because it cannot regress anything, is why it cannot reach the case a reader actually meets. On a real comic page the two kinds of text coexist: dialogue on clean white balloons, and captions printed as a tint with the lettering laid over the dots. The ordinary pass reads the balloons, the page is never "unread", the rung never runs, and the caption stays untranslated with no explanation. `screenSweep` is the same low-pass spent on the opposite branch. **Additive, not a repositioning**, because the screen pass is not better as a replacement: measured over the corpus it gains on screened material (+47% and +18% confident words on two real scenes, and `IN THE COURT OF KING` finally completing to `.. TARENT.`) and loses badly where there is no screen (16 confident words to 0 on one cover, 71 to 49 on a poster). Every plate the ordinary pass produced survives untouched and only non-overlapping plates join it. **The trigger is the detector asking a narrower question:** `screenPitchOutside` votes only over tiles no existing plate covers by more than `ocrScreenTileCoverMax`, so the second recognition is spent only where there is screened area the reader is not served on - a screen under lettering that is already plated has nothing left to give. Half rather than "touches at all", or a plate clipping a tile's corner would blind the detector to the caption standing right beside it, which is the entire case. **The merge measures the union of the overlaps, not the maximum**, which is what answers "a candidate that overlaps two": each existing plate can take 17.5% of it, under the 20% bound on its own, while together they take 35% - a per-plate rule would see two halves and paint a duplicate over lettering that already has one, which is visible damage on a page the reader was happy with. Both editions, hand-ported: `TestParityOCRScreenRung` now pins the two new constants *and* three structural facts - the sweep runs on the branch that already read, it measures only outside the plates, and it merges rather than replaces - proven to fail on a one-sided constant change. **The confidence floor is inherited, and said so plainly rather than quietly:** the local prior is the rescue prior (nothing was found *in this region*) while a wrong plate costs more here, so 80 is a lower bound argued for, not a number re-derived. **Cost:** unconditional is one decode plus the detector sweep, capped at 96 tiles whatever the page size; the recognition is paid only where the trigger fires. `scripts/test.ps1`, `scripts/lint.ps1` green, `npm test` 117/117. **Not claimed done:** the ticket's first three Done criteria need three real screened *whole pages* with reviewed annotations - the corpus's three screened scenes are caption crops and cannot score plate composition, which is exactly what an additive pass moves. Status `BlockNeedUserTest`. |
| 2026-08-12 03:10:00 | tools/ocrlab/metrics/selftest.go, tools/ocrlab/metrics/metrics_test.go, tools/ocrlab/evidence/evidence.go, tools/ocrlab/cmd_run.go, extension/scripts/_ocrlab-evidence.mjs, extension/test/ocrlab-evidence.test.mjs, tests/parity_test.go, docs/PARITY.md | ocrlab: two measures that named defects the product does not have | Found by trying to fix the baseline's top two findings and discovering there was nothing to fix - which is the argument for measuring before tuning, arriving from the other direction than usual. **"The patch stops short of the glyphs" (26 of 45 scenes, 73%) was a tautology.** It asked whether source ink in the rim outside a plate survived the render. It always does: an overlay is CSS over an untouched `<img>`, so no pixel outside a plate can change, and the number therefore read ~100% on every scene with artwork near its lettering - including synthetic scenes built to be geometrically perfect. `CutGlyphInk` replaces it and asks the question that was meant: rim ink *joined, through ink, to the ink under the plate*, which is the shape a patch leaves when it cuts a letter in half. Connectivity is what discriminates - artwork standing beside the text is not attached to it - so the ink threshold is taken across the grown area as a whole rather than per side, or a stroke would break at the seam. 7 of the 8 synthetic scenes now read exactly 0; the corpus reads 0-38% with a mean of 9%, and the finding survives on 14 scenes instead of 26. `synth-text-on-halftone` reads 21% because a screen connects across the plate edge - a known confound, worth a comment rather than a special case. **The clipping gate was reading integer rounding.** Every clipped record in the corpus overshot by exactly 2 or 3 px and none by more: the shipped re-fit's `height:auto` escape had fired on all 17 scenes and nothing was hidden, but `scrollHeight` and `clientHeight` are each rounded from a fractional layout and the lab allowed 1 px. Clipping is one of the strategic spec's four **hard gates**, so this would have failed every future run on rounding. `evidence.ClipSlackPx` is 4 - clear of the measured 3 px residue, and unable to hide a real clip because hiding text costs at least one line and a line is at least a font size tall. Mirrored as `CLIP_SLACK_PX` and guarded by `TestParityOCRLabEvidenceSchema`, which reads the value from both codebases rather than comparing against a literal. Both fixes carry a deterministic test that fails on the old behaviour: `TestCutGlyphInkFollowsStrokesNotNeighbours` builds a stroke with an unrelated mark inside the rim and asserts the covering plate scores 0 while the plate that stops mid-stroke does not. **No rendering decision changed** - this is the instrument, so the strategic spec's "no phase before 06" rule stands. The corrected picture puts the biggest defect where it was third: 11 of 45 scenes get no plate at all and show the reader the original text. |
| 2026-08-12 01:55:00 | tools/ocrlab/report/gate.go, tools/ocrlab/cmd_run.go, tools/ocrlab/main.go | ocrlab: an acceptance gate that refuses to invent a number (`ocrlab gate`) | The one place in the lab where a number decides something, kept deliberately away from everywhere else: `metrics` answers "what happened", `gate.go` answers "is that acceptable", and separating them is what lets a scoring change be re-applied to old evidence and compared - the only way to tell "the app got better" from "the bar moved". `LoadThresholds` **rejects the file rather than the run** when a derived bound carries no `baseline` field: a bound with no recorded measurement behind it is a bound somebody chose, which is exactly what the strategic spec forbids, and this makes a retrofitted bound show up in a diff instead of only in a verdict. The four hard gates - protected-area damage, merged reading groups, clipped plates, plates crossing another group - are read from the file so the whole contract is in one place, but are refused if set to anything but zero. Reporting is ordered for how it is actually read: failures first, then everything, then one line that is the exit code in words; a category with no scored scene prints "not a pass, an absence" rather than a silent tick. The non-regression check looks only at the holdout, because the dev split is what tuning is allowed to move and comparing it would score an intended improvement as a failure. **Nothing is judged yet, on purpose:** `DEV/ocrlab/thresholds.json` is a later step's output, so the command currently stops with that file named. `ocrlab verify` reports 11 gradable annotations over 45 scenes and no holdout at all, and a threshold derived from that would be a decimal point with no evidence behind it. |
| 2026-08-12 01:40:00 | tools/ocrlab/runner/cdp.go, tools/ocrlab/runner/browser.go, tools/ocrlab/runner/probe_test.go | ocrlab: the desktop runner drives the browser over DevTools, because its one-shot switches stopped working | Found by phase 05's own gate, not by a report: `TestProbeCollectsPlatesAndExits` failed with "browser returned an empty DOM" after 30 ms. **The browser is what changed.** Measured on Chrome 151.0.7922.76 and the matching Edge, under `--headless=new`, `--headless=old` and plain `--headless`, with output redirected to a real file: `--dump-dom` writes zero bytes and `--screenshot` writes no file. Neither errors - they silently do nothing, which is why the symptom read as a rendering fault rather than a missing feature, and why the desktop half of the lab had quietly stopped producing evidence at all. `cdp.go` speaks the DevTools protocol instead, the same protocol and for the same reason as the extension edition's `_ocrlab-cdp.mjs`. **No new dependency:** `golang.org/x/net` was already a direct requirement and carries a WebSocket client, so `go.mod` is untouched. **The probe is untouched** - the injected script, the `#ocrlab-collect` / `#ocrlab-stress=` fragment protocol and `extractProbeResult` all still hold; only how a page is opened and read back moved. Two traps of the new transport, both now commented at the point that would otherwise repeat them: Chrome has refused a DevTools WebSocket carrying an `Origin` header since 111 (its DNS-rebinding defence) and reports only "bad status", while `x/net/websocket` refuses to dial without one - so exactly `http://127.0.0.1` is sent and exactly that origin is allowed, rather than the `*` most tooling reaches for; and a fragment-only navigation does not reload, so every open goes through `about:blank` or each state would report the previous state's numbers. `--virtual-time-budget` is gone with the switches: it was never a statement that the page was ready, only a guess at how long things take, and the session now waits for the probe's own output - the exact thing the caller is about to read. The one-shot design also cost a browser launch per page state; one session now serves the whole run and viewports are emulated rather than relaunched. `TestBrowserProfileDirIsAbsolute` still guards the absolute-profile trap, against `launchArgs` instead of the removed `baseArgs`, and the probe test now closes the session it opens - a browser that outlives the test holds the profile open and `t.TempDir` cannot clean up. Verified end to end: `ocrlab run` over two scenes produced 18 plate records each, six 720x360 stress captures per scene mapped into the source image's own pixels, scores and a report; `scripts/test.ps1` and `scripts/lint.ps1` both green. |
| 2026-08-12 01:23:00 | extension/scripts/_ocrlab-evidence.mjs, extension/scripts/_ocrlab-cdp.mjs, extension/scripts/ocrlab.mjs, extension/test/ocrlab-evidence.test.mjs, extension/package.json, tests/parity_test.go, docs/PARITY.md | ocrlab: the browser edition now produces the same evidence the desktop edition does (`npm run ocrlab`) | The lab could measure the app but not the extension, so half the product's OCR quality had no instrument at all. It has one now, and it grades through the same `ocrlab score` against the same annotations: the runner loads the working tree as an unpacked extension, opens each corpus scene in the real viewer over a local `node:http` server (not `file://`, so lazy loading and the IntersectionObserver behave as in production), waits for the real tesseract.js recognition, and reads each `.ocr-plate` back out of the laid-out DOM - rect mapped into the source image's natural pixels, computed background and ink, `scrollHeight`/`clientHeight` as the browser reports them. It measures the shipped code and re-implements none of it. **The contract is what is pinned, not the text:** strategic §6 accepts that the Tesseract CLI and tesseract.js read different words, so `_ocrlab-evidence.mjs` mirrors the Go `evidence` package field for field and in declaration order (two runs of one scene diff line by line), and `TestParityOCRLabEvidenceSchema` reads the schema version, the `Plate` field list and the six-case stress table out of both codebases and fails when one side moves alone - proven by bumping `SCHEMA_VERSION` on the JS side only and watching the Go test name the drift. The stress cases are the same six literals as `stress.go`, applied as real text-node mutations so the extension's own `MutationObserver` re-fit fires, which is the event the clipping measurement exists to catch; the three viewports are `runner.Viewports`. **Dependency-free**, like `build.mjs`: Node's global `WebSocket` is all the DevTools protocol needs, and `devDependencies` is unchanged. A missing browser, a missing `vendor/`, a missing asset or a hash that does not match stops the run rather than shrinking it. **First cross-edition result:** on `synth-two-columns` the extension merges the two columns into one plate that crosses the neighbouring reading group at all six stress cases - the same defect, in the same place, as the desktop edition's baseline, so it is the port that is faithful and the grouping that is wrong. |
| 2026-08-12 00:45:00 | internal/ocr/screen.go, internal/ocr/tesseract.go, internal/ocr/screen_test.go, internal/ocr/rescue_test.go | ocr: the rescue ladder's last rung reads lettering printed on a halftone screen | A dot screen is the one thing no thresholder gets through: when the screen's own tone falls between the ink and the paper, Otsu splits the *dots* from the paper instead of splitting the lettering from everything, and the mask that reaches recognition is a field of dots with the words lost inside it. `synth-text-on-halftone` had produced nothing on the colour pass, the grey pass, the tiled-Otsu pass, at 1x, 2x, 3x and 4x. The new rung measures the screen's period and hands tesseract a copy low-passed with a Gaussian of `sigma = pitch / 4`; the scene comes back with its exact transcript, `MEANWHILE, ELSEWHERE`, in a box within 5 px of the annotation on every edge. **The kernel is derived, not fixed**, because a screen's pitch depends on the press *and* on the scan - and on the app's own upscale, which is easy to miss: this scene is a pitch-6 file but a **pitch-12 picture** by the time a rescue sees it. The divisor is corroborated across pitch 6 and 12 on real material as well as synthetic, and the working window is narrow enough that guessing would have failed - the full transcript returns at sigma 3.0-3.5 and *nothing* at 2.75 or 4.0. **A resolution change is not a substitute** (measured at 2x/3x/4x: still nothing): block-averaging only erases a screen once the block reaches the screen's period, and the lettering goes with it. Detection needs no frequency transform - a lattice repeats, so tile-wise autocorrelation of a 3x3 high-pass finds the period at O(n) with the tile count capped at 96 whatever the image size, so a 20-megapixel page pays what a panel does. Position is the safety: last rung, after both grey rungs returned nothing, so no image that produces a plate today can be touched. `greyRendition` now returns pixels instead of a path, since the rung measures them and `writeTempPNG` already existed for the staging. **Cost, measured as an A/B with the rung off and on:** nothing on an image that reads (605 -> 610 ms on a screened comic page), 2-27 ms on an unreadable image with no screen, one extra recognition where a screen is found (+141 ms on the diagnostic scene, +443 ms on a 1750x1350 crop); a 12-megapixel page pays the same measurement as a panel. **Dev split** (`pitch1` -> `loop4` - the grouping ticket's run is the baseline, not `loop3`, which predates it): 5 scenes gained plates, **0 lost**, 40 unchanged, and four of the five gains are new corpus scenes, so the only existing scene that moved is the diagnostic one. Merges, splits, protected-area damage, cross-group overlap and drift all unchanged; the mean recall and IoU dip only because three new scenes joined the average, one of which reads nothing before and after. |
| 2026-08-12 00:45:00 | extension/src/ocr-screen.js, extension/src/ocr-overlay.js, extension/test/ocr-screen.test.mjs | extension: the halftone rung (parity port) - and the decline the ticket allowed was not needed | The ticket left room for the extension to decline, on the grounds that the JS side has no image-processing library on this path. It does not need one. The measurement is arithmetic over a typed array (`ocr-screen.js`, a pure module like `ocr-cluster.js` so it can be tested without loading the vendored Tesseract bundle), and the kernel is CSS `blur(<sigma>px)`, whose length **is** the Gaussian's standard deviation - the same kernel the desktop app convolves by hand, applied in the same canvas draw as `grayscale(1)`. The added browser cost is one `getImageData` and one `recognize`, and only on an image that already produced nothing. The greyscale draw was factored into a shared `greyCanvas` so the rendition handed to Tesseract and the pixels the detector reads come from one place and cannot drift apart. The new test builds the identical dot lattice the Go test builds and asserts the same pitches, so the two detectors are judged on the same picture; it also pins that the tile sampling stays capped. `npm test` 102/102 (97 before). |
| 2026-08-12 00:45:00 | docs/PARITY.md, tests/parity_test.go | parity: the halftone rung is a shared invariant, position included | PARITY.md gains the row - what the rung is, that it is **last**, all eight detector constants, the derived sigma, and the intentional implementation difference (hand-convolved Gaussian vs CSS `blur`) recorded so nobody later "fixes" it into agreement. `TestParityOCRScreenRung` compares every constant against its JS twin and then pins two things a value check cannot see: that the rung is still called *after* the grey ladder on both sides, and that the sigma is still computed from the measured pitch rather than hardcoded. Position is as much the invariant as the numbers - moving this rung in front of the grey ones would spend a lossy filter on every image the cheap rungs already read. |
| 2026-08-12 00:45:00 | DEV/ocrlab/corpus.json, DEV/ocrlab/annotations/{samson-and-delilah-03-scroll,samson-and-delilah-15-court-caption,le-petit-journal-1908-map-labels}.json, tools/ocrlab/cmd_add.go | ocrlab: three real screened scenes, because the corpus did not hold what the ticket assumed | The ticket's binding constraint was to annotate real screened material *before* choosing a parameter. Scoring all 30 commons scenes for a lattice and then looking at the top of that list at 1:1 zoom showed the premise was wrong, in three different ways: on the comic covers the screen is in the artwork while the lettering sits on clean white balloons; the `atomicwar` pages and other low-resolution scans resolve no screen at all, the scanner having averaged it away; and the 1862 and 1916 newspaper cartoons are wood engraving and crosshatching, not halftone. Exactly one existing scene carries legible text **on** a resolvable screen. So the corpus was extended rather than the fix tuned to one synthetic screen: 19 candidates harvested and scored, and the three that qualify are annotated - a 1950 comic caption scroll (light pink screen), a caption band from the same issue (saturated red, the tone that lands between ink and paper), and the 1908 chromolithograph's map lettering (outline letterforms, `MONTENEGRO` marked partly-illegible). Line geometry was measured from the pixels by luminance threshold and projection, never from OCR, so the lab's first rule holds. Their parent pages are in the manifest and the crops are tied to them by `derivedFrom`; `ocrlab add` grew a `-derived-from` flag so this did not need hand-edited JSON. |
| 2026-08-11 23:35:00 | DEV/plan/done/2026-08-11_ocr-grouping-splits-one-balloon.md, DEV/plan/ROADMAP.md, DEV/plan/2026-08-11_ocr-visual-fidelity-lab/PHASE_07__concealment-and-grouping.md, tools/ocrlab/synth/synth.go, tools/ocrlab/synth/synth_test.go | plan: P43 implemented, and the decision Phase 07 asked for | The ticket carries a "What landed" section written against the measured result, its three open questions answered or bounded rather than dropped, and the two lab tables that show the trade did not happen. The phase note's open target is struck through **with the decision it demanded**: this was taken as its own ticket instead of a second half of Step 07.3, because 07.3's instruction is to add a boundary condition while keeping the grouping constants, and bundling the two would have moved one gate twice in one step. 07.3 keeps its substance and only its constant's name changes. The adjacent-balloon scene is now documented and guarded in pitch currency: the guard asserts both bounds at once - the step across must sit outside the clusterer's merge window (or the scene's two annotated groups would be unreachable) and inside twice a balloon's own leading (or distance alone separates them and the scene tempts nothing). Measured: 26 px leading, 42 px step, 31.2 px bound. |
| 2026-08-11 23:35:00 | docs/PARITY.md, tests/parity_test.go | parity: the grouping factor's **quantity** is part of the invariant, not just its value | The plate-granularity row is rewritten around line pitch and the reference-pitch estimate, and the constants line gains `OCR_MAX_LEADING_RATIO`. The important half is the new one: equal numbers over different quantities is exactly the drift that hurts here - a side that multiplied 1.2 by the ink height instead of the pitch would pass every value check and still group all-caps lettering into different plates. `TestParityOCRClustering` therefore pins the expression that computes the bound and the one that measures a pitch, on both sides, alongside the numbers; the clustering constants are read from their new home (`ocr-cluster.js`) while the resolution and PSM constants stay in `ocr-overlay.js`. Proved by drifting the JS side alone - factor 1.3 **and** a bound recomputed from the ink height - and watching both checks fire. |
| 2026-08-11 23:35:00 | extension/src/ocr-cluster.js, extension/src/ocr-overlay.js, extension/test/ocr-cluster.test.mjs | extension: the pitch gate (parity port), in a module that can finally be tested | Hand port of the desktop change with the same constant names, values and expressions. The clustering moved out of `ocr-overlay.js` into a new pure module for the same reason `ocr-text.js` exists: `ocr-overlay.js` imports the vendored Tesseract bundle at load time and `vendor/` is gitignored, so on a clean checkout nothing inside that file can carry a unit test - the grouping arithmetic is the part that drifts, so it is the part that needed one. `medianOf` moved with it and is imported back for `blockColors`; no behaviour outside clustering changed. The new test feeds both scenes' **real** tesseract line boxes through `clusterLines` and asserts one plate and two, and repeats each case at half scale, because the extension divides coordinates by the upscale factor before clustering while the desktop app clusters in the upscaled space - the decision must not depend on which space it is made in. `npm test` 97/97 (93 before). |
| 2026-08-11 23:35:00 | internal/ocr/tesseract.go, internal/ocr/cluster_test.go | ocr: one balloon is one plate again - the gate measures line pitch, not ink | A speech balloon became three plates, so the browser was handed `ONE WAY TO` as a sentence to translate. The cause was arithmetic, not recognition: lines joined while the vertical **gap** stayed within 1.2 x the median height of the recognized **ink box**, and all-caps lettering with no descenders boxes far shorter than its own line - measured on `synth-balloon-on-panel`, 29-34 px of ink for 72 px of leading, so a 38 px gap missed a 34.8 px bound. Adjacency is now `pitch <= ocrClusterPitchFactor x referencePitch`, top of one line to top of the next; the factor keeps its 1.2 and loses its old name, because a constant called "gap factor" that multiplies a pitch is the next reader's trap. The reference is the **image's** median pitch over successive lines that share a column and sit within a new `ocrMaxLeadingRatio (3)` ink heights - per image and not per cluster on purpose, since a cluster's own pitch is missing exactly at the join that split the balloon, and the ratio is what stops a heading and one distant paragraph from making that single jump the page's "typical" pitch. **Raising the factor was refused as the fix**: it would have bought this balloon at the cost of the adjacent-balloon scene, and the point of changing the quantity was that both could hold. Fixtures are the recognizer's own line boxes, not drawn geometry; the balloon fixture was proved a real guard by disabling the pitch branch (3 plates, red). Lab dev split (`pitch1` vs `loop3`, 8 scored): recall and precision 0.625 -> 0.750, mean IoU 0.695 -> 0.820, worst IoU 0.139 -> 0.349, CER 0.163 -> 0.149, concealment 0.736 -> 0.777, and `merges` 1, `splits` 0, `crossGroup` 6, `clipped` 0, `protectedHitPx` 0 all **unchanged**. Exactly two scenes changed plate count: the balloon 3 -> 1, and `synth-two-columns` 2 -> 1 - not a new merge, since tesseract already reads that scene as three lines each spanning both columns, so the crossing plate is now one instead of two. |
| 2026-08-11 22:00:00 | README.md, README_RU.md, README_UK.md, docs.html, docs.ru.html, docs.uk.html, index.html, {de,it,es,fr,pt,ar,hi,bn,ur,zh}/index.html, extension.html, privacy.html, extension-privacy.html, extension/store/PRIVACY.md, docs/PARITY.md, tests/parity_test.go | send-logs-to-author phase 07: every surface says the same true thing | The three READMEs carry the `-report` row and the feature bullet; the docs trio gains a "Sending logs to the author" section naming the archive field by field, where it is written, the 15 MB cap that drops the oldest logs first, the clear-logs control and the extension's counterpart. The eleven landing pages gain a `#support` note - one sentence, in the support area and **deliberately not in the hero** (owner-confirmed 2026-08-11): the hero sells what the product does to documents, and "send us your logs" in that slot reads as an admission of unreliability. The RTL pages carry no new Latin token at all, so no `dir="ltr"` island was needed. Two facts a reader would otherwise re-derive are now written down. **No attachment is made programmatically**, and that is not a limitation to fix later: `mailto:` cannot carry one and the app opens no mail API of its own, so the archive is put in front of the user instead - folder revealed with the file selected, path on the clipboard - and nothing leaves the machine until the user presses Send. **The extension's shape differs by decision** (ADR-4), not by omission: the browser sandbox hosts no run-log store worth archiving and no way to reveal or attach a file, so it copies a text summary to the clipboard. The three privacy statements say all of this and keep their no-telemetry claim, made precise rather than removed; their "last updated" dates move with the text. PARITY.md pins the five field labels the two reports share (`edition`, `version`, `platform`, `interface language`, `ocr`) and records the divergence, and `TestParityReportFields` fails the build if either side renames one alone. |
| 2026-08-11 21:05:00 | DEV/plan/done/2026-08-11_cli-flags-after-input-are-ignored.md, DEV/plan/done/2026-08-11_ocr-language-mismatch-is-silent.md, DEV/plan/ROADMAP.md | plan: P41 and P42 implemented, moved to done/ | Both tickets carry a "What landed" section written against the measured result, not the intent: the chosen shape and why the other was not taken, the trade each decision accepts, and the commands that were actually run. Their open questions are struck through where answered and left standing where they are not - real batch conversion (P41) and script detection (P42) stay open on purpose. Relative links rewritten one level deeper and every one of them resolved. |
| 2026-08-11 21:00:00 | docs/PARITY.md, tests/parity_test.go | parity: the empty-result language report is a shared invariant | PARITY.md gains the row - both editions name the language data a fruitless pass used, in the same `code (Name)` shape, and both say where to change it. The trigger is an **intentional** difference and is written down as one: the desktop reports at the end of a run it knows is finished, the extension after `OCR_EMPTY_RUN_HINT (3)` empty images, because its queue grows as the reader scrolls and never ends. `TestParityOCRLangReport` guards the pair and was proved to fail by reversing the label shape on the JS side alone. |
| 2026-08-11 21:00:00 | extension/src/ocr-lang.js, extension/src/viewer.js, extension/src/ocr.js, extension/_locales/*/messages.json (13 files), extension/test/i18n.test.mjs | extension: name the recognition language when nothing was found (parity port) | `langLabel` is the twin of the desktop's `LangLabel`. All three empty-result surfaces now carry the sentence: the standalone image, the right-click OCR page, and the lazy in-document/comic path, which needed a counter of images that actually produced a plate. The badge on the picture stays short; the status line carries the reason. New key `ocrNoTextLang` translated into all thirteen locales, and the i18n guard now scans ocr.js too - it resolves messages through its own helper, so a key added only there would have shipped missing from every file. |
| 2026-08-11 21:00:00 | internal/ocr/tesseract.go, internal/ocr/tessdata.go, internal/ocr/tessdata_test.go, internal/pipeline/pipeline.go, internal/pipeline/overlay_report_test.go | ocr: say which language was read, and say when its data is missing | `no text found` is a true sentence about the data that was loaded and reads as a verdict on the picture - the wrong lesson when an English recognizer meets a Russian page, which is the default. The language is now named before the pass (`eng (English)`) and again when nothing at all was recognized, with the way to change it. A missing pack is reported once with its exact `-ocr-download` line instead of one identical engine error per image; the probe asks the engine itself and unions that with the app's own tessdata, and a probe that could not run reports nothing - a wrong "not installed" would skip a pass that would have worked. No recognition parameter moved. |
| 2026-08-11 21:00:00 | internal/config/flags.go, internal/config/flags_test.go, cmd/doc-html-ui/main_test.go | cli: flags written after the input path now take effect | `app book.epub -folder out -force` dropped every flag after the path: output beside the source, `-force` inert, and an explicit `-max-cost` cap gone. Operands now permute behind the flags before parsing, with arity read from the FlagSet (so `-split 5000 book.epub` keeps its value and `-ocr book.epub` does not swallow the path), an undefined flag left for Parse to name, and `--` still ending flag parsing. A second operand - almost always an unquoted Windows path with a space - is refused by name instead of dropped. Flags-first invocations permute to themselves, so nothing that worked changed meaning. The GUI's own argument vector is now run through the real parser in a test. |
| 2026-08-11 19:10:00 | DEV/plan/2026-08-11_cli-flags-after-input-are-ignored.md, DEV/plan/2026-08-11_ocr-language-mismatch-is-silent.md, DEV/plan/2026-08-11_ocr-grouping-splits-one-balloon.md, DEV/plan/2026-08-11_ocr-halftone-defeats-recognition.md, DEV/plan/ROADMAP.md | Four Draft tickets from the OCR fidelity cycle, queued P41-P44 | Everything the grey rescue cycle measured but deliberately did not bundle into it, written as tickets instead of left in a report's closing section. **P41** is not an OCR item at all: Go's flag parsing stops at the first operand, so `app file.png -folder out -force` drops every flag after the path - the book lands beside the source, `-force` and `-notranslate` do nothing, and an explicit `-max-cost` cap disappears. Found while preparing the manual test kit, on the fresh build. **P42**: both editions default OCR to English (`-src` is `en`, the extension's default is `eng`), so a Russian or Ukrainian comic gets no overlay and the only message is `no text found` - a sentence about the language pack that reads as a sentence about the image; the packs can already be listed and downloaded, nothing picks one or says which was used. **P43**: one balloon becomes three plates because the line-gap gate measures recognized ink height (15-17 px on all-caps) instead of line pitch, missing 19-21 px gaps by two pixels. The ticket records that it moves the same constant as lab Step 07.3 in the opposite direction, so the two are decided together rather than in two unmeasured steps. **P44**: halftone screens defeat every thresholder tried, including both grey-rescue rungs; the ticket''s binding constraint is that real screened scenes are annotated before any parameter is chosen, because a fix tuned to the one synthetic pitch-6 screen would be a fix for pitch 6. **Deliberately not ticketed:** the corpus annotation backlog and the extension''s own lab runner, which are already phases of the fidelity-lab plan - duplicating them here would fragment the queue. |
| 2026-08-11 18:45:00 | tools/ocrlab/cmd_run.go | ocrlab: rename the self-diagnosis slice so the typo gate passes | The local slice of self-diagnoses carried a plural spelling that `typos` reads as a misspelt "self", so `./scripts/typo.ps1` and therefore `./scripts/check.ps1` were failing on a Phase 04 file. Renamed to `selfScores`, matching its `scores` sibling in the same function. Local variable only - no exported name, no behaviour, no output change. (The word itself cannot be quoted here: this file is scanned too.) |
| 2026-08-11 18:40:00 | internal/ocr/tesseract.go, internal/ocr/rescue_test.go, internal/ocr/tsv_test.go, internal/ocr/upscale_test.go | ocr: grey rescue ladder for artwork tesseract cannot read | Fourteen of forty lab scenes produced no plates at all, and the cause was not the upscale gate. Tesseract's default thresholder (`thresholding_method=0`) runs Otsu on each RGB channel and marks a pixel as ink only where all three agree, so saturated artwork loses its lettering before recognition ever sees it; separately, one global cut-off cannot survive a varying background, where Otsu splits the gradient itself and a white caption ends up the same value as its ground. The ordinary colour pass is unchanged down to its command line, and only an image that produced **no plates at all** is retried - grey with the engine thresholder, then grey with Leptonica's tiled Otsu. A ladder rather than a replacement because greyscale is not globally better: `commons-right-to-read-poster-1970` drops 38 -> 2 confident words when converted, so retrying only where there is nothing left to lose is regression-free by construction. A rescue is a second guess, so it answers to a stricter floor, `ocrRescueLineConf = 80` - genuine rescued lines measured 93.1-97.0 line confidence, the invented plate that the first attempt painted over two Cyrillic posters measured 50.8. Dev split: failing scenes 5 -> 2, recall and precision 0.375 -> 0.625, concealment 0.433 -> 0.736, merges, splits, clipping and protected-pixel damage all unchanged, total OCR time 5806 -> 5028 ms. |
| 2026-08-11 18:40:00 | extension/src/ocr-overlay.js | extension: grey rescue ladder (parity port) | Hand port of the desktop ladder - `GREY_RESCUE_PASSES`, `greyRescue`, `greyRendition`, `OCR_RESCUE_LINE_CONF` - with `clusterLines` parameterized by its confidence floor so the rescue can demand a higher one. The two editions reach the same place by different means and that is deliberate: a canvas `grayscale(1)` filter leaves an RGBA bitmap whose three channels hold equal luminance, and per-channel Otsu over three equal channels is one decision - measured identical to the desktop's 8-bit grey PNG. The worker's `thresholding_method` is restored in a `finally` so a rescue cannot leak into the next page. Not measured: the extension's own lab runner is Phase 05 and is not built, so this port rests on the parity guard and unit tests alone. |
| 2026-08-11 18:40:00 | docs/PARITY.md, tests/parity_test.go | parity: the grey rescue ladder is a shared invariant | PARITY.md gains the ladder row - what it is, when it fires, the rung order, both numbers, and the intentional implementation difference recorded so that nobody later "fixes" it. `TestParityOCRGreyRescue` compares the rung order by normalized symbol name and the constants against their JS twins, so a change made on one side and not the other fails the build. The guard was proven to fail by temporarily reversing the JS ladder order. |
| 2026-08-11 18:40:00 | DEV/research/ocr_grey_rescue_2026-08-11.md, DEV/plan/2026-08-11_ocr-visual-fidelity-lab/INDEX.md + PHASE_06 + PHASE_07, temp/ocrlab/loop1..loop3 (run artifacts, gitignored) | ocr-visual-fidelity-lab: grey rescue measure-fix cycle | The dated cycle report: what was actually wrong and how it was proven outside the app, why a ladder and not a replacement, the defect the first attempt introduced (two Cyrillic posters read with English data gained a plate of invented words over the artwork) and the floor that closes it, the three-run table read honestly - mean IoU and CER dip because a scene that had recognized *nothing* joined the average with a poor score, not because anything regressed - and what still fails. The next hypothesis is named and deliberately not bundled into a recognition change: `synth-balloon-on-panel` is recognized perfectly but split into three plates because `ocrClusterGapFactor` measures the gap against the ink box height, 15-17 px for all-caps with no descenders, instead of the line pitch. The plan files record that this cycle set a number in Phase 06's territory and changed what a reader sees ahead of Phase 07, on the owner's instruction, so Phase 06 re-derives that number over the full corpus instead of inheriting it. |
| 2026-08-11 18:20:00 | DEV/plan/2026-08-11_ocr-visual-fidelity-lab.md, DEV/plan/2026-08-11_ocr-visual-fidelity-lab/INDEX.md + PHASE_01..08 | ocr-visual-fidelity-lab: tactical plan | Strategic spec promoted Draft-side `Approved` -> `Tactical`, priority 40, and broken into 8 phases. The order is forced by the spec's own rule that nothing may be tuned before it is measured: the corpus contract, ground truth and the metrics all land before the first line of rendering code may change. Two gates are marked **human-owned** and cannot be closed by an agent - acquiring 200+ licence-verified scenes (a person must read each asset's own licence page) and the two-reviewer holdout annotation. Two rows of the spec's edition table are recorded as deliberately out of scope with the reason, rather than dropped. |
| 2026-08-11 18:20:00 | tools/ocrlab/corpus/{corpus,verify,fetch,corpus_test}.go, tools/ocrlab/main.go, DEV/ocrlab/corpus.json, .gitignore | ocr-visual-fidelity-lab phase 01: corpus contract | `ocrlab verify` and `ocrlab fetch`. The licence set is a closed type - PD, PDM, CC0, CC-BY-3.0/4.0, OWN, SYNTHETIC - so a non-commercial or unclear entry cannot be spelled, and `licenceVerifiedBy` is a human-only field no code path fills. The validator reports and never repairs: a hash mismatch stays visible instead of being re-measured into agreement, and the section 4.1 coverage table (per-category floors, 30% holdout, 35% holdout-share ceiling) is checked as named constants. `fetch` is idempotent, credential-free and renames a download into place only after its hash matches. Seeded with the two Wikimedia public-domain images the spec names, media measured and hashed; both wait on the human licence gate and `verify` says so and exits 1. |
| 2026-08-11 18:20:00 | tools/ocrlab/truth/{truth,validate,region,truth_test}.go, tools/ocrlab/synth/{synth,synth_test}.go, tools/ocrlab/cmd_truth.go | ocr-visual-fidelity-lab phase 02: ground truth + diagnostic scenes | Annotations carry transcript, reading groups, permitted replacement areas, protected polygons and a two-reviewer record. `IsTruth()` is the single predicate the scorer consults, and it is false for an OCR-seeded draft - so the engine can never be graded against its own output. `ocrlab seed` writes `<id>.draft.json` and refuses to touch a reviewed file. `ocrlab synth` draws eight diagnostic scenes (uniform paper, two columns, bordered balloon over a panel, adjacent balloons, caption on a gradient, halftone, display type, RTL layout) whose truth is exact by construction and whose PNGs are byte-stable, using the Go fonts already inside `golang.org/x/image` - no new dependency, no network, no licence question. Two plan corrections landed here: "replaceArea inside bounds" was wrong (a balloon interior is legitimately larger than its text) and became "replaceArea must cover its own text"; the adjacent-balloon scene was redrawn after a test proved its 65 px text gap was too wide to tempt the clusterer at all. |
| 2026-08-11 18:20:00 | tools/ocrlab/evidence/evidence.go, tools/ocrlab/metrics/{geometry,recognition,grouping,placement,concealment,damage,replacement,score,metrics_test}.go | ocr-visual-fidelity-lab phase 03: the measurement | One cross-edition evidence record both runners emit, and all eight dimensions of the spec's quality table as pure functions over truth + evidence: CER/WER and detection precision/recall, merges/splits/reading-order inversions, IoU and cross-viewport drift, concealment (covered area, residual ink and the halo band where a block fill leaves ascenders showing), rendered contrast read back from the pixels rather than from the sampled values, protected-area damage named by region, and clipping/collision per stress case. No threshold lives in the package and an AST-parsing test enforces that, so a moved bound and a moved measure can never look alike in a diff. A group the annotation marks less than clear leaves the denominator instead of counting as a miss. |
| 2026-08-11 18:20:00 | internal/ocr/diag.go, internal/ocr/diag_test.go, internal/ocr/overlay.go | ocr-visual-fidelity-lab phase 04: OCR diagnostics sidecar | `DOCHT_OCR_DIAG` makes the overlay append one JSON line per image - geometry, the computed percent style, the sampled paper/ink - so the lab reads what the app actually drew instead of re-implementing `percentStyle` and `blockColors` and then measuring its own copy. Off unless the variable is set, and an output-identity test renders the same page with and without it and fails on any byte of difference. The only shipped-code change in this ticket. |
| 2026-08-11 18:20:00 | tools/ocrlab/runner/{run,convert,browser,probe,stress,probe_test,testutil_test}.go, tools/ocrlab/report/{report,html}.go, tools/ocrlab/cmd_run.go | ocr-visual-fidelity-lab phase 04: the desktop runner | `ocrlab run | score | report`. Each scene goes through the shipped standalone-image path, is rendered at three pinned viewports, and has six deterministic translation-stress strings (short, long Latin, long Cyrillic, Arabic RTL, CJK) swapped into its plates - literals, so no service is called and a clipping result is reproducible. Geometry is read back from the laid-out DOM in the source image's own pixels; screenshots are cropped and rescaled into that same space so concealment can be compared pixel for pixel. `score` runs entirely offline from saved evidence, which is what lets a scoring change be told apart from a product change. Four Windows traps are now guarded and commented: `msedge --version` never exits (it launches a browser), `cmd.Stdout` as a Buffer makes `cmd.Wait` hang forever because browser helpers inherit the pipe, and a relative `--user-data-dir` or `--screenshot` hangs or silently misplaces its output. |
| 2026-08-11 18:20:00 | temp/ocrlab/baseline (run artifacts, gitignored) | ocr-visual-fidelity-lab: first measured baseline | 10 scenes, 8 scored, 2 awaiting human annotation - and **5 of 8 carry a hard failure on the first run**. Four scenes produce no plates at all: the comic balloons, the caption over a gradient and the lettering on a halftone are not recognized, so a reader gets the original text untouched and untranslatable. `synth-two-columns` merges its two columns into one plate, and that plate crosses the other column at every one of the six stress cases. These are the findings the tuning phases exist to fix; no rendering code was changed to chase them. |
| 2026-08-11 16:35:00 | extension/src/diagnostics.js, extension/src/viewer.js, extension/src/options.html, extension/src/options.js, extension/_locales/*/messages.json (13), extension/test/diagnostics.test.mjs | send-logs-to-author phase 06: the extension's copy-diagnostics action | New `diagnostics.js` keeps one `lastRun` record - format, page count, truncated error, timestamp - and composes an always-English report text: version, browser, platform, interface language, the option set, and the last run. The options page's footer became an About block with a Copy-diagnostics button that writes it to the clipboard from the user's own click, so **no permission was added** (`manifest.json` is untouched). The shape differs from the desktop archive on purpose - the browser sandbox hosts no run-log store worth archiving (ADR-4). What it must never carry is pinned by test, not by review: no file name, no page text, no URL, and disabled hosts as a **count** rather than names. The viewer feeds it from three places, and the four `page-total` assignments were funnelled into one `setPageTotal` helper so a reader added later cannot leave the record without a page count. |
| 2026-08-11 16:05:00 | internal/config/flags.go, internal/app/app.go, internal/i18n/i18n_cli.go, tests/ui_cli_parity_test.go, internal/config/flags_test.go | send-logs-to-author phase 05: `-report` on the command line | `doc-html-translate -report` builds the same archive `report.Build` builds for the GUI and prints where it landed - no input file required, because a report is needed exactly when the last run failed. Two localized console lines ("Report written to:", the dropped-logs notice), each with the twelve translations the init-time guard demands; the path itself prints raw so it can be copied out of the console. **The CLI opens no mail program** - that is the GUI's job, and neither `mailto` nor `explorer` appears in `internal/app`. `report` joins the `guiNative` allow-list in the parity test: the GUI serves it natively through the About button and `/api/report` rather than by forwarding the flag. Proven by running it: exit 0, correct path, `Отчёт записан в:` under `-ui-lang ru`, and the archive carried the run log the same binary had just written. |
| 2026-08-11 15:40:00 | cmd/doc-html-ui/i18n.js, cmd/doc-html-ui/ui.html, cmd/doc-html-ui/i18n_test.go | send-logs-to-author phase 04: the About section and its one button | The settings page gained an "About the program" section - version, edition, converter engine, how many run logs are held - and three real `<button>` controls: send logs, open the archive, clear the store. One press builds the archive, copies its path, opens its folder with the file selected, and hands a prefilled `mailto:` to the user's own mail program; **the wording never claims the app attached anything**, because it did not (ADR-3). 21 keys × 13 languages: the 19 the plan listed plus `aboutEngineFound`/`aboutEngineMissing` (the engine line renders from a boolean and would otherwise be hardcoded English) and `aboutSite`/`aboutFeedback` for the two links. The address reaches the page only through `/api/env` - `TestGUIMailAddressComesFromEnv` fails if `sza@ukr.net` is written into `ui.html` a second time. The inline clipboard code became a shared `copyToClipboard` helper, and the About values are kept as facts and re-rendered by `renderAbout()` on a language switch rather than frozen in whichever language answered first. **Measured, not asserted:** the built exe was driven at `?lang=ar` under a temp `%LOCALAPPDATA%` - `<html lang="ar" dir="rtl">`, the whole block Arabic, `aboutMail` carrying the served address - and a live `POST /api/report` returned a readable archive. |
| 2026-08-11 14:58:00 | cmd/doc-html-ui/report.go, cmd/doc-html-ui/main.go, cmd/doc-html-ui/report_test.go | send-logs-to-author phase 03: the GUI service can build, reveal, open and clear | Four endpoints - `/api/report` (POST, builds the archive from the saved GUI settings and answers path, dropped count and size), `/api/report-reveal` (Explorer with the file selected), `/api/report-open` (the archive itself, so the user can look before sending) and `/api/logs-clear`. Both path-taking handlers share **one** containment check: a path outside `report.Dir()` is refused, because a handler that opens whatever path it is handed is a file manager for the whole disk. Explorer is started, not waited on - it exits non-zero even when it did open the window, so "the process started" is the only honest signal. `/api/env` gained `logs` (count + bytes) and `author`, the latter served from a new `authorEmail` constant so the page does not carry the address a second time. |
| 2026-08-11 14:35:00 | internal/report/redact.go, internal/report/environment.go, internal/report/archive.go, internal/report/redact_test.go, internal/report/archive_test.go | send-logs-to-author phase 02: one archive, safe to send | `report.Build` writes `%LOCALAPPDATA%\doc-html-translate\reports\report_doc-html-translate_<version>_<yyyymmdd-hhmm>.zip` holding `environment.txt` (an always-English `key: value` block - version, edition, platform, interface language, tesseract, OCR languages, Ollama model), a redacted `settings.json`, and the recent run logs newest-first under `logs/`, stopping at a 15 MB cap and reporting how many logs it had to drop. Everything written passes through `Redact`, which is the gate rather than a courtesy: labelled 20+ character credentials and bare `AIza..` tokens become `<redacted>`, and the user's own folders collapse to `%LOCALAPPDATA%` / `%USERPROFILE%` - the more specific location first, since it sits inside the other. Document **file names survive on purpose**: a log whose document cannot be identified cannot be acted on. `TestBuildNeverArchivesTheAPIKeyFile` and `TestBuildRedactsSettings` pin that the Google key can reach the archive by neither route. |
| 2026-08-11 14:12:00 | internal/report/store.go, internal/report/store_test.go, internal/logging/log.go, internal/logging/log_test.go, cmd/doc-html-translate/main.go | send-logs-to-author phase 01: run logs survive the session | New package `report` owns the per-user store: `Dir()` under `%LOCALAPPDATA%\doc-html-translate` (the only location writable under the read-only MSIX install), `LogsDir()`, `RunLogPath(at)` taking the clock as a parameter so tests are deterministic, and `Trim()`/`ClearLogs()` bounding the store at 20 files or 20 MB, oldest-first. `logging` gained a run-log tee - `StartRunLog`/`StopRunLog` plus an `emit` helper that all four printers now route through; the console text is unchanged and the file copy of `Progress` carries a whole line instead of the in-place carriage return. The CLI opens the run log right after `logging.AppVersion`, before the version line, and **swallows every error on that path**: a store that cannot be written leaves the run untouched and says nothing, so logging can never fail a conversion (`TestRunLogWriteErrorIsIgnored` pins it). |
| 2026-07-30 01:25:00 | cmd/doc-html-ui/main_test.go, DEV/plan/done/2026-07-01_app-ocr-image-overlay.md, DEV/plan/ROADMAP.md, DEV/plan/done/*.md | OCR umbrella closed: the by-hand release smoke, run and measured; four new GUI tests; 24 broken plan links repaired | `2026-07-01_app-ocr-image-overlay` had sat `Partial` since its three defect children landed on 07-17, because what was left was a by-hand smoke nobody had run. All four criteria are now measured, not asserted. **Criterion 1's translation half - the one the July sweep could not show, having run `-notranslate`:** `-ocr -ollama -ollama-model aya-expanse:8b` on a comic page produced 4 `ocr-fig` containers with 13 `ocr-box` plates whose text came out **Russian**, so the plates really are ordinary translatable text the app's own engine rewrites in place (`gemma3:12b`, the flag default, is not installed here). On a scanned PDF in `-multipage`: `5 image(s) overlaid, 1 with no text found`, plates 7/21/9/6/5 across `page_002..006`, and `verify-html.ps1` reports all 7 pages OK with `broken=0`. **Criterion 2:** `-ocr-download rus` really fetched 3,861,738 bytes and the catalog flipped to `installed`. **Criterion 3 was recorded as "met by design" and is now measured:** with `DOCHT_TESSERACT` pointing at nothing **and** Tesseract off `PATH`, the conversion exits 0, prints `OCR skipped: tesseract executable not found (..)`, and writes the page with zero plates. **Criterion 4** was driven against a live `doc-html-ui` started with `LOCALAPPDATA` redirected to a temp profile and no CLI beside it, so it touched neither the real `ui-settings.json` (still stamped 07-17) nor HKCU: the dumped DOM shows `ocrLang` holding exactly the installed pair and `ocrDownloadSel` only the rest, and a live `POST /api/ocr-download {"lang":"ukr"}` installed a language. The OCR path had **no GUI test at all**, so a dropped `-ocr` was indistinguishable from OCR being off - now `TestAssembleArgsForwardsOCRAndLanguage` (mutation-checked: forwarding disabled makes it fail), `TestAssembleArgsOmitsOCRWhenToggleIsOff`, `TestHandleOCRLangsListsCatalogWithInstalledFlag` and `TestUIMarkupExposesOCRControls`. **One trap recorded, not changed:** `ocr.Locate` treats `DOCHT_TESSERACT` as a hint, so a typo'd override silently falls through to `PATH` - visible only because the run logs which engine it picked, which is how it was noticed. **Housekeeping the move to `done/` had skipped:** 24 relative links across the plan tree were dead (`../../docs/PARITY.md` from `done/` needs three levels, four ROADMAP rows still pointed at `DEV/plan/` for files already moved). All repaired and checked - every link under `DEV/plan` now resolves. |
| 2026-07-30 00:55:00 | extension/scripts/make-screenshots.mjs, extension/scripts/_fixtures.mjs, extension/package.json, extension/src/viewer.html, extension/_locales/*/messages.json, extension/store/screenshots/*.png, extension/store/LISTING.md, DEV/DOCS_SURFACES.md | Thirteen UI languages, phase 07 closed: the extension's store screenshots in 13 languages | The last open step of the 13-language ticket, left undone in July because capturing the viewer needs a real `chrome-extension://` origin. It needs no dependency after all: Node 22+ ships a global `WebSocket`, which is all the DevTools protocol requires, so `make-screenshots.mjs` is a hand-rolled CDP client - 26 PNGs at 1280x800, `npm run screenshots`. **`--load-extension` is dead in branded Chrome** (it logs `not allowed in Google Chrome, ignoring` and starts without the extension); the working route is the DevTools `Extensions.loadUnpacked` domain, which needs no extra switch and returns the extension id, so nothing has to derive it. The id arithmetic is recorded in the ticket only because it pinned the diagnosis: on Windows an unpacked id hashes the path as **UTF-16LE**, and that answer matched the id in the address bar of the old hand-made frames, which is how we knew the extension was not loading. Fixtures are generated rather than read from the gitignored `test_doc/`, mirroring `tools/store/make-screenshot.ps1`: a small Helvetica PDF and a real EPUB 3 with a `properties="nav"` nav document, both from the same public-domain Alice passage the desktop screenshots use, zip timestamps pinned to the DOS epoch so two runs are byte-identical. The RTL guard could not be ported from the desktop script - `applyViewerChromeI18n` translates `#toolbar`/`#toc` only and sets no `lang` on them - so the check compares the contents heading against `tocHeading` in the locale file, which is what actually proves a language landed instead of falling back to English. **The capture immediately earned itself:** `#btn-save-src` and `#btn-save-html` had localized tooltips but hard-coded English labels, so `↓ File` and `↓ HTML` would have shipped in English on all 13 listings; new `btnSaveFile`/`btnSaveHtml` keys in all 13 locales fix the first, and `↓ HTML` stays HTML everywhere because it is a format name. `npm test` 88/88, `go test ./tests -run TestTypography` ok. |
| 2026-07-29 01:45:00 | tools/store/listing/{en,ru,uk}.txt, tools/store/build-store-listing-csv.ps1, tools/store/listingData.csv, winget/*.yaml | Release 26.0729.0134: "what's new", and the one field a fill-only generator cannot fill | Release notes for 26.0729.0134 written in the three author languages, leading with the 13 interface languages and closing on the machine-translation disclosure. The listing generator was fill-only by design - which is right for the asset-URL cells and wrong for `ReleaseNotes`, the single field rewritten every release - so it gained `-Refresh <fields>`, with a guard that **refuses** to refresh any Screenshot or Logo row: overwriting one of those detaches an image already uploaded to the dashboard. Run: 0 filled, 2 refreshed. All 15 winget manifests stamped to the release version, with the installer URL, `ReleaseDate` and `ReleaseNotesUrl` pointed at the new tag; `InstallerSha256` still holds the old hash and is updated after the GitHub Release exists, because the artifact it hashes does not exist yet. |
| 2026-07-28 20:40:00 | extension.html, extension/README.md, AGENTS.md, configs/.typos.toml | Thirteen UI languages: the surfaces the release checklist names, and a language-scoped typo gate | Two surfaces from the release checklist's "Docs & site" step still described a English-only extension: `extension.html` now leads its hero with the 13 languages in ru/en/uk, and `extension/README.md` documents `src/i18n.js`, the `_locales` set and the chrome-only RTL rule. `AGENTS.md` gains `internal/i18n` in the architecture map (with the resolution order and why the language is process-wide state rather than a parameter threaded through `htmlgen`), plus two invariants: the interface language never reaches `<html lang>`, and house typography is scoped to en/ru/uk. **The typo gate needed the same treatment as the typography guard in phase 01:** `typos` carries an English dictionary and no per-language mode, ordinary German, French, Italian and Portuguese words were reported as misspellings of the English words they resemble - 30 files, 300+ false positives, and `./scripts/check.ps1` red. (Quoting the offending words here would have failed the gate on the changelog itself, which is the point in miniature.) The five Latin-script language surfaces are now excluded by path with the reason recorded in the config; the non-Latin ten need no entry because a non-Latin script is skipped anyway, and every English file stays in scope by being listed out rather than excluded wholesale. Three loanwords that sit inside otherwise-English files (the extension listing's table of store names, the installer's `[CustomMessages]`) are whitelisted as words instead, so those files keep their English coverage. `./scripts/check.ps1` green. |
| 2026-07-28 19:05:00 | tools/store/listing/*.txt, tools/store/build-store-listing-csv.ps1, tools/store/listingData.csv, winget/*.locale.*.yaml, extension/store/LISTING.md, docs/PARITY.md, DEV/DOCS_SURFACES.md, docs.html, docs.ru.html, docs.uk.html, index.html | Thirteen UI languages: listings, docs and the closing sweep (phases 08-09) | **Listings.** 13 `tools/store/listing/<code>.txt` sources (title, short and long description, 13 features, 7 search terms), with generator guards for the 200-char short description and the 21-word-per-language search-term cap that fails a whole import. New `build-store-listing-csv.ps1` patches a Partner Center export instead of regenerating it: empty cells only, so the listing-asset URLs survive, and `-FillNothing` proves a **byte-identical** round trip before any patching run is trusted. Two corrections against the plan: the export is UTF-8 **with** BOM (Partner Center's own encoding; without it Cyrillic, Arabic and CJK import as mojibake) and quoting stays minimal, because quoting every field would have destroyed the round-trip check. No locale column is invented - an unknown one is dropped silently on import, so the script names the 11 missing locales and tells you to add them in the dashboard first. 12 new winget locale manifests, `winget validate` clean on all 13. **Docs.** `docs/PARITY.md` gains the shared invariant - one language set on both sides, and the rule that the interface language dresses the chrome only while `<html lang>` stays the document's, because breaking it stops Chrome offering "Translate page". The line calling interface localization a Go-only reader feature is gone; it is no longer true. `DEV/DOCS_SURFACES.md` is rewritten around two explicit tiers (13-language surfaces vs the three-language trio) with every file from phases 06-08 listed. The docs trio gains an "interface languages" section and the missing `-ui-lang` flag row, and the landing hero now leads with the feature in ru/en/ua. **Deliberately not translated:** the extension's long store description stays en/ru/uk - Chrome review rejected that exact copy twice for keyword spam and the wording that passed is tuned word by word, so a machine translation is a submission risk; its name and short description are localized for all 13 and served automatically from `_locales`. |
| 2026-07-28 18:35:00 | installer/doc-html-translate.iss, msix/AppxManifest.xml | Thirteen UI languages, phase 08 (2/7): installer and MSIX languages | The installer wizard spoke three languages and the MSIX declared one, so the Store had no way to offer the listing in the ten new locales. `[Languages]` grows to 8 with the official Inno `.isl` files (`de it es fr pt-BR`) and 24 `[CustomMessages]` lines. Arabic, Hindi, Bengali, Urdu and Chinese are deliberately **not** added - Inno ships no official `.isl` for them and a hand-made translation of the wizard's own text is not something this project can keep correct; they fall back to English in the installer only, and a comment above `[Languages]` says so, so nobody "completes" the list later (strategic D2). `AppxManifest.xml` now declares all 13 `<Resource Language>` entries in Store locale tags, with `Identity` untouched. `./scripts/build-installer.ps1` compiles clean. Remaining in this phase: the 13 listing source files, the CSV generator port, the regenerated `listingData.csv`, the 12 winget locale manifests and the extension listing sections. |
| 2026-07-28 18:25:00 | tools/store/make-screenshot.ps1, tools/store/make-gui-screenshot.ps1, tools/store/*.png, cmd/doc-html-ui/ui.html | Thirteen UI languages, phase 07 (4/5): store screenshots per language | The listing had two English frames for a product that now speaks 13 languages, which is exactly the gap that makes a Partner Center locale count as incomplete. `make-screenshot.ps1` gained `-Language` (all 13 by default), drives `-ui-lang` per run and names output by Partner Center locale (`en-us`, `pt-br`, `zh-hans`). New `make-gui-screenshot.ps1` captures the GUI: because `doc-html-ui` is a local HTTP app that never prints its port, the script finds it through the OS connection table and shoots `?lang=<code>` - a new URL-only override in `ui.html` that is deliberately never persisted, so a 13-language run cannot overwrite the developer's saved language (settings are still backed up and restored in a `finally`). Both scripts assert the RTL rule **before** writing a PNG: the chrome must carry `lang="<code>"`, must mirror only for `ar`/`ur`, and the document root must never be `dir="rtl"` - a wrong frame throws instead of reaching a store listing. No bitmap flipping: a mirrored image would be the bug, not the fix. Result: 39 PNGs (13 × reading view + TOC + GUI) at 1366×768; the two language-less files are gone and nothing outside `DEV/plan/` referenced them. **Step 07.5 is not done:** capturing the extension needs a real `chrome-extension://` origin (its `chrome.i18n`/`chrome.storage` do not exist on `file://`), which means a Puppeteer dependency the extension does not have or a hand-rolled CDP client - left out rather than half-built. The four existing en/ru extension PNGs stay valid. |
| 2026-07-28 18:05:00 | index.html, extension.html, privacy.html, extension-privacy.html, {de,it,es,fr,pt,ar,hi,bn,ur,zh}/index.html, sitemap.xml, robots.txt, README.md, README_RU.md, README_UK.md, docs.html, docs.ru.html, docs.uk.html | Thirteen UI languages, phase 06: site and READMEs | The site spoke three languages while the product now speaks 13, and search engines had no way to learn the other ten existed. Ten complete landing pages under `/<code>/` (hero, use cases, how-it-works, install with the winget command, the free-vs-paid note, documentation links) - single-language pages, no `data-l` machinery, each self-canonical. Every root page gained a 13-entry `hreflang` block plus `x-default`; because the ru/en/uk versions live on one URL, the root boot script now honours `?l=ru\|uk\|en`, which is what gives those three addressable URLs to point `hreflang` at. RTL (`ar`, `ur`) mirrors the layout while keeping the product name, format pills, `winget` command and `Chrome / Edge` inside `dir="ltr"` islands - verified by a headless render at 1366 px. New `sitemap.xml` (17 URLs, each carrying all 14 alternates) and `robots.txt`; neither existed before. `README_RU.md` and `README_UK.md` are full translations, cross-linked with the English one; all three gained the 13-language feature bullet and the `-ui-lang` row that phase 03 had left undocumented. The machine-translation disclosure (strategic D5) now appears in 16 places: 3 READMEs, 3 docs pages, 10 landing pages. **Caveat:** a 360 px headless render clips content on the new pages - and clips the pre-existing root page identically, so it is the headless renderer ignoring the viewport meta, not a regression; a real narrow-viewport check is still owed. |
| 2026-07-28 17:40:00 | extension/src/i18n.js, extension/src/viewer.js, extension/_locales/*/messages.json | Thirteen UI languages, phase 05 complete: viewer status and error strings localized | Closes the gap left by the entry below. All 50 remaining user-facing literals in `viewer.js` - load/parse status ("Reading EPUB..", "Rendering page 3 / 40"), every failure notice, the PDF password dialog, the "little or no text" banners, TOC fallback labels - now go through `t("key", "English fallback")`. `t()` gained trailing substitution arguments filling `{1}`, `{2}` markers: counted messages are translated as whole sentences instead of glued fragments, so word order and plural wording stay correct per language, and `{n}` (rather than Chrome's `$1`) keeps every `messages.json` valid without a `placeholders` block. Singular/plural pairs get separate keys (`vComicReadyOne` / `vComicReady`). 66 new keys added to all 13 locales (119 keys each, 123 for en/ru/uk which also hold listing copy). Reuses the existing `ocrProgress`/`ocrNoText`/`ocrDone` keys rather than duplicating them. `npm test` 88/88, `go test ./tests -run TestTypography` green over the new locales, `node build.mjs zip` OK (6.2 MB). |
| 2026-07-28 16:45:00 | extension/src/i18n.js, extension/src/{viewer,options,popup}.{html,js}, extension/_locales/*/messages.json, extension/test/i18n.test.mjs | Thirteen UI languages, phase 05 (partial): extension chrome localized, 13 locales | The extension's own interface was English-only: `_locales` existed but carried 20 keys, none of them viewer UI. New `src/i18n.js` adds `t()`/`uiLang()`/`applyI18n()` over `chrome.i18n` plus an explicit interface-language override (options page, default "follow the browser") - a reader on an English work browser still reads books in their own language, and the override is also what makes 13-language screenshots possible without 13 browser profiles. Toolbar, TOC panel, popup and options markup are tagged with `data-i18n*`; `_locales` grew from 3 to **13** directories with 53 runtime keys each (`de it es fr pt ar hi bn ur zh_CN` new, `ru`/`uk` topped up). RTL: `applyI18n` mirrors the **chrome** only - the rendered document keeps its own direction and `<html lang>`, matching the Go rule from phase 03. Guarded by `extension/test/i18n.test.mjs` (13 dirs, identical runtime key sets, no empty message, every markup and `t()` key defined); `npm test` 88/88. **Not yet done in this phase:** the ~40 runtime status strings inside `viewer.js` ("Reading EPUB..", "Couldn't load this document", ..) are still English literals; the long `storeDescription` and screenshot captions stay en/ru/uk and land with the rest of the listing copy in phase 08. |
| 2026-07-28 16:05:00 | cmd/doc-html-ui/i18n.js, cmd/doc-html-ui/ui.html, cmd/doc-html-ui/main.go, cmd/doc-html-ui/i18n_test.go | Thirteen UI languages, phase 04: the GUI in 13 languages | The dictionary left `ui.html` for its own served file (`/i18n.js`, `go:embed`): at 13 languages × 88 keys it would have been four fifths of the markup, and `ui.html` shrank from 1318 to ~1050 lines instead of growing past 2200. Ten new languages added (de it es fr pt hi bn zh ar ur), each complete - the three-button EN/RU/UK switch became a `<select>` of endonyms built from the dictionary itself. RTL: `documentElement.dir` follows the language for ar/ur; script fonts come from `FONT_STACKS` (Nirmala UI for hi/bn, Microsoft YaHei UI for zh - both ship with Windows, and the GUI runs offline so a webfont is not an option), with `/api/env` now reporting the resolved language and its font. The window's language rides along to the converter as `-ui-lang`, so a book converted from a Bengali window gets Bengali navigation. Guarded by `TestGUIDictionariesCoverEveryLanguage` (13 identical key sets), `TestGUIMarkupKeysExist` (no markup key without a translation) and `TestGUIDictionaryValuesAreNotEmpty`. `./scripts/build-ui.ps1` green. |
| 2026-07-28 15:40:00 | internal/config/flags.go, internal/app/app.go, internal/app/splash.go, internal/app/splash/*.txt, internal/i18n/i18n_cli.go, internal/i18n/i18n_reader.go, internal/htmlgen/navbar.go, internal/htmlgen/htmlgen.go, internal/htmlgen/singlepage.go, internal/syslocale/*, cmd/doc-html-ui/main.go, tests/smoke_test.go | Thirteen UI languages, phase 03: console output and converted-page chrome in 13 languages, new `-ui-lang` | The CLI's welcome screen moved from two ~49-line `fmt.Println` walls to 13 embedded resources (`internal/app/splash/<code>.txt`, `{{rule}}` placeholder for the horizontal rule) and the nine `if syslocale.IsRussian()` branches became `i18n.S` calls; `IsRussian` is deleted. The converted page's navigation and reader controls now speak all 13 languages, including the three strings that were English-only before (`Chapters: N`, and the Serif/Sans/Mono labels). New flag `-ui-lang <code>` selects the language of both, validated against `i18n.Codes` and refused on a typo (a silent English fallback would produce 13 identical "translated" screenshots); the GUI forwards its own selector through it. **Page-language invariant:** the chrome declares `lang="<ui>"` (plus `dir="rtl"` for ar/ur) on the navbar and toolbar only, while `<html lang>` keeps carrying the *document's* language - that attribute is what makes Chrome offer "Translate page", the product's free flow. Verified: `TestConvertedChromeLanguage` (en/zh/ar) plus a real Arabic conversion showing `<html lang="en">` with `<div class="dht-navbar" lang="ar" dir="rtl">`, `verify-html.ps1` 2/2 pages OK. Full gate `./scripts/test.ps1` green. |
| 2026-07-28 15:32:00 | internal/i18n/i18n.go, internal/i18n/i18n_test.go, internal/syslocale/locale_windows.go | Thirteen UI languages, phase 02: the Go localization layer | New `internal/i18n`: `Codes`/`Names` for the 13 shipped languages (English is index 0 and the key language, so a key *is* its English text), `Add`/`T`/`S`, `IsRTL`/`Dir`, `FontFamily` (Nirmala UI for hi/bn, Microsoft YaHei UI for zh - both ship with Windows; the GUI runs offline so a webfont is not an option), and `Resolve(explicit, saved, system)`. The interface language is process state set once at start-up rather than a parameter threaded through every `htmlgen` signature. `Add` panics on a wrong column count, and `TestEveryKeyHasTwelveNonEmptyTranslations` fails on an empty column - an empty translation falls back to English at runtime and looks fine, so only a static check catches a language left behind. `syslocale.Lang()` now maps all 13 primary LANGIDs instead of 2. No caller moved onto the layer in this phase. |
| 2026-07-28 15:20:00 | tests/typography_test.go | Thirteen UI languages, phase 01: scope the typography guard to the author languages | The house style (`..` not `...`, short hyphen, no `…`) is an *author-language* rule, but the guard applied it to every string in the repo - which would fail the 13-language work on correct punctuation: Chinese writes `——` and `……`, German `–`, Arabic `؟`. `badTypography` now takes the language: en/ru/uk keep all three rules (including the `<title>` em-dash exception), every other language is checked only for an ASCII `...`, and not even that in Chinese. The `_locales` scan keys on its directory name (`pt_BR` -> `pt`), with an unknown directory falling to the weaker rule rather than the house style. New `TestTypographySplashResources` scans `internal/app/splash/*.txt` (the per-language console text phase 03 adds; an empty glob passes), and `TestBadTypographyLanguageScope` pins the rule itself with 10 cases. |
| 2026-07-18 02:15:00 | msix/AppxManifest.xml, extension/store/LISTING.md, extension/store/PRIVACY.md, extension.html | Release prep: metadata + store-copy polish for the 26.0718 release | The 2026-07-18 corpus re-sweep found the 07-17 feature work never reached the shipping *metadata*. The MSIX `<uap:VisualElements>` Description listed only the nine doc formats while the file associations already register `.cbz/.cbr/.cb7/.cbt` - it now names comic archives and images. Store copy brought to the house rule (short hyphens, `..`): em dashes in `LISTING.md`/`PRIVACY.md` and the `...` menu-path placeholders reduced to `-`/`..` (they slipped the typos gate, which does not scan `.md`/`.html` for dashes). The winget en-US locale description is refreshed at submit time (Step G). No code change - comics were already surfaced across README/site/docs in en/ru/uk by the feature commit. |
| 2026-07-18 00:18:00 | internal/{comic,pdf,pipeline,mobi,translator,app,htmlgen}/*.go, extension/_locales/{en,ru,uk}/messages.json, tests/typography_test.go, CLAUDE.md, DEV/plan/done/2026-07-17_output-typography.md | App + Extension: stop breaking the app's own typography rule in generated output | Roadmap P13. CLAUDE.md's rule - "short hyphens (no long dashes), '..' not '...'" - "applies to generated output", but the output broke it in ~42 places: `...` where the rule says `..` (`[1/4] Extracting PDF...`, `Loading model into VRAM...`, `Installing Poppler via winget...`, and the excerpt-truncation `…` in `htmlgen.go`), and long dashes in user-visible chatter (`Single-page mode — ..`, `Calibre not found — install ..`, `Google Translate — Cost Warning`, `PDF Quality Reduced — pdftotext Unavailable`). Swept every log line, dialog title, and error string across the seven affected Go packages to `-`/`..`; the extension carried the same drift in exactly three strings - the `_locales/{en,ru,uk}` "Right-click, Translate to .." message - fixed in all three languages (its JS UI text is otherwise all in `_locales`, and no user-visible `—`/`…` lives in `extension/src`). **The one product decision** (settled with the owner): the converted page `<title>Book — Page N</title>` **keeps its em dash** - "Alice in Wonderland — Page 5" is conventional book typography, not app chatter - so the five extractors' title lines are left untouched, and the exception is recorded in CLAUDE.md. **Guard against re-drift:** `typos` (the existing `configs/.typos.toml` gate) is a spell-checker and cannot ban a dash or an ellipsis, so the guard is a source-scanning test, `tests/typography_test.go`, beside the existing repo-scan guards in `parity_test.go` - it AST-walks every non-test Go string literal under `internal/` and `cmd/` and every `_locales` message value, failing on `—`/`…`/`...`, and allows an em dash only inside a literal that also contains `<title>` (the one exception, pinned tight). Ticket moved to `DEV/plan/done/`. `scripts/test.ps1` green. |
| 2026-07-18 00:15:00 | internal/ocr/overlay.go, internal/ocr/batch_test.go, internal/pipeline/pipeline.go, DEV/plan/2026-07-17_ocr-pool-per-book.md | OCR: batch recognition per book, not per content file (`-multipage` was serial) | Roadmap P11. The overlay is advertised as running "across a process pool" of `tesseract` workers, and on a single-page book it does. On a **`-multipage`** book it silently degraded to serial: the pool was scoped to the images found *inside one HTML file*, while the overlay loop walked content files one at a time - and a page carrying one image (every comic page, every scanned PDF page) handed the pool a one-item queue, so its width bought nothing. The default single-page path was never affected (the whole book is one content file there, so the pool already saw every image at once), which is why the defect hid. The fix lifts the batching boundary from one file to the **whole book**: new `OverlayBook(bin, htmlPaths, ..)` runs three phases - **(1)** parse each file and collect the recognizable image paths, deduped by absolute path in first-seen order (docs released); **(2)** `recognizePaths` runs one `ocrWorkers()`-wide pool over *every* image at once; **(3)** re-parse each file one at a time and wrap its images from the precomputed `map[path]recognition`. Recognition needs only the image path (a self-contained `tesseract` shell-out), so it decouples cleanly from the DOM; only the small results map (text + geometry) is held across the book - never every parsed page or decoded image - so memory stays close to the old per-file cost (the ticket's explicit watch). `decodeImage` (plate colours) and the HTML rewrite stay in phase 3, one file at a time, because the tree is not safe for concurrent mutation. `pipeline.overlayImages` now builds the content-file list and makes one `OverlayBook` call under a single book-wide ticker, so progress stays **per image** (not per file - a per-file counter would sit at 0/1 through a single-page run). `OverlayFile` is retained as `OverlayBook` over a one-file book, which also makes it the **faithful "before"** for measurement: looping it per page reproduces the old serial path exactly, not an estimate. **Measured** with the real `tesseract` on `cbz-tiny_Nyoka-the-Jungle-Girl-071` (37 pages, `-multipage`, 20-core / pool 16): BEFORE (per-file loop) **67.2s @ 0.55 img/s**, AFTER (one `OverlayBook`) **11.0s @ 3.35 img/s** - **6.1x**, identical output (36 overlaid, 1 no-text, 0 failed). Best-effort contract preserved: a missing tesseract, an unreadable page, or a single failed image still never aborts the book. `internal/ocr` gains its first batching tests (`batch_test.go`): `collectBookImages` (cross-file collect + dedup + first-seen order, external/missing filtered - the boundary itself) and `applyOverlays` (the three outcomes + changed flag), plus a no-op test proving an image-less book touches no engine. `docs/PARITY.md` "OCR execution" row stays true (Go still OCRs eagerly across a pool; only the pool's real width changed) - the extension is untouched (its OCR is lazy on scroll, no equivalent defect). `scripts/test.ps1` green. |
| 2026-07-17 23:50:00 | internal/comic/*, internal/pipeline/pipeline.go, internal/windowsreg/*, cmd/doc-html-ui/main.go, internal/app/app.go, installer/doc-html-translate.iss, msix/AppxManifest.xml, tests/testdoc_test.go, tests/parity_test.go, extension/src/comic.js, extension/src/viewer.js, extension/src/viewer.css, extension/src/viewer.html, extension/src/background.js, extension/manifest.json, extension/test/comic.test.mjs, docs/PARITY.md, scripts/parity-check.ps1, DEV/plan/2026-07-17_comic-archives.md | New input format: comic archives (CBZ / CBR / CB7 / CBT) across every edition | Roadmap P12. A comic archive is a container of page images with **no text layer at all** - every word lives in the artwork - so, like a standalone image, opening one forces the OCR overlay on (independent of `-ocr` / the "Use OCR for images" toggle): each page is wrapped in a one-page HTML doc and its bubbles become translatable plates, so the browser's "Translate page" works on them. Before this, a `.cbz` was fed to the text extractor (garbage) on desktop and to the EPUB reader (misleading "not an EPUB") in the extension. **The four extensions are three containers**, which decides feasibility per edition: CBZ is ZIP (stdlib `archive/zip` / the extension's central-directory reader), CBT is TAR (stdlib `archive/tar` / a small hand-rolled reader) - both pure, no dependency; CBR is RAR and CB7 is 7z, which have no pure-Go/JS decoder, so the desktop app **shells out to 7-Zip** and the extension **declines them** with a "use the desktop app" notice. **Go** (`internal/comic`): `Extract` dispatches by extension, filters page entries, sorts them, writes `page_NNN.<ext>` + an HTML wrapper per page, and returns an `epub.Book` the single-page pipeline runs; `pipeline.go` gains a `comic.IsComic` arm that forces OCR (mirroring `internal/img`). 7-Zip is located by `LookPath` **plus a probe of known install paths** - measured: 7-Zip's installer registers itself in neither the machine nor user `Path`, so a bare `LookPath` fails on a machine that *has* it (the same trap the MOBI/Calibre `findEbookConvert` avoids, followed fully here). Archive-hardening caps (per-page 200 MB, 4 GB inflated total, 20 000 pages) refuse a decompression bomb. **Extension** (`comic.js`): a memory-safe **lazy** reader - the central-directory walk and TAR walk return one loader per page that inflates just that page on demand, so a large comic never inflates every page at once; the viewer lays out placeholder sections up front (scrollbar reflects the whole book) and inflates + OCRs each page only as it nears the viewport (the scanned-PDF pattern), OCR forced on via `registerImagesForOcr(.., true)`. The **ZIP-vs-ZIP ambiguity** (EPUB and CBZ are both `PK..`) is broken by the filename extension without regressing the EPUB hot path; the `&#8595; HTML` export warns on a partial (un-scrolled) save, mirroring the PDF path. **Two new shared invariants**, both guarded so they cannot drift: **natural (numeric-aware) page order** - page order *is* archive entry order by filename, so `page2.jpg` must precede `page10.jpg` (`naturalLess` == `naturalCompare`) - and the **page-entry filter** (`png jpg jpeg gif webp bmp`; TIFF excluded; `ComicInfo.xml`/`Thumbs.db`/`__MACOSX/`/dotfiles ignored) with a new `TestParityComicPageFilter` asserting the two sides' extension sets match. **Registration**: `.cbz/.cbr/.cb7/.cbt` added to the Windows handler list, the GUI picker (new "Comics" filter group), the MSIX `<uap:FileType>` rows, the installer's context-menu sweep, the CLI help (en/ru, noting CBR/CB7 need 7-Zip), the extension's `CONVERT_EXTS` (all four) + DNR interception (cbz/cbt only) + `web_accessible_resources` + the file-picker `accept` list (which was also **missing `.mobi`/`.azw3`** - fixed here). `docs/PARITY.md`: new port-map rows, the two invariants, and the CBR/CB7 extension decline under Intentional divergences. Gate: Go `go test ./...` green (new `internal/comic` unit tests for natural sort, entry filter, CBZ+CBT extraction; the corpus sweep gains CBR/CB7 skips when 7-Zip is absent), `go vet` clean; extension `npm test` 84/84 incl. 9 new comic tests (CBZ stored+deflate, CBT, CBR/CB7 rejection, filter, ordering). Browser-side rendering + real-comic OCR quality still want the manual smoke test before publication. |
| 2026-07-17 23:20:00 | internal/ocr/overlay.go, internal/ocr/overlay_test.go, extension/src/ocr-overlay.js, extension/src/ocr-overlay.css, docs/PARITY.md, DEV/plan/2026-07-17_ocr-plate-fit.md | OCR: fit plate text to its box at runtime, and again after the translator swaps it | Roadmap P10. A third of OCR plates clipped their text and an eighth sat half-empty, because the plate box and its font were computed from the **source** geometry while the text poured in - reflowed to the box width, then swapped by the browser translator for a string of another length - never matched. No compile-time font size can win that: the final length is unknown when the size is chosen. The durable fix is to stop guessing and fit **after** layout. New shared `ocrScript` (Go, injected once per overlaid page via `ensureScript`) / `fitPlate` + `scheduleFit` (extension): each plate's `cqw` font is shrunk (down to `0.5 x`) until the text fits its source-region box, and if it still overflows at that floor the box is allowed to grow (`height:auto`) so **nothing is ever clipped**; a `MutationObserver` re-fits when the page translator swaps a plate's text, and a resize handler re-fits on reflow. CSS moves from top-aligned (`align-items:flex-start`) to **centred** so short text no longer floats at the top of a tall region (shape 1), and the extension plate gains `overflow:hidden` so the fit has a bounded box to measure. **Verified in real headless Chrome** on `comic-scan-tiny` (33 plates, single-page): before-fit `clipped=1`, after-fit `clipped=0 shrunk=4 grew=1`; and the case that started the ticket - doubling every plate's text to simulate a longer translation - the observer re-fit holds `clipped=0`. Degrades safely to the CSS `overflow:hidden` if the script never runs. New `TestEnsureOverlayAssets` guards the wiring (CSS centres, script injected and not entity-escaped, grow-fallback present); shape 2 (the plate that missed its bubble) is fed by recognition, addressed by P9 above, not by fit. `docs/PARITY.md` plate-geometry row updated + new "Plate runtime re-fit" shared-behaviour row. `scripts/test.ps1` green (Go incl. 226s integration), lint green; extension `npm test` 75/75. |
| 2026-07-17 23:05:00 | internal/ocr/tesseract.go, internal/ocr/upscale_test.go, extension/src/ocr-overlay.js, tests/parity_test.go, docs/PARITY.md, DEV/plan/2026-07-17_ocr-upscale-threshold-misses-page-scans.md | OCR: gate the pre-recognition upscale on estimated DPI, declare the resolution, ASCII-stage the path | Roadmap P9. The pre-OCR upscale that turns unreadable scans legible was gated on the long side being `< 1000 px` - but a page scan clears 1000 px even at ~100 DPI, so it fired for thumbnails and **never for the page scans it exists for**, which OCR'd into word salad a translator then rendered as confident nonsense. Raw pixel count is the wrong signal (it cannot tell a clean 1600 px render from a grubby 1674 px newsprint scan of the same size); the fix gates on **estimated DPI** instead - long side over an assumed `OCR_ASSUMED_PAGE_INCHES (11)`-tall page - and does two things, both **measured on the corpus with the real tesseract**, not reasoned. **(1) Always declare the resolution** (`user_defined_dpi`, clamped `>= 70`): tesseract's own estimate on a bare scan runs ~70 DPI for a ~180 DPI page, and that low estimate is what makes its layout analysis merge adjacent speech balloons into one plate. Declaring it separates them for free - `comic-scan-mid_Plastic-Man-v1-017` p.14: conf 68.6 -> 77.2, blocks 12 -> 38 (~12 real balloons), words 121 -> 176, zero pixel cost. **(2) Upscale `2 x` only below `OCR_UPSCALE_DPI_FLOOR (120)`** (declaring the doubled DPI): the genuinely low-res newsprint (`comic-scan-tiny`, ~90 estDPI) gains hugely - a formerly-salad page now reads *"He spread the rocket fuel over a great area of reddish sand.. his flight through the track-less void, and then set it aflame by using a long fuse"* (conf 76.8 -> 91.7; one page went 2 -> 46 words) - while a ~150-DPI scan is left un-upscaled because 2x over-segments it (blocks 38 -> 54, lower conf) for no gain and 4x the OCR time. So the cost lands only where it pays: the tiny scan upscales (~1.4s -> 4.5s on 6 pages), Plastic-Man does not (37 images, 8.7/s, unchanged). **Also fixes the masked non-ASCII path bug** the old gate hid: `prepareForOCR` now stages any non-ASCII image path to an ASCII temp copy before handing it to tesseract (tesseract/leptonica mangle non-ANSI-codepage bytes on Windows and fail silently) - the same `internal/pdf` does for pdftotext - so `комикс-скан_..pdf` goes **2/6 -> 5/6 overlaid**, matching its ASCII-named twin. Shared with the extension (`estimateDpi` + `user_defined_dpi` via `setParameters`); the `OCR_UPSCALE_BELOW` constant is replaced by `OCR_UPSCALE_DPI_FLOOR` / `OCR_ASSUMED_PAGE_INCHES` / `OCR_MIN_DECLARED_DPI` across `TestParityOCRClustering` + `docs/PARITY.md`. New Go tests for the DPI estimate, the gate decision, and ASCII staging. |
| 2026-07-17 22:20:00 | internal/pdf/extract.go, internal/pdf/extract_test.go, extension/src/pdf-images.js, docs/PARITY.md, DEV/plan/done/2026-07-17_pdf-raster-extraction-takes-the-wrong-images.md | PDF: drop the page thumbnail and collapse proportional-scale duplicate rasters so the right image shows | Roadmap P8. A text-less PDF is shown as *the embedded rasters of each page* - fine for a plain scan, wrong for anything else, and silent. Three measured shapes, three corpus fixtures. **(1) A page thumbnail taken for the page.** pdfcpu returns a page's `/Thumb` preview among its images, so `pdf-1page-tiny_NASA-Quaoar-sky-chart.pdf` (a vector chart, no text layer) came out as a **128x104 postage stamp** where the chart should be. `selectPageImages` now drops `img.Thumb`; with the thumbnail gone the page yields nothing, so the existing no-text fallback fires and the reader gets the **real vector chart** in an `<embed>` of the original PDF instead of a stamp (Chrome-checked: `<embed application/pdf>` present, `original.pdf` on disk). **(2) One page emitted twice.** `pdf-1page-blackletter_Plague-Proclamation-1625.pdf` embeds the same scan at two resolutions (`_I1` 1455x2065 and `_Im001` 4363x6193, exactly 3x) and showed both - the reader scrolled the proclamation twice, logged as `Images: 2 ... 1 pages` like success. `selectPageImages` collapses **proportional-scale duplicates** (aspect ratios equal within `aspectRatioTolerance` 1% = the same picture uniformly scaled) down to the largest; now `Images: 1`, and a NOTE says one duplicate was skipped so the drop is not silent. Chrome-checked: `total=1 render=1 broken=0`. **(3) A vector page with nothing to read** is the general case behind (1) - handled by the same thumbnail drop feeding the fallback embed. **The dimensions were the trap:** pdfcpu's real (non-stub) `ExtractPageImages` leaves `Width/Height` at **zero** (only the stub pass fills them from the image dict), so the first cut of the dedup never fired - a cheap second stub pass now enriches each image's dimensions before selection, decoding no pixels. Composed pages (differently-shaped images) are left fully intact - guessing "the page" among genuinely distinct images would be wrong as often as right - verified by controls: Lincoln single scan 1/1, comic-scan 6 pages 6 images (no cross-page collapse), a 319-page book with 0 multi-`<img>` pages. **Extension checked against the same three fixtures:** it mines page paint operators, so a `/Thumb` (a page-dict entry, never painted) can't reach it, and a vector page is handled *better* - `rasterizePage` renders the whole page; but two same-scan XObjects with different names *would* both show, so the same `dedupeSameShape`/`sameShapeRaster`/`ASPECT_RATIO_TOLERANCE` is ported to `pdf-images.js` (`extractPageImages` returns the deduped set). `docs/PARITY.md`: new "PDF page-image selection" shared invariant (the 1% tolerance, both sides) + the thumbnail (Go drops `/Thumb`, extension never sees it) and vector-page (Go embeds the original, extension rasterizes) asymmetries recorded as intentional-by-capability. `scripts/test.ps1` green (all packages incl. `internal/pdf` + the 177s integration suite), lint green; extension `npm test` 50/50. |
| 2026-07-17 21:30:00 | internal/fb2/extract.go, internal/fb2/extract_test.go, internal/htmlconv/extract.go, internal/htmlconv/extract_test.go, docs/PARITY.md | FB2 and HTML: carry images into the output instead of dropping them silently | Roadmap P6+P7 - two readers, same symptom (every image lost, silently, reported as success). **FB2** (`internal/fb2`): an illustrated FB2 converted to a text-only book. The extractor unmarshalled only `<p>` into a struct, so both the base64 `<binary>` payloads and the `<image l:href="#id">` refs were thrown away. Rewritten to a single **token walk** (`collectContent`) that keeps body content in document order and decodes every `<binary>`; referenced binaries become deduped sibling files (`imageFileName`, written once), the `<coverpage>` image opens the book, and a ref with no binary shows a visible `[image not found: id]` placeholder + a WARNING rather than vanishing. The catch the ticket did not predict: FB2 wraps most illustrations *inside* a paragraph (`<p><image/></p>`), so the first pass found only the cover (1 image) - `readParagraph` now pulls inline images out of each `<p>`, taking Alice from 1 to **55 images**. Verified by headless Chrome: `total=55 render=55 broken=0`; `fb2-russian_Moskoviya.fb2` gives 47 refs -> 41 files (6 refs reuse an image, deduped), 0 broken. **HTML** (`internal/htmlconv`): a "Save page as" HTML (the file plus an `images/` folder) came out with all 28 pictures broken - the package had no image code at all beyond a CSS line. New `copyLocalImages` walks the parsed body, copies each local image from the source's directory **subtree** into the output and rewrites the `src`; it **refuses `../` traversal** (a test proves `../secret.png` is not copied out), leaves remote (`http(s):`, `//`), inline (`data:`) and absolute paths untouched, dedupes a file referenced twice, suffixes colliding basenames, and leaves a dangling ref as a visibly broken image rather than dropping it. Verified by Chrome: Alice **0/28 -> 28/28 render**; the dangling `Pride-and-Prejudice` fixture still converts and shows its 164 missing images as honest broken icons. **Extension needed no change for either** (checked, not assumed): it inlines FB2 `<binary>` as `data:` URLs already, and cannot reach an HTML file's sibling images from the picker (browser sandbox grants no directory access) - both recorded as intentional divergences in `docs/PARITY.md` (the file-vs-inline split, same pattern as EPUB). Tests: `internal/fb2` (cover / inline-in-`<p>` / block-level / dangling / dedup) and `internal/htmlconv` (copy / traversal-refused / remote+data left alone / dangling) + integration (215s) + lint green; extension `npm test` 45/45. |
| 2026-07-17 20:10:00 | internal/img/extract.go, internal/img/extract_test.go, docs/PARITY.md | Image input: transcode TIFF to PNG so the browser can show it (multi-page -> one page per frame) | Roadmap P5. **Chrome cannot decode TIFF** - the whole app is "convert, open index.html in Chrome" - so a `.tif` input, copied through untouched, rendered as a broken-image icon with OCR plates floating over white space, reported as success (exit 0, 8 plates, `Done.`). A multi-page TIFF was worse: tesseract OCR'd every frame and the overlay stacked all frames' plates onto the single `<img>` (measured before: 3-frame -> 33 plates, `OCR overlay: 1 image(s) overlaid`). Owner decision: **transcode, don't refuse** - the Go app has `golang.org/x/image/tiff`, so it can honestly support the format. `internal/img.extractTIFF` decodes every frame and writes each as a PNG, giving each its own page. Since `x/image/tiff.Decode` only reads the first IFD, multi-frame is reached via the fact that **TIFF strip offsets are absolute from the file start**: `tiffFrameOffsets` walks the IFD chain (header -> first IFD, each IFD ends with the next's offset) collecting every frame's offset, and `decodeTIFFFrame` copies the file with the header's first-IFD offset repointed at frame N, so `tiff.Decode` returns frame N with its strip offsets still valid - reusing the standard decoder for every frame instead of reimplementing TIFF. **Verified by Chrome itself** (the done criterion, not a plate count): headless Chrome + `naturalWidth` - the raw `.tif` returns `onerror` (bug reproduced), each transcoded PNG returns `W=800`; the 3-frame corpus fixture produces 3 PNG pages, all three rendering, and OCR now reports `3 image(s) overlaid` with plates in 3 separate `ocr-fig` containers - the pile-up is gone, plates from frame N land on frame N. Both halves of the ticket closed by the one change, as predicted. Multi-frame unit test uses the real 3-frame fixture (a hand-built multi-frame TIFF is fragile - x/image/tiff's decoder is strict about required tags) and skips if the generated fixtures are absent. `docs/PARITY.md` records the intentional divergence: **Go transcodes TIFF, the extension refuses it** (a browser tab has no TIFF decoder; the Go app does). Non-TIFF images still copy through untouched. `go test` + lint green (bar the pre-existing env-dependent `TestExtract_NoCalibre`). Untouched: `.bmp` still renders but the overlay can't colour-sample it (no Go BMP decoder), a separate defect noted for a future ticket. |
| 2026-07-17 17:35:00 | internal/txt/sniff.go, internal/txt/sniff_test.go, internal/pipeline/pipeline.go, docs/PARITY.md | Pipeline: refuse a binary instead of converting it into a garbage document | Roadmap P4. Dragging a `.docx`, `.djvu`, or a comic archive onto the app produced a multi-megabyte "document" of raw bytes rendered as prose, reported as `Done.` with exit 0 - measured across eight extensions (`.docx` `.odt` `.pptx` `.xlsx` `.djvu` `.cbz` `.cbr` `.cb7` `.cbt`), the `.cbr` a 17 MB `index.html` of 14.3M characters at 18% control bytes shown as a 363-page book. The pipeline dispatches known extensions by name, and everything else fell through the `default:` arm straight into the text extractor. New `internal/txt.LooksBinary(head)` guards that arm: it checks a **BOM first** (UTF-16 text is full of NUL bytes, so without this a Notepad "Unicode" save would be refused), then **named signatures** - ZIP (`PK..`; DOCX/XLSX/PPTX/ODT/CBZ), RAR (`Rar!`; CBR), 7z (CB7), tar (`ustar` at **offset 257**, since a tar has no magic at offset 0), DjVu (`AT&TFORM`), plus PDF/MOBI/image defensively - then a **NUL byte in the first 4 KB** as the catch-all for unsigned binary. A match returns a non-zero exit, names what the file looks like (`X looks like a ZIP archive (DOCX, XLSX, PPTX, ODT and CBZ are ZIP-based), not a text document - refusing to convert it into garbage`), and leaves no output directory. **Verified by running all 12 corpus binaries** (not by reasoning about the sniff): every one exits 3, names the format, and leaves no book dir; all five text fixtures (plain, real cp1251, UTF-16LE/BE, UTF-8-BOM) still produce `index.html` at exit 0. The first verify harness lied the usual way - `-folder` is a *parent*, the book lands in a `<folder>/<name>` subdir, so it checked the wrong path and flagged everything; corrected to check the real output dir. BMP naming is tightened to require the zero reserved bytes so a text file merely starting "BM" is not mistaken for an image. Extension confirmed already-correct **by tracing** `detectFormat` -> EPUB reader (CBZ) / `loadPdfData`+`handleLoadError` (DjVu/RAR/7z/tar), both clear notices. `docs/PARITY.md` records signature-first detection as a shared invariant. Also removes the old dispatch line's long-dash + `...` (a P13 typography item, free here). `go test` + lint green (bar the pre-existing env-dependent `TestExtract_NoCalibre`). Left open deliberately: the guard is on the `default:` arm only, so a binary renamed `.txt` is still unguarded - a wider change, noted in the ticket. |
| 2026-07-17 17:10:00 | internal/txt/legacy.go, internal/txt/extract.go, internal/txt/extract_test.go, extension/src/txt.js, extension/test/txt.test.mjs, docs/PARITY.md | TXT: detect and decode legacy Cyrillic code pages (cp1251/koi8-r/cp866/iso-8859-5) | Roadmap P3, filling the `decodeLegacy` seam P2 left. A `.txt` in a pre-Unicode Cyrillic code page - a DOS-era archive, a pre-UTF-8 RU/UA web page - was decoded as UTF-8 and came out as mojibake. New `detectLegacy` (both editions) decodes the bytes with each candidate and picks by **two metrics, and it needs both** (measured, not reasoned): a **frequency-weighted fit** chooses the code page - cp1251 and koi8-r remap the *same* byte range so both yield ~the same *number* of Cyrillic letters (fraction 0.761 vs 0.760 on the corpus fixture), and only weighting each letter by its natural Russian frequency separates them (16718 vs 10612) - and a **Cyrillic-fraction floor of 0.30** is the confidence gate: French Latin-1 tops the *weight* score as koi8-r but sits at fraction 0.17, so the floor rejects it and the bytes pass through as UTF-8 unchanged. The floor was read off printed scores (0.76 real vs 0.17 French), not guessed. `test_doc/Лицензионное Соглашение.txt` - real cp1251, 3873 B, 76% high-bit - now converts to readable Russian and logs `Decoded from windows-1251`, verified by reading the output. Tests generate the bytes by encoding a known string through each of the four candidates (`TestExtractDecodesLegacyCyrillic`) and assert non-Russian legacy bytes are left alone (`TestExtractLeavesNonCyrillicLegacyAlone`); `txt.test.mjs` mirrors both. **Accepted limit, surfaced by a test that first failed honestly:** a very short, accent-dense non-Russian string can exceed 0.30 and be mis-Cyrillized (my first French fixture was 15 bytes at fraction 0.40) - that is the "short files carry too little signal" hazard the ticket named; the floor is the agreed trade and the test now uses a realistic-length sentence. `RU_LETTER_FREQ`, the candidate list, and the floor are duplicated verbatim across editions and recorded as a `docs/PARITY.md` invariant. `go test` + lint green (bar pre-existing `TestExtract_NoCalibre`); extension `npm test` 45/45. |
| 2026-07-17 16:40:00 | internal/txt/extract.go, internal/txt/extract_test.go, extension/src/txt.js, extension/test/txt.test.mjs, docs/PARITY.md | TXT: decode UTF-16 and BOMs instead of reading bytes as characters | Roadmap P2. A `.txt` saved from Notepad as "Unicode" (UTF-16) was read one byte per character, so a Cyrillic file turned to mojibake ("Это обычное" -> "-B> >1KG=>5"). ASCII hid it: ASCII-in-UTF-16 is "character, NUL, character, NUL", and the single-page merge strips control chars on the way past - so the *same file* read correctly, as spaced-out letters, or as mojibake depending on the alphabet and on `-multipage`. New `decodeText` (both editions) decides encoding from the leading bytes: **BOM first** (UTF-8 trimmed, UTF-16 LE/BE via `golang.org/x/text/encoding/unicode`), then `utf8.Valid` taken as-is, then a `decodeLegacy` seam that stays a passthrough until P3 fills it. Fix goes in the decoder, not the merge - the NUL-stripping was a coincidence, not a decode. **Measured on both editions, not assumed:** the extension's `TextDecoder("utf-8")` produced `U+FFFD U+FFFD` + 75 NULs on the same UTF-16 bytes - identically broken - and is fixed with an explicit `utf-16le`/`utf-16be` label; but the **UTF-8-BOM leak was Go-only**, because `TextDecoder` strips a BOM unless `ignoreBOM` is set (the Go side leaked it into the first paragraph as an invisible character). Verified by reading the output on the four real `_generated` fixtures in **both** modes - all eight correct, the mode no longer changes the text - after two harness misreads were caught (a swallowed CLI log reporting eight false BAD results, and `-folder` being the *parent* dir so the output was one level deeper than the script looked). Regression: `Лицензионное Соглашение.txt` produces a byte-identical `index.html` before and after (same SHA-256). PARITY.md records the decode order as a shared invariant, including the BOM-stripping difference as intentional-not-drift. **Found while verifying:** that same licence file is real Windows-1251 (3873 B, 76% high-bit, mojibake today) - so P3's "corpus has no non-UTF-8 fixture" was wrong; corrected there with the candidate-decode evidence (koi8-r and iso-8859-5 both yield fluent-looking-but-wrong Cyrillic, so P3's heuristic must score letters, not count them). `go test` + lint green (bar the pre-existing `TestExtract_NoCalibre`); extension `npm test` 43/43. |
| 2026-07-17 16:05:00 | cmd/doc-html-translate/main.go, internal/config/flags.go, internal/config/flags_test.go, internal/logging/log.go, internal/app/app.go, internal/pipeline/pipeline.go, internal/pdf/extract.go, internal/pdf/extract_test.go, internal/ocr/overlay.go, internal/ocr/overlay_test.go, DEV/plan/2026-07-17_cli-diagnostics-honesty.md | CLI: the log stops lying, and the error paths stop hanging | Roadmap P1, the instrument every later fix is verified with. Three items from the 2026-07-17 corpus sweep. **1. Every error path hung when piped.** `waitOnError` printed "Press Enter to close" and blocked on `Scanln` regardless of whether a console was there to close - so `doc-html-translate -h \| Select-Object -First 60` never returned. New exported `logging.StdoutIsTerminal` (the check `logging.Progress` already had, unexported) gates the pause. The sweep found one site; there were **four**, and the worst was not the one reported: `pipeline.go` pauses the same way after a *translation failure*, so a scripted conversion whose translation broke hung forever - invisible to the sweep because it ran `-notranslate` throughout. The `internal/app` splash pause is gated too. `promptSetDefault` is deliberately **not**: a pause serves a console (gate on stdout), a question needs an answer (depends on stdin), and it already takes the safe "no" on EOF - gating it on stdout would have made `echo y \| app > out.txt` silently stop registering. Measured with a harness that gives the child a stdout pipe and an open, never-written stdin (a background job cannot show the bug at all - it hands the child a closed stdin, so `Scanln` returns EOF and even the buggy build looks fine): pre-fix `-h`, `-bogus` and "no input file" all hang; post-fix all three return. **`-h` also exited 1 with the help on stderr under an `Error: flag: help requested` banner** (owner-approved as a semantic change, since `-version` in the same binary already exits 0 via a sentinel and `-h` was the outlier). flag writes usage and parse errors to one writer, so `ParseArgs` now buffers it and routes once Parse says which happened - rather than scanning args for "-h" and second-guessing the parser. `-h` -> stdout, exit 0; a bad flag -> stderr, exit 1, unchanged. **2. "Pages: N (with text: N)" counted pages with no text.** The counter reported `generated` - pages that produced *any* output, images included - under a "with text" label, so a scan with no text layer announced `Pages: 6 (with text: 6)` directly beneath `pdftotext extracted no content`. Both emit loops (pdftotext and the pdflib fallback) now count each kind separately; the pdflib one already had the comment `// Image-only page - include it without text` two lines above the counter it fed. New `pageCountSummary` (pure, tested) prints `Pages: 6 (with text: 0, image-only: 6)` - which does not just stop lying, it now diagnoses the file as a scan at a glance - and names empty pages instead of leaving them as an unexplained gap below the total. **3. An image that failed to overlay vanished.** `recognizeJobs` collapsed four outcomes into one `ok=false` and **discarded the error entirely**, so "this art panel has no dialogue" (normal - 593 of Aphrodite's 2304 pages) and "recognition failed" (the whole bug on `Kupní smlouva ..pdf`) were indistinguishable, and establishing which cost an investigation per book. New pure `classifyRecognition` splits them: an error, or no page geometry, is a failure carrying its reason; no blocks is an ordinary empty image. `OverlayFile` returns `OverlayResult{Overlaid, NoText, Failed}` instead of a bare count. The fixture that prompted the ticket now reads `5 image(s) overlaid, 1 with no text found` - the mystery gap was benign all along - and a genuinely unreadable image reports `0 overlaid, 1 failed` + `WARNING: OCR failed on fake.png: tesseract: exit status 1: Leptonica Error ..`. Reasons go to stdout beside the count they explain (matching this pipeline's other best-effort warnings) rather than stderr, where a redirected log would lose them; capped at 5 named with the remainder counted, since a systemic cause fails every image with the same sentence. Gate: `go test ./...` green except the pre-existing environment-dependent `TestExtract_NoCalibre` (asserts Calibre is absent; it is installed here - verified failing on a clean tree), lint green. |
| 2026-07-17 03:15:00 | internal/pdf/extract.go, internal/pdf/extract_test.go, internal/htmlgen/navbar.go, extension/src/viewer.js, extension/src/viewer.css | A scanned page fills the window; reader text to ~28px | **Scanned pages were rendered at the text measure.** A page that is nothing but a scan was laid out inside the reading column (`main.dht-single` is 46em), so a 1600x1200 comic page came out around 960px - roughly half the window - and had to be read at browser zoom. New `pdf.pageScanBox` gives such a page a box sized `min(<image width>px, 96vw, <92vh scaled by the image's aspect>)`: capped at the image's own pixel width so it is never upscaled into mush, at the window's width, and at the window's height via its aspect ratio - so one scanned page lands on about one screen and scrolling moves page by page. Measured on the 2304-page book: the landscape pages get `min(1600px,96vw,122.7vh)` and the portrait cover `min(1086px,96vw,69.0vh)`, which on a 1920x950 window is 1166x875 instead of 960x720. Only a page with exactly one image and no text is treated this way - a composed page is left to the column rules, since guessing which of several images is "the page" would be wrong as often as right. Crucially it sizes the *box*, never the image: the OCR overlay positions its plates as percentages of a container carrying the image's aspect ratio, so bounding the image's height directly would clamp that container and drift every plate off the text it covers. `readerCSS` only centres the box out of the narrow column (`margin-left:50%` + `translateX(-50%)`) rather than widening the column, which would wreck the measure of a page that does have text. **Reader text 130% -> 175% (~28px), extension 25px -> 28px**, on report that 25 was still too small; `MAXSZ` 180 -> 300, since a 175 default left A+ nowhere to go. Note for the next person: `readerScript` is a `fmt.Sprintf` format string - a literal `%` in a *comment* inside it is enough to break the build ("unknown verb"). |
| 2026-07-17 02:35:00 | internal/logging/log.go, internal/ocr/overlay.go, internal/htmlgen/favicon.go, internal/htmlgen/navbar.go, internal/htmlgen/htmlgen.go, internal/htmlgen/singlepage.go, scripts/build.ps1, extension/src/viewer.js, extension/src/viewer.css | Wider OCR pool, calmer progress, bigger reader text, tab icon on results | Four things the 2304-page scan turned up in use. **OCR pool 8 -> 16.** Eight processes left a 20-core machine at 85%; the cap is now `min(NumCPU-2, 16)`. NumCPU-2 still leaves the desktop and the GUI streaming the log a core to breathe. **Progress every 4s, not every 1s.** `logging.progressInterval` - a rate and an ETA that redraw every second are noise (the numbers barely move) and they bury the lines around them; `Ticker.Quiet` uses the same constant, so a run too short to have looked stuck still prints nothing at all. **Reader text +30%.** The generated reader defaulted to `--dht-reader-size:100%` and the extension viewer to 19px; now 130% and 25px. The Go side also had a trap: `applySize` persisted on every load, so the first book ever opened stamped 100% into localStorage and no later change to the default could ever reach that reader - it now persists only what A+/A- actually asked for. The CSS `:root` value is bumped alongside `DEFSZ` because it styles the page before the script runs, and a mismatch would show as the text resizing on every load. **Tab icon.** A converted book got the browser's blank-page glyph, and readers keep several of these tabs open. `htmlgen.WriteFavicon` drops the app icon beside index.html and every generated page links it - a sibling file rather than a data: URI in each page, since the icon is 21 KB and a multi-page book would otherwise carry a copy in every file (verified: 10 pages, 1 icon on disk, 10 links). Covers all three generation paths: navbar injection (relative to each page's depth), the merged single page (relative to the book's base dir), the TOC index, and - via `injectFavicon` - the one-page path that skips navbar injection entirely. `scripts/build.ps1` keeps `internal/htmlgen/favicon.ico` in step with the generated app icon, mirroring what build-ui.ps1 does for the GUI. |
| 2026-07-17 02:05:00 | extension/src/viewer.js, extension/src/viewer.css, docs/PARITY.md, cmd/doc-html-ui/main.go, cmd/doc-html-ui/main_test.go, cmd/doc-html-ui/ui.html | Extension: extract page images lazily; GUI: find the result again | **Extraction now rides the same trigger as OCR.** The viewer pulled the raster out of every page inline in the render loop - pdf.js decodes the image, the viewer draws it to a canvas and re-encodes it, and a page with no image of its own got rasterized whole - while the *recognition* of those same images was already deferred to scroll. That split was incoherent and it was the real cost: page text fetches at ~2100 pages/s, so on a scanned book the minutes were all image work, for pages the reader might never reach. `deferPageImages` now hands each section to an IntersectionObserver (1500px margin) and `extractSectionImages` re-opens the page and runs `appendPdfImages` only when the reader nears it. This costs nothing in translation coverage: the plate that carries the readable text always arrived on scroll anyway. Two things that would have made it a regression: (1) a scanned page would have collapsed to a label and snapped back a page-height when its raster landed, so a pending page with no text reserves a box of the page's own aspect ratio (`.pdf-page-pending`, and only for a text-less page - exactly the case `appendPdfImages` rasterizes whole, so the reserved shape is known to be right), and `<img>` now carries its intrinsic width/height so the browser holds the space while the blob decodes instead of laying out at zero height; (2) the "little or no text" banner keyed on `ocrTotal > 0`, which is now 0 at banner time, so it would have contradicted the OCR it was about to do - it now asks whether pages are queued for extraction. PARITY.md: the OCR-execution divergence rewritten - Go OCRs eagerly across a process pool because nothing is readable until the file is written; the extension extracts *and* recognizes lazily on one worker because a reader only needs the page in front of them and the tab is shared with the reading itself. **GUI: "Open result".** A dropped file is copied into the app's own data folder and the result lands beside it, so once the browser tab was gone the conversion was effectively lost ("it unpacked somewhere into a temp folder"). New `/api/open-output` opens the result page, or - with `folder` - the directory holding it; the hint now shows that directory and makes it clickable. It carries the same guard as `/api/delete-output` (only ever acts on a directory that actually contains index.html, so a stray Output value can't steer it into shell-executing an arbitrary path), covered by two new tests via an injected `openTarget` so the suite never spawns a file manager. `runConvert` re-checks output status when it finishes, which is what puts the button on screen the moment the result exists. Strings in en/ru/uk. |
| 2026-07-17 01:45:00 | extension/src/viewer.js | Extension: 100-page chunks + page prefetch | Corrects the assumption the chunking was built on. Chrome's translator does **not** pick up pages appended after it ran: whatever was not in the DOM when the reader pressed "Translate page" stays in the source language, and they have to toggle back to the original and translate again. So every chunk boundary costs the reader an interruption, and the design goal is the opposite of what the code's comments claimed - **fewer and bigger** chunks, not smoother small ones. `PAGE_CHUNK` 50 -> 100, `CHUNK_LEAD` 2 -> 5 (a bigger chunk takes longer to build, so start it further from the edge). Page fetching now keeps `PAGE_LOOKAHEAD` (8) requests outstanding to the pdfjs worker, consumed in page order, instead of awaiting `getPage` + `getTextContent` one page at a time - which left the main thread idle during each worker round trip and the worker idle while the main thread built DOM. Measured in Node (a lower bound: Node has no real worker thread, so the overlap is smaller than in a browser) over a 609-page text PDF: 158ms -> 108ms for 100 pages, **1.46x**, byte-identical output (5117 text items both ways). The stale comments claiming a one-shot insert lets "the translator absorb" a chunk are corrected: the one-shot insert is worth keeping, but for layout cost, not translation coverage. **Measured, not fixed:** on the 2304-page scan that prompted all this, page fetching was never the bottleneck - it runs at ~2100 pages/s and the file has **no text layer at all** (0 text items; pdftotext agrees, and pdfcpu finds exactly one image per page). Its slow render is `appendPdfImages`, which decodes each page's raster (or rasterizes the page) inline in the render loop when "Use OCR for images" is on. Image *extraction* is eager while *recognition* is lazy - a mismatch worth its own ticket. |
| 2026-07-17 01:36:00 | internal/ocr/overlay.go, cmd/doc-html-ui/ui.html | OCR runs across cores; GUI layout pinned to the window | **Parallel OCR.** One Tesseract process pins about one core, so OCR-ing a scanned book used ~5% of a 20-core machine while the reader waited: 2304 images at 4.1/s is roughly 9 minutes of a nearly idle CPU. `OverlayFile` now recognizes across a pool of `ocrWorkers()` processes (NumCPU-2, capped at 8 - leaving cores for the desktop and the GUI streaming the log). Only recognition is parallel: results are applied afterwards in document order, because the HTML tree is not safe for concurrent mutation, and because `decodeImage` stays there, one image at a time, rather than holding thousands of decoded pages in memory. `Recognize` was already safe to run concurrently (its own `os.CreateTemp` file, its own process); each worker writes only its own job slot, the counter and progress callback are mutex-guarded, and `wg.Wait()` orders the writes before the apply loop. Images with no local file are filtered out up front so the progress total is real work, not a padded count. Measured on a 9-image scan: 8.7s (1.0/s) -> **1.9s (4.6/s)**, same 8 overlays. Confirmed 8 concurrent tesseract processes at 85% CPU on the 2304-page book (was a couple of percent). Note: the race detector could not be run here (no gcc for cgo, and the default toolchain is 386). **GUI layout.** The log pane had `flex: 1 1 auto` with `overflow: auto` but no `min-height: 0` - the flexbox trap where an item refuses to shrink below its content, so instead of scrolling it grew, and with thousands of OCR progress lines it dragged the whole page taller until the controls scrolled out of sight. Nothing capped the height either. The window is now the frame: `body` is `100vh`, `overflow: hidden`, and the grid + its children carry `min-height: 0`, so the log scrolls inside its box and the page never does. The body's `max-width: 1180px; margin: 0 auto` centring is gone, so content is pinned to the top-left and the log stretches into all the landscape space. The narrow-window stacked layout (<=900px) hands scrolling back to the page and keeps a 260px log, since one viewport cannot hold both columns. |
| 2026-07-17 01:20:00 | internal/logging/log.go, internal/ocr/overlay.go, internal/pipeline/pipeline.go, internal/pdf/extract.go | App: progress for the OCR overlay (the last silent loop) | With PDF extraction fixed, a `-ocr` run on a scanned book moved the apparent hang one stage down: `[2/4]` printed `OCR overlay: engine ...` and then went quiet for the entire recognition pass. It was never hung - Tesseract needs roughly a second per image, so 2304 scanned pages is about 40 minutes of honest work - but nothing said so. Progress is now reported **per image, not per file**: `overlayImages` loops content files, and in single-page mode the whole book *is* one file, so a per-file counter would have read `0/1` for the entire run. `ocr.OverlayFile` takes an `onProgress(done, total)` callback - the shape `translator.ProgressReporter` established - so `internal/ocr` still knows nothing about logging. The ~1 line/s throttle that the PDF loops grew is now the shared `logging.Ticker` (label + unit, rate + ETA, closing summary, `Quiet()` to skip the summary on a trivial run), replacing `pdf.reportPageProgress` so the three loops cannot drift. Verified on a 9-image scan: `OCR overlay: 4/9 images 0.9/s ETA 6s` -> `9 images in 8.7s (1.0/s)`, 8 overlaid. **Found while verifying, not fixed here:** OCR silently does nothing when the image path contains non-ASCII characters - the same 9-image PDF overlays 0 images from `Kupní smlouva ....pdf` and 8 from an ASCII-named copy, because `tesseract.exe` receives the path mangled through the ANSI codepage ("Kupní" -> "Kupni") and reports `cannot read input file`, which `OverlayFile` treats as a skippable per-image failure. `internal/pdf` already solves exactly this for pdftotext (`needsPDFToTextPathStaging` / `stagePDFForPDFToText`); `internal/ocr` needs the same staging. Extension unaffected by construction: tesseract.js takes a pixel Blob, never a filesystem path. |
| 2026-07-17 01:05:00 | extension/package.json, extension/build.mjs, extension/src/viewer.js, extension/test/pdf-images.test.mjs, go.mod, go.sum, internal/pdf/extract.go | Deps: major bumps across both editions (pdfjs 6, tesseract.js 7, marked 18, pdfcpu 0.13) | Pre-release dependency sweep. **Go:** pdfcpu 0.11.1 -> 0.13.0 (which moved `WriteImageToDisk` from `pkg/pdfcpu` to `pkg/api`; `ExtractPageImages` / `ReadValidateAndOptimize` stayed), goldmark 1.7.16 -> 1.8.4, plus x/crypto, x/image, x/net, x/sys, x/text. Re-verified end to end on the 379 MB / 2304-page PDF: image extraction got *faster* (6.2s -> 4.4s), all 2304 images still extracted and mapped. **Extension:** pdfjs-dist 4.10.38 -> 6.1.200, tesseract.js 5.1.1 -> 7.0.0, tesseract.js-core 5.1.1 -> 6.1.2, marked 12.0.2 -> 18.0.6. Two traps the bump laid: (1) from pdfjs 5 the **JBIG2 and JPEG2000 decoders and the QCMS colour engine moved into WASM modules** fetched at runtime from `wasmUrl` (ICC profiles from `iccUrl`) rather than being compiled into the worker - and JBIG2/JPX are precisely what scanned PDFs use, so shipping without them would have silently broken the documents this extension exists for; `build.mjs` now vendors `wasm/` + `iccs/` and `viewer.js` passes both URLs to `getDocument` (the manifest CSP already allows `wasm-unsafe-eval` for Tesseract). (2) marked moved `LICENSE.md` -> `LICENSE`, breaking the vendor step; `vendorMarked` now accepts either. Also, pdfjs 6 constructs a `DOMMatrix` at module scope, so importing `pdf.mjs` under `node --test` throws - the browser contexts that load it (viewer page, pdfjs worker) all have `DOMMatrix`, so `pdf-images.test.mjs` stubs the constructor and imports dynamically. Verified statically: every pdfjs entry point the extension uses (`getDocument`, `GlobalWorkerOptions`, `PasswordResponses`, `OPS.{paintImageXObject,paintInlineImageXObject,save,restore,transform}`) still exists, tesseract.js 7 keeps both signatures we call (`createWorker(langs, oem, options)`, `recognize(image, opts, {blocks:true})`) and the `blocks -> paragraphs -> lines -> words` result shape the overlay reads. Tessdata stays pinned at 4.0.0_fast, so the cross-edition OCR parity invariant is untouched (all 7 parity guards green). Store zip 6.1 MB. **Browser-side behaviour is not covered by the unit tests** - the extension needs its manual smoke test (extension/README.md steps 1-6, especially OCR quality under the new engine) before publication. |
| 2026-07-17 00:38:00 | internal/pdf/extract.go, extension/src/viewer.js, docs/PARITY.md | App + Extension: large PDFs stop freezing | A 379 MB, 2304-page scanned PDF took over two hours to convert - the GUI sat with no output for the entire time, indistinguishable from a hang - and minutes to render in the extension viewer, where asking for "Translate page" mid-render collapsed the translation and forced a toggle back to the original and back again. Two independent causes, one per edition. **App:** `extractImages` called `api.ExtractImagesFile` once per page, and that API takes a file *path* - so every call re-opened, re-read, re-validated and re-optimized the whole document just to fetch one page's images. Measured: 3.6s per call x 2304 pages ~ 2h20m, silent throughout. The per-page form was there to cap peak memory, but it never could: each call already loaded the entire document, and images stream to disk one at a time either way. New `writePDFImages` does a single `api.ReadValidateAndOptimize` and walks pages over that one context via `pdfcpulib.ExtractPageImages`, keeping per-page error isolation (an unreadable image skips its page instead of aborting the run) and byte-identical output names (`maxPageDigits` stays 1, so existing output folders keep matching). Measured on the same file: image extraction 2h20m -> **6.2s**, whole conversion **41s**, peak heap 1114 MB - the same memory the old loop paid 2304 times in a row; all 2304 images present and mapped into the HTML. Both formerly silent per-page loops (image extraction, and the pdflib text read, whose library re-walks the page tree from the root on every `Page(i)`) now report throttled progress (~1 line/s, rate + ETA) via the new `reportPageProgress`, in the shape `pipeline.translateContent` already uses - a line per page would shove thousands through the GUI log, which streams and flushes per line. **Extension:** the viewer reflowed every page in one unbounded pass, which both looked hung on a long book and broke native translation - Chrome's translator snapshots the DOM, ships the text off and patches it back, so a render loop still appending pages underneath pulls the rug out from under it. `viewer.js` now renders forward in chunks (`PAGE_CHUNK` 50), building the next chunk once the reader reaches `CHUNK_LEAD` (2) pages from the edge; chunks after the first are built off-DOM and inserted as one mutation a live translation can absorb, while the first still streams page by page (nothing is translated yet, and an OCR-heavy PDF would otherwise show a blank screen for tens of seconds). Page-box and TOC jumps past the edge render the gap on the way there, so sections stay contiguous and in document order. The "HTML" export saves the pages actually reached and says so in the status bar, rather than truncating silently or re-imposing the freeze to finish. PARITY.md: chunking recorded as extension-only (Go writes static HTML in a batch job with no translator racing it), including that the scanned/image-only banner now samples the first chunk in JS but the whole document in Go. |
| 2026-07-15 21:45:00 | installer/doc-html-translate.iss, scripts/build-installer.ps1, a.ps1, scripts/commit-push.ps1, reinstall.ps1, README.md, index.html, DEV/RELEASE.md, scripts/release.ps1, AGENTS.md, DEV/README.md, configs/.typos.toml | New: universal Windows installer (setup.exe) + dev-workflow scripts | A single universal `setup.exe` (Inno Setup) that runs on both 32- and 64-bit Windows and installs the matching architecture per-user (`%LocalAppData%\Programs`, no admin / no UAC). Bundles CLI + GUI + offline English OCR data (tessdata `eng`). Optional setup tasks: add to "Open with" + the non-destructive "Convert to HTML" right-click menu (checked; never steals a file-type default - the app owns association logic via `internal/windowsreg`), a desktop icon (unchecked), and - when Chrome/Edge is detected - install the matching browser extension from its store (opt-in, per-user HKCU pointer). Uninstall sweeps the app's HKCU "Open with"/context-menu entries and releases the default handler (`-unregister`). Built locally via `scripts/build-installer.ps1` (needs ISCC/Inno Setup) into `dist/doc-html-translate-setup-<ver>.exe`; **not built by CI**, so it is attached to the GitHub Release by hand (documented in DEV/RELEASE.md + `release.ps1` step 2). Also new dev tooling: the Android-style `a.ps1` alias launcher (`a inst` builds the installer; `a c`/`a cc` quick commit+push via `scripts/commit-push.ps1`; `a id`/`a ind` local MSIX reinstall via `reinstall.ps1`), with `a.ps1` excluded from the typos gate (short mnemonic aliases, not prose). README + index.html (en/ru/uk) download sections now feature the installer as the easiest no-admin option; winget still installs the portable build. |
| 2026-07-15 11:20:00 | internal/windowsreg/register_windows.go, internal/windowsreg/register_nonwindows.go, internal/config/flags.go, internal/app/app.go, cmd/doc-html-ui/main.go, cmd/doc-html-ui/ui.html, tests/ui_cli_parity_test.go, extension/src/defaults.js, extension/src/background.js, extension/src/popup.html, extension/_locales/{en,ru,uk}/messages.json, docs/PARITY.md, DEV/plan/2026-07-15_optional-file-association-context-menu.md | App + Extension: file-type association optional (off by default) + always-on right-click "convert" | No edition grabs file-type defaults anymore; the default handler is an explicit opt-in, and a right-click "convert" entry is always available for all 10 supported types. **Desktop:** the no-arg first run and every GUI launch now register only the non-destructive "Convert to HTML" shell verb (new `RegisterContextMenu`, under `HKCU\..\SystemFileAssociations\<ext>\shell`) + the existing "Open with", instead of silently becoming the default handler. Becoming the default is opt-in: CLI `-register`, a new GUI **association toggle** (`/api/register` on, new `/api/unregister` off, state read via new `/api/assoc-status` -> `windowsreg.IsDefaultHandler`), and a one-time first-run prompt (CLI `[y/N]`; GUI banner, dismissal persisted in ui-settings). New CLI `-unregister` releases it (`windowsreg.Unregister`, leaves the verb + "Open with"). No-arg CLI stays the "registration flow" invariant, just non-destructive now. Parity guard allow-lists `-unregister`. **Extension:** `enabledByDefault` flips `true -> false` (no DNR interception until the popup toggle is on); new "Convert with doc-html-translate" context-menu item on links to / pages of supported docs (link+page contexts, `targetUrlPatterns`/`documentUrlPatterns`, opens the reflow viewer via `?file=<encoded>`); popup note + i18n `convertDocMenu` (en/ru/uk). **MSIX:** declined - HKCU is virtualized so the runtime verb can't persist; the manifest association is opt-in by OS design (Windows always prompts) and the GUI hides the toggle/prompt when packaged; a native IExplorerCommand handler is deferred. PARITY.md: new "association is opt-in, off by default" invariant + MSIX divergence. |
| 2026-07-14 16:58:00 | internal/img/extract.go, internal/img/extract_test.go, internal/pipeline/pipeline.go, cmd/doc-html-ui/main.go, cmd/doc-html-ui/ui.html, docs/integration-image-translate.md, README.md, index.html, docs.html, docs.ru.html, docs.uk.html, AGENTS.md, docs/PARITY.md | App: standalone image input with OCR overlay | A bare image (PNG/JPG/JPEG/WebP/GIF/BMP/TIFF) is now accepted as input. The new `internal/img` package wraps the picture in a one-page HTML doc and the pipeline forces the OCR overlay on (independent of the `-ocr` flag), so the browser's built-in page translation works on the overlaid text plates - the same result the browser extension produces for images. The GUI shows images in the Browse picker (new "Documents & images" + "Images" filter groups), in the drop-zone "Supported" line, and notes in the OCR hint that a standalone image is recognized automatically (en/ru/uk). New `docs/integration-image-translate.md` documents the CLI contract for embedders (Fast Media Sorter LITE). PARITY.md updated: both editions now accept a standalone image, differing only in engine (extension = tesseract.js WASM; Go = local `tesseract` CLI). |
| 2026-07-12 21:58:00 | extension/src/pdf-images.js, extension/test/pdf-images.test.mjs, docs/PARITY.md | Extension: un-mirror flipped PDF image placements | pdf.js XObject extraction returns the stored raster, bypassing the flip a negative-scale placement matrix applies at draw time, so an image placed mirrored in a PDF was OCR'd (and shown) backwards. `pdf-images.js` now tracks the CTM across `save`/`restore`/`transform` ops, and `paintFlips` derives `flipX`/`flipY` from a negative scale on a non-rotated placement, mirroring the pixels back to their rendered orientation before OCR; rotated placements are left as stored. New test covers `composeTransform`/`paintFlips`. Documented in PARITY.md as a JS-only normalization (Go's pdfcpu extraction has no counterpart yet). |
| 2026-07-09 22:26:00 | internal/pdf/extract.go, internal/pdf/extract_test.go, docs/PARITY.md, DEV/plan/2026-07-01_cross-edition-parity.md | PDF: detect Cyrillic ALL-CAPS headings | `classifyBlock` treated only Latin capitals as a heading trigger (`ContainsAny(text, "ABC..Z")`), so a Russian ALL-CAPS heading like "ГЛАВА ПЕРВАЯ" / "ПРОЛОГ" in a PDF was reflowed as a plain paragraph, not an `h2`. Replaced the Latin-only check with a language-agnostic one: `upper == text && text != strings.ToLower(text)` - the line is all-caps and has at least one cased letter - matching the browser extension's `/[A-ZА-ЯЁ]/u` and still rejecting digit/punctuation-only lines. Closes part of cross-edition parity gap #15 (the remaining JS font-size heading dimension stays a documented JS-only improvement). New `TestClassifyBlock` covers Latin/Cyrillic all-caps, mixed case, digits and centered h2/h3. |
| 2026-07-09 22:25:00 | internal/config/lang.go, internal/config/lang_test.go, internal/pipeline/pipeline.go | Translation: warn on malformed -src/-dst codes | New `config.SuspiciousLangCodes` flags a language code that does not look like one (a full name like "russian", an underscore form like "en_US", an empty value) via a shape check (`^[a-zA-Z]{2,3}(-[a-zA-Z0-9]{2,8})?$`) that still accepts valid-but-uncommon codes (zh-CN, pt-BR, sr-Latn, fil). When a translation engine is on, the pipeline prints a one-line non-fatal `WARNING: -src russian does not look like a language code..` and proceeds (closes old low-priority TODO G3). Shape check, not a registry lookup, so no false positives on rare codes; Ollama's free-form-name tolerance noted as advisory-only. |
| 2026-07-09 02:15:00 | internal/windowsreg/register_windows.go, internal/windowsreg/register_nonwindows.go, internal/config/flags.go, internal/app/app.go, cmd/doc-html-ui/main.go, tests/ui_cli_parity_test.go, README.md, docs.html, docs.ru.html, docs.uk.html | Windows "Open with": non-destructive registration | The app now shows up in Windows' "Open with.." list even when the user never made it the default handler. New `RegisterOpenWith`/`RegisterOpenWithFor` write `HKCU\Software\Classes\Applications\<exe>` with `FriendlyAppName`, `DefaultIcon`, `shell\open\command` and a `SupportedTypes` subkey listing all 10 extensions - the standard way to appear under "Open with" without touching any file type's default ProgID (so it never hijacks the default). Two triggers: the GUI auto-runs it on every launch (`ensureOpenWithRegistered`, no-op under MSIX where the package manifest already declares the associations, and when the bundled CLI is not found), and a new scriptable CLI flag `-register-openwith`. Associations point at the CLI so "Open with -> DOC-HTML-TRANSLATE" converts and opens the result, matching double-click. Non-destructive and admin-free (HKCU). Parity guard allow-lists the new flag (exposed via GUI auto-registration). Verified on Windows: keys written correctly, extension defaults untouched. |
| 2026-07-09 02:14:00 | internal/config/flags.go, cmd/doc-html-ui/main.go, cmd/doc-html-ui/ui.html, README.md | Flag rename: -single -> -multipage (single page is the default) | Single-page output is the default; the old `-single` opt-in became `-multipage` (opt out into per-chapter pages with a TOC). GUI `assembleArgs` now forwards `-multipage` when the single-page box is unchecked, and the box is `checked` by default so the GUI matches the CLI default. README flags table updated. |
| 2026-07-02 13:12:00 | extension/src/ocr-overlay.js | OCR (extension): silence Tesseract engine logs | Stopped Tesseract's diagnostic chatter ("Estimating resolution as N", "Detected N diacritics", "Invalid resolution 0 dpi. Using 70 instead") from showing up in Chrome's extension error console. Those lines are printed by the engine's C++ `tprintf`, which tesseract.js forwards to the worker console where Chrome logs them as errors. The worker now sets `debug_file=/dev/null` (a separate best-effort `setParameters` call after the PSM one, so a failure can't revert PSM) to route that output to the Emscripten null device; genuine failures still reject the recognize promise. Desktop CLI needs no change - it captures stderr and drops it on success. |
| 2026-07-02 12:57:00 | extension/src/viewer.css, extension/src/ocr-overlay.css, internal/ocr/overlay.go, docs/PARITY.md | OCR overlay: fix vertical plate drift | Fixed OCR plates drifting vertically - the further a plate sat from the image centre, the more it was offset (up above the centre, down below, correct only at the centre). Cause was a CSS specificity leak in the extension viewer: `#content img { margin: 1em auto }` (higher specificity than `.ocr-overlay-img`) put a 1em top/bottom margin on the overlay image, growing its aspect-ratio container ~2em and centering the image inside it - so plates, positioned as a percent of the taller container, drifted center-anchored from the picture. Fix: `#content .ocr-overlay-img { margin: 0; max-height: none }`. Also top-aligned plate text (`align-items: center` -> `flex-start`) so it starts at the source line and longer translated text grows downward instead of re-centering. Desktop mirror hardened for parity: `.ocr-fig>img` now sets `margin:0; max-height:none` (the reader's global `img { max-height:100vh }` could otherwise shrink the overlay image and drift plates) and `.ocr-box` top-aligns too. PARITY.md plate-geometry invariant updated. |
| 2026-07-02 12:45:00 | extension/src/ocr-overlay.js, internal/ocr/overlay.go, internal/ocr/tsv_test.go, tests/parity_test.go, docs/PARITY.md | OCR: larger overlay plate font | Bumped the plate font-fit factor from `0.85` to `0.92` on both editions (`FONT_FIT` / `fontFitFactor`), so OCR plate text renders closer to the original size. Still below `1.0` so a longer translation has room before it overflows the block, and plates keep growing downward via `min-height`. Updated the shared invariant in PARITY.md (plate geometry + aligned list), the `TestPercentStyle` expected value (8.50 -> 9.20cqw), and added a `TestParityOCRFontFit` guard so the two editions can't drift. |
| 2026-07-02 12:39:00 | extension/src/viewer.js | OCR (extension): suppress the "no text" banner when OCR covers the images | The image-only banner ("Little or no text found") is now skipped entirely when "Use OCR for images" is on and page images were queued for recognition (`options.ocrImages && ocrTotal > 0`) - the extension OCRs them into translatable plates, so the banner (which judges only the document's text layer) contradicted what the viewer just did (it showed above the OCR'd plates). Applied to both `finishDocument` (PDF) and `renderBook` (EPUB). When OCR is off the banner still shows; its advice paragraph now also points at the in-extension "Use OCR for images" toggle, not only the desktop app. Extension-only viewer copy/logic. |
| 2026-07-02 12:04:00 | internal/ocr/tesseract.go, DEV/plan/2026-07-02_ocr-desktop-tsv-config.md | OCR (desktop): TSV independent of the configs dir | Fixed the desktop app producing no OCR overlay when recognizing with its bundled tessdata. `Recognize` asked for TSV via the `tsv` config file, which lives in `<tessdata>/configs/tsv`; the app ships only `eng.traineddata` (no `configs/`) and `--tessdata-dir <exe>/tessdata` redirects config lookup there, so tesseract couldn't find `tsv` (`read_params_file: Can't open tsv`) and fell back to plain text - `parseTSV` then returned zero blocks. Now requests the TSV renderer with `-c tessedit_create_tsv=1`, independent of any `configs/` directory. Most affected the MSIX/Store build (always bundled data); the system-tesseract path is unchanged. Extension unaffected (tesseract.js returns structured data via its JS API, not a CLI config file). Verified end-to-end via the 5.4.0 CLI with and without `--tessdata-dir`. |
| 2026-07-02 11:49:00 | extension/src/ocr-overlay.js, internal/ocr/tesseract.go, docs/PARITY.md, tests/parity_test.go, DEV/plan/2026-07-02_ocr-psm-parity.md | OCR: page-segmentation parity (extension junk fix) | Fixed the browser extension stamping garbled OCR plates over full-page illustrations (e.g. a game/visual-novel render: one clean speech bubble read as "< = Ugh.. can't school be \| 4 optional just this once" + a duplicate plate). Root cause was a hidden parity gap - the desktop CLI runs Tesseract in its default PSM 3 (AUTO, layout analysis isolates the real text region) while the extension inherited `tesseract.js`'s default PSM 6 (SINGLE_BLOCK), which reads the whole 8-MP frame as one text block and folds scene edges into the dialogue. Both editions now pin **PSM 3**: the extension sets it via `worker.setParameters({ tessedit_pageseg_mode })` (`OCR_PSM`), the desktop makes its existing default explicit via `--psm 3` (`ocrPageSegMode`; no behaviour change). New shared invariant in PARITY.md (OCR table + aligned list) guarded by `TestParityOCRClustering`. Verified with the extension's own bundled tessdata_fast 4.0.0 model: `--psm 6` reproduces the junk, `--psm 3` returns the clean bubble text and nothing else. |
| 2026-07-01 21:34:00 | internal/ocr/tesseract.go, internal/ocr/upscale_test.go, extension/src/ocr-overlay.js, docs/PARITY.md, tests/parity_test.go, DEV/plan/2026-07-01_ocr-pre-upscale.md | OCR: pre-recognition upscale | Small images are now enlarged before OCR so Tesseract reads low-res thumbnails/scans better. Both editions: if the long side is below `OCR_UPSCALE_BELOW` (1000 px), the image is scaled `OCR_UPSCALE_FACTOR` (2x, high-quality) before recognition, then recognized coordinates + dimensions are divided back so plates still map onto the original picture (Go: `upscaleForOCR` via `x/image/draw` + temp PNG, `scaleDown`; JS: `upscaleForOcr` via canvas + scale-back in `collectLines`). Best-effort - a large image or any decode/scale failure recognizes the original. Verified no regression on the earlier repro (identical 2-plate result). New constants added to the parity guard + PARITY.md "Pre-OCR upscale" row; new Go tests. Note: very large "poster" display type stays a Tesseract limit that upscaling does not fix. |
| 2026-07-01 20:40:00 | docs.html, docs.ru.html, docs.uk.html, extension.html, extension/store/LISTING.md, extension/_locales/en/messages.json, extension/_locales/ru/messages.json, extension/_locales/uk/messages.json | Docs: OCR-image capability | Documented the image-OCR feature on the remaining stale surfaces. Added the four `-ocr` / `-ocr-lang` / `-ocr-langs` / `-ocr-download` flags (missing) to the CLI flags table on all three docs pages (en/ru/uk), wording matched to README.md. Added "text inside images (OCR)" to the store short description (`appDesc`, en/ru/uk locales + the LISTING.md mirror) - stays <= 132 chars (115/119/123); release-sensitive, covered by the existing owner sign-off banner. Refreshed the extension page's stale "scanned PDFs -> OCR with the desktop app" limitation (all three languages) to point at the extension's own "Use OCR for images" / right-click-image OCR. (Most surfaces - README, extension README, popup/options, index.html, privacy, GUI, flags.go - already described it.) |
| 2026-07-01 19:30:00 | internal/ocr/tesseract.go, internal/ocr/tsv_test.go, extension/src/ocr-overlay.js, docs/PARITY.md, tests/parity_test.go, DEV/plan/2026-07-01_ocr-overlay-line-clustering.md | OCR overlay: line clustering | Fixed three overlay defects on illustrated images: an opaque plate covering the picture, one uniform text block split into several plates, and a ballooning heading font. Both editions now flatten recognition to text lines, drop noise lines below `OCR_MIN_LINE_CONF` (50) mean word confidence, then group survivors into one plate per proximity cluster (vertical gap within `OCR_CLUSTER_GAP_FACTOR` 1.2 x median line height, same column), each plate boxed to the union of its line boxes - replacing the per-paragraph plates that trusted the engine's unstable segmentation. Confirmed on the repro image (heading plate + one body plate + fully-visible illustration) via the real tesseract.js. New `TestParityOCRClustering` guard + Go unit tests; PARITY.md granularity/geometry/noise-filter rows updated |
| 2026-07-01 12:05:00 | docs/PARITY.md, extension/README.md, extension/store/LISTING.md, extension/store/PRIVACY.md, extension/_locales/en/messages.json, extension/_locales/ru/messages.json, extension/_locales/uk/messages.json, README.md, docs.html, docs.ru.html, docs.uk.html, index.html, extension.html, extension-privacy.html, extension/src/options.html | Extension formats (phase 05): docs & store | Feature complete: the extension now opens PDF, EPUB, MOBI, AZW3, FB2, RTF, TXT, Markdown and local HTML (was PDF/EPUB only). Updated every source that described it as PDF/EPUB-only - parity port-map + divergence note, extension README, store listing/privacy + localized name/short-description (en/ru/uk; the store **name** change is flagged for owner sign-off at release), root README editions, and the website pages. Note: extension-privacy.html still needs a full re-render from PRIVACY.md at release (its OCR section is pre-existing drift) |
| 2026-07-01 12:00:00 | extension/src/ebook.js, extension/test/ebook.test.mjs, extension/build.mjs, extension/package.json, extension/src/viewer.js, extension/src/viewer.html, extension/src/popup.html, extension/manifest.json, extension/src/background.js | Extension formats (phase 04): MOBI, AZW3 | Client-side MOBI/KF8 (AZW3) reader via the newly vendored foliate-js mobi.js (+ fflate for KF8 fonts) - replaces the desktop's Calibre dependency; each section loaded via foliate's `load()` (images resolved to blob: URLs), sanitized into the shared book shape, TOC mapped via resolveHref; MOBI/AZW3 URLs intercepted via DNR; picker/notice copy generalized from "PDF or EPUB" to "a document". `node --test` covers `isMobiBytes` |
| 2026-07-01 11:57:00 | tests/parity_test.go, tests/ui_cli_parity_test.go | Parity guard tests | New drift guards that parse both codebases: theme palette (navbar.go vs viewer.css, 3/6-digit hex normalized), OCR tessdata version + language catalog (Go vs extension), PDF reflow constants (extract.go vs reflow.js), and "GUI exposes every CLI flag" (flags.go vs doc-html-ui/main.go); fail the gate on drift - see docs/PARITY.md |
| 2026-07-01 11:57:00 | internal/ocr/tessdata.go | OCR parity: pin tessdata version | Pinned `cdnBase` from the floating `main` branch to the `4.0.0` tag so downloaded languages match the extension's 4.0.0_fast; corrected the bundled-eng comment (build-time provisioning) and the catalog comment (shared with ocr-lang.js) |
| 2026-07-01 11:57:00 | extension/src/defaults.js, extension/src/background.js, extension/src/popup.js, extension/src/options.js, extension/src/viewer.js, extension/manifest.json | OCR parity: unify extension option defaults | New shared `defaults.js` exporting `DEFAULT_OPTIONS`, imported by all four consumers so they can't drift (background.js had silently lost the `ocrImages`/`ocrLang` fields); added to web-accessible resources |
| 2026-07-01 11:57:00 | extension/src/ocr-lang.js, extension/src/ocr-overlay.js, extension/build.mjs | OCR parity: align catalog + version | Expanded the extension OCR catalog (`LANGS`) and BCP-47 map (`HTML_LANG`) to the full 13 languages matching the desktop app; pinned the CDN/build tessdata version comments to 4.0.0 |
| 2026-07-01 11:57:00 | internal/pdf/extract.go | PDF reflow parity: name constants | Extracted the paragraph/heading heuristic magic numbers (gap x1.5, indent 8pt, ligature <3.0 over >=4 words, short/medium word caps 8/14) into a documented `const` block cross-referencing reflow.js + docs/PARITY.md |
| 2026-07-01 11:57:00 | internal/epub/toc.go, extension/src/epub.js | EPUB TOC parity: whitespace + external-href | Go NCX titles now collapse internal whitespace (`collapseWS`, matching the nav path + extension); the extension's `isExternalHref` now mirrors toc.go (any `://` scheme, or mailto:/tel:/data:) and is shared by the TOC resolver and the chapter-link rewriter |
| 2026-07-01 11:57:00 | docs/PARITY.md, DEV/plan/2026-07-01_cross-edition-parity.md | Cross-edition parity: execution | Recorded the aligned invariants (OCR version/catalog, EPUB TOC rules), fixed the light-theme bar-fg value, and logged which backlog items were aligned in code, documented as intentional, or deferred |
| 2026-07-01 11:30:00 | extension/src/md.js, extension/src/fb2.js, extension/build.mjs, extension/package.json, extension/src/viewer.js, extension/src/viewer.html, extension/manifest.json, extension/src/background.js | Extension formats (phase 03): Markdown, FB2 | Markdown reader via the newly vendored `marked` (split at H1/H2 into sections + TOC); FB2 reader (pure DOMParser port of internal/fb2, nested-section TOC, embedded `<binary>` images inlined as data: URLs); wired into the viewer dispatch + file picker; FB2 URLs intercepted via DNR (not .md). `build.mjs` now vendors `marked` |
| 2026-07-01 11:25:00 | extension/src/txt.js, extension/src/rtf.js, extension/src/html.js, extension/test/txt.test.mjs, extension/test/rtf.test.mjs, extension/src/viewer.js, extension/src/viewer.html, extension/manifest.json, extension/src/background.js | Extension formats (phase 02): TXT, RTF, HTML | Client-side readers for plain text (paragraph split + 30-per-page paging, ported from internal/txt), RTF (control-word stripper with `\'XX` Windows-1251 decode, ported from internal/rtf), and local HTML (sanitized `<body>`); wired into the viewer dispatch + file picker; RTF URLs now intercepted via DNR (not TXT/HTML). `node --test` covers `splitParagraphs` and `stripRtf` |
| 2026-07-01 11:20:00 | extension/src/sanitize.js, extension/src/viewer.js, extension/manifest.json | Extension formats (phase 01): dispatch seam | New shared `sanitizeToFragment` helper; viewer generalized to `detectFormat` + `renderBook` + `loadBook` so any parser returning the book shape renders through one path; `sanitize.js` exposed as a web-accessible resource. Foundation for TXT/MD/FB2/RTF/HTML/MOBI/AZW3 (2026-07-01_extension-format-parity) |
| 2026-07-01 11:05:00 | docs/PARITY.md | Cross-edition parity: new canonical doc | Single source of truth for Go-app vs JS-extension parity: the port map (Go file <-> JS file), the shared invariants that must stay identical (4-theme palette hexes, PDF reflow constants, EPUB TOC rules, OCR download host/catalog/CSS classes, settings defaults), and the intentional divergences (do-not-"fix" list) - from the 2026-07-01 parity audit of OCR, PDF reflow, EPUB, reader UI and settings |
| 2026-07-01 11:05:00 | DEV/plan/2026-07-01_cross-edition-parity.md, DEV/plan/_TEMPLATE_cross-edition.md | Cross-edition parity: process | New backlog ticket enumerating every drift (P0 unintended bugs -> P3 test debt) with Align/Port/Document targets, plus a one-feature-one-ticket template with an edition parity checklist (replaces the anti-pattern of a separate ticket per edition, e.g. the two OCR tickets) |
| 2026-07-01 11:05:00 | AGENTS.md, CLAUDE.md, extension/README.md | Cross-edition parity: wiring | Added a "Cross-edition parity" pointer to docs/PARITY.md in the agent guides and the extension README (the per-file "ported from" notes are now a pointer, not a second source of truth) |
| 2026-07-01 11:05:00 | DEV/README.md | De-duplicate flag table | Replaced the stale "Key Flags" table (missing -ocr/-single/-toc-depth/-max-cost) with a pointer to README.md + docs/PARITY.md so the flag list has one source of truth |
| 2026-07-01 11:02:00 | cmd/doc-html-ui/main.go, cmd/doc-html-ui/ui.html, cmd/doc-html-ui/favicon.ico | GUI window icon | The GUI runs as a Chrome --app window, which showed the browser's default globe because the page had no favicon; embed the project icon, serve it at /favicon.ico, and link it in ui.html so the window shows our logo |
| 2026-07-01 11:02:00 | scripts/generate-icon.ps1 | Multi-size icon | Rebuilt the icon generator to produce a proper multi-resolution .ico (16/24/32/48/64/128/256, PNG-compressed frames) from the canonical 1080x1080 project logo instead of a single blurry 64px frame |
| 2026-07-01 11:02:00 | assets/doc-html-translate.ico | Project icon | Regenerated as the crisp multi-resolution icon (used by both exe resources and the site favicon) |
| 2026-07-01 11:02:00 | scripts/build-ui.ps1 | Build | Copy the canonical icon to cmd/doc-html-ui/favicon.ico before build so the embedded app-window favicon stays in sync |
| 2026-07-01 10:52:00 | internal/htmlgen/singlepage.go | Single-page output | New `GenerateSinglePage`: merges every spine chapter, in reading order, into one index.html with the unified reader header (theme/font controls, progress bar) and no per-chapter navigation; writes it inside the base dir so relative refs resolve, adds a redirect at the output root when nested, and collapses the book to one spine entry for OCR/translation |
| 2026-07-01 10:52:00 | internal/config/flags.go | Single-page flag | Added `-single` to combine the whole document into a single HTML page (no table of contents) |
| 2026-07-01 10:52:00 | internal/pipeline/pipeline.go | Single-page pipeline | `-single` builds the merged page instead of nav bars, skips paragraph splitting, and (single spine) skips the post-translation TOC step |
| 2026-07-01 10:52:00 | cmd/doc-html-ui/main.go, cmd/doc-html-ui/ui.html | Single-page GUI | Added a "Single page (no table of contents)" checkbox that forwards `-single`; persisted with the other settings |
| 2026-07-01 10:52:00 | README.md | Single-page docs | Documented the `-single` flag in the CLI options table |
| 2026-07-01 10:30:00 | internal/htmlgen/navbar.go | Unify reader UI | Reworked the generated-HTML header to match the browser extension: theme-reactive chrome via a shared CSS-variable palette, a 4th "Night" theme, a theme dropdown (replacing the cycle button), A-/A+ text-size and Serif/Sans/Mono font-family controls, and prev/next moved to a corner turn-group (Next flush to the edge) |
| 2026-07-01 10:30:00 | internal/htmlgen/htmlgen.go | Unify reader UI | index.html toolbar now uses the same shared reader controls (text size, font family, theme dropdown) plus the continue-reading link |
| 2026-07-01 10:30:00 | internal/htmlgen/navbar_test.go, internal/htmlgen/htmlgen_test.go | tests | Updated regression assertions to the theme dropdown + text-size controls |
| 2026-07-01 10:30:00 | extension/src/viewer.css, extension/src/viewer.html, extension/src/options.html | Unify reader UI | Added the "Night" theme (values matching the desktop app) and a small project link in the viewer toolbar for header parity |
| 2026-07-01 03:35:00 | internal/ocr/tesseract.go, internal/ocr/tessdata.go, internal/ocr/overlay.go, internal/ocr/tsv_test.go | App OCR: new package | Shell out to the Tesseract CLI (TSV bbox parsing), overlay recognized text as opaque translatable plates over document images, and manage tessdata (bundled English + on-demand language downloads) |
| 2026-07-01 03:35:00 | internal/config/flags.go | App OCR: flags | Added `-ocr`, `-ocr-lang`, `-ocr-langs`, `-ocr-download` |
| 2026-07-01 03:35:00 | internal/app/app.go | App OCR: language commands | `-ocr-langs` lists languages and `-ocr-download <lang>` fetches a pack, both exiting without a document |
| 2026-07-01 03:35:00 | internal/pipeline/pipeline.go | App OCR: pipeline step | When `-ocr` is set, OCR-overlay each content page after nav injection and before translation (best-effort) |
| 2026-07-01 03:35:00 | cmd/doc-html-ui/main.go, cmd/doc-html-ui/ui.html | App OCR: GUI | "Image OCR" section: overlay toggle, OCR-language picker, and per-language Download; `/api/ocr-langs` + `/api/ocr-download` endpoints; passes `-ocr`/`-ocr-lang` |
| 2026-07-01 03:35:00 | scripts/build.ps1, scripts/build-ui.ps1 | App OCR: bundle English | Provision `<exe>/tessdata/eng.traineddata` (from the extension vendor copy or download) so `-ocr` works offline out of the box |
| 2026-07-01 03:35:00 | README.md, AGENTS.md | App OCR: docs | Documented `-ocr` and the language-download flow |
| 2026-07-01 03:15:00 | extension/manifest.json | OCR fix: WASM CSP | Added `content_security_policy.extension_pages` with `wasm-unsafe-eval` - required for the Tesseract WASM engine and scanned-PDF rasterization to run under MV3 (min_chrome 105) |
| 2026-07-01 03:15:00 | extension/store/PRIVACY.md | OCR phase 08: privacy | Disclosed on-device OCR, the optional user-triggered language download from the tessdata_fast CDN, and the contextMenus permission; bumped "Last updated" to 2026-07-01 |
| 2026-07-01 03:15:00 | extension/store/LISTING.md | OCR phase 08: listing | Added the image-OCR key feature and the contextMenus permission justification |
| 2026-07-01 03:15:00 | extension/_locales/en/messages.json, extension/_locales/ru/messages.json, extension/_locales/uk/messages.json | OCR phase 08: store copy | Added an image-OCR line to `storeDescription` in en/ru/uk |
| 2026-07-01 03:15:00 | extension/README.md | OCR phase 08: docs | Added the "Translate text in images (OCR)" section, updated the CSP/rasterization limitation note, layout, and dev steps |
| 2026-07-01 03:10:00 | extension/src/pdf-images.js | OCR phase 07: PDF image extraction | New module: `extractPageImages` walks the page operator list for image XObjects and decodes them to PNG blobs; `rasterizePage` renders a full-page fallback for scanned pages |
| 2026-07-01 03:10:00 | extension/src/viewer.js | OCR phase 07: PDF image OCR | PDF render loop now (when `ocrImages` is on) extracts page images before cleanup, appends them as `<img>`, and feeds them to the shared lazy OCR scheduler; extracted-image object URLs revoked on teardown |
| 2026-07-01 03:05:00 | extension/src/viewer.js | OCR phase 06: EPUB image OCR | Lazy image OCR in the viewer: an IntersectionObserver queues each chapter image (when `ocrImages` is on), overlays translatable plates via the shared unit one at a time, and shows an overall "OCR: done/total" status; no-op and unchanged rendering when OCR is off |
| 2026-07-01 03:05:00 | extension/src/viewer.css | OCR phase 06: overlay styles | Reading-column styling for the pending-image badge wrapper and the finished OCR overlay |
| 2026-07-01 03:00:00 | extension/src/popup.html, extension/src/popup.js | OCR phase 05: popup controls | Added a "Use OCR for images" toggle next to the open-file button with a nested, indented OCR-language list: installed languages selectable, others with a Download button showing progress; persists `ocrImages`/`ocrLang` |
| 2026-07-01 03:00:00 | extension/src/options.html, extension/src/options.js | OCR phase 05: options controls | Added an "Image OCR" section mirroring the popup: toggle, default recognition language, and per-language Download buttons with progress |
| 2026-07-01 03:00:00 | extension/_locales/en/messages.json, extension/_locales/ru/messages.json, extension/_locales/uk/messages.json | OCR phase 05: i18n | Added control strings (`ocrUseImages`, `ocrDownload`, `ocrDownloading`, `ocrInstalled`, `ocrLangsHint`) in en/ru/uk |
| 2026-07-01 02:55:00 | extension/src/background.js | OCR phase 04: context menu | Added an image context-menu item ("OCR & translate this image") that opens `ocr.html?src=` in a new tab; created/cleared on install and startup |
| 2026-07-01 02:55:00 | extension/src/ocr.html, extension/src/ocr.js | OCR phase 04: OCR page | New standalone page: reads `?src=`, runs the shared overlay unit with the preferred OCR language, shows progress, sets `<html lang>` so the browser offers "Translate page" |
| 2026-07-01 02:55:00 | extension/_locales/en/messages.json, extension/_locales/ru/messages.json, extension/_locales/uk/messages.json | OCR phase 04: i18n | Added OCR strings (`ocrImageMenu`, `ocrProgress`, `ocrNoText`, `ocrLoadError`, `ocrDone`) in en/ru/uk |
| 2026-07-01 02:50:00 | extension/src/ocr-overlay.js | OCR phase 03: overlay core | New reusable unit: lazy single Tesseract worker + FIFO queue (one image at a time), cross-origin-safe image fetch, `recognize` returning block bboxes, `buildOverlay` rendering opaque percent-positioned plates with real HTML text, `overlayImage`, `ocrLangToHtmlLang` |
| 2026-07-01 02:50:00 | extension/src/ocr-overlay.css | OCR phase 03: overlay styles | Styling for the overlay container (inline-size container query), opaque text plates (grow-downward, wrap), and per-image progress badge |
| 2026-07-01 02:45:00 | extension/src/ocr-lang.js | OCR phase 02: language manager | New module: `LANGS` catalog, offline `eng` path, `downloadLang`/`getInstalledLangs`/`isInstalled`/`resolveLangPath`, shared worker asset/option helpers; other languages fetched from tessdata_fast and cached in IndexedDB on explicit request |
| 2026-07-01 02:40:00 | extension/package.json | OCR phase 01: engine deps | Added `tesseract.js` + `tesseract.js-core` (^5.1.1) as dev deps, vendored at build time like pdfjs |
| 2026-07-01 02:40:00 | extension/build.mjs | OCR phase 01: vendor engine | `vendor` command now also vendors Tesseract: ESM entry, worker, the SIMD+LSTM self-contained core, and fetches `eng.traineddata` (tessdata_fast, decompressed, 3.9 MB) so English OCR ships fully offline |
| 2026-07-01 02:40:00 | extension/manifest.json | OCR phase 01: permissions + WAR | Added `contextMenus` permission and web-accessible OCR resources (`ocr.html/js`, `ocr-overlay.js/css`, `ocr-lang.js`, `pdf-images.js`) |
| 2026-06-27 18:33:00 | cmd/doc-html-ui/ui.html | GUI compact redesign | Reworked layout: document drop-zone and Convert share one row (primary "choose and run" flow), translation engine is now a radio group (Chrome free / Google / Ollama / off), advanced number fields and Ollama/Windows/Log sections fold into collapsibles, and the window auto-fits its content height |
| 2026-06-27 18:33:00 | cmd/doc-html-ui/main.go, cmd/doc-html-ui/ui.html | GUI drag-and-drop | Dropping a file onto the window now works: the browser sandbox exposes no path, so the page uploads the bytes to `/api/drop` and the server saves them under %LOCALAPPDATA% for the CLI to read |
| 2026-06-27 18:33:00 | cmd/doc-html-ui/main.go, cmd/doc-html-ui/ui.html | GUI language pickers + saved settings | Source/target languages are dropdowns with a swap button; form state (engine, languages, output, advanced, toggles) now persists across sessions via `/api/settings` (the random server port rules out browser localStorage) |
| 2026-06-27 18:33:00 | cmd/doc-html-ui/main.go | Fix: non-ASCII dialog paths | File/folder dialog paths with Cyrillic (e.g. OneDrive\Документы) were mangled by Windows PowerShell's OEM code page; the path is now carried as base64(UTF-8) and the scripts run via `-EncodedCommand` |
| 2026-06-27 18:33:00 | cmd/doc-html-ui/main.go | Fix: dialog opened behind the window | The file/folder picker now owns to the current foreground window (`GetForegroundWindow()`), so it opens in front and focused instead of behind the app |
| 2026-06-27 18:33:00 | cmd/doc-html-ui/main.go, cmd/doc-html-ui/ui.html | Fix: server killed mid-conversion | The heartbeat watchdog no longer shuts the server down while a conversion is running (it surfaced as a bare "network error"); the log now appends in O(n), not O(n^2) |
| 2026-06-27 18:33:00 | cmd/doc-html-ui/main_test.go | tests | Added coverage for `/api/drop` (save, Cyrillic name, path-traversal strip), `/api/settings` round-trip, and base64 dialog-path decoding |
| 2026-06-26 17:00:00 | internal/config/flags.go, internal/pipeline/pipeline.go | cost cap | Feature [21]: `-max-cost N` hard pre-flight guard - paid (Google) translation is skipped before sending if the estimate (chars/1e6*$20) exceeds N; book is still produced untranslated |
| 2026-06-26 17:00:00 | internal/htmlgen/navbar.go | reading themes/position | Feature [12]/[14]: injected reader layer - Light/Sepia/Dark/Night theme toggle (localStorage) and reading-position tracking (per-book key, scroll restore, navbar progress bar); edge-scroll auto-nav switched to class-based prev/next selection |
| 2026-06-26 17:00:00 | internal/htmlgen/htmlgen.go | index reader toolbar | Feature [12]/[14]: index.html gets a theme toggle and a "Continue reading" link wired to the same per-book localStorage key |
| 2026-06-26 17:00:00 | internal/htmlgen/navbar_test.go, internal/htmlgen/htmlgen_test.go | tests | Regression assertions for the theme button, progress bar, continue link and reader script |
| 2026-06-26 17:00:00 | README.md, AGENTS.md | docs | Documented `-max-cost`, reading themes and reading position |
| 2026-06-12 00:00:00 | internal/htmlgen/htmlgen.go | TOC snippets | Fix: TOC snippets now resolve chapter files through EPUB BasePath (e.g. OEBPS/) and fallback labels are no longer double-numbered |
| 2026-06-12 00:00:00 | internal/pipeline/pipeline.go | pipeline performance | Optimization: parsed HTML pages are now reused for Google cost estimation, translation, and TOC snippet generation to avoid repeated DOM parsing |
| 2026-06-12 00:00:00 | cmd/doc-html-ui/main.go, cmd/doc-html-ui/ui.html | GUI split size | Fix: GUI now passes `-split 0` explicitly so disabling split matches CLI behavior; added inline help text |
| 2026-03-04 01:30:27 | DEV/research/epub2html_research_ru.md | research | Добавлен подробный research по задаче EPUB->HTML+Translate: варианты решения, риски, задачи разработчика |
| 2026-03-04 01:32:14 | DEV/plan.md | planning | Создан стратегический план разработки: 4 фазы, 11 блоков задач, Q1-Q7 вопросы пользователю |
| 2026-03-04 01:33:49 | scripts/generate-icon.ps1 | build-assets | Добавлен генератор примитивной иконки EPUB2HTML в формате .ico |
| 2026-03-04 01:33:49 | scripts/build.ps1 | build | Сборка дополнена генерацией и копированием иконки epub2html.ico рядом с exe |
| 2026-03-04 01:33:49 | internal/windowsreg/register_windows.go | windows-registry | Регистрация .epub расширена: ProgID, open command и DefaultIcon с использованием epub2html.ico |
| 2026-03-04 01:35:37 | scripts/commit_after_build.ps1 | git-workflow | Исправлен commit workflow: COMMIT_LOG добавляется в тот же коммит через amend |
| 2026-03-04 01:35:37 | DEV/plan.md | planning | Обновлён live-прогресс: baseline commit выполнен (f3fbc43) |
| 2026-03-04 01:37:57 | scripts/build.ps1 | build | Сборка переведена на temp/: output temp/build, icon temp/assets, build.log в temp/logs |
| 2026-03-04 01:37:57 | scripts/test.ps1 | test | Тестовый скрипт пишет лог в temp/logs/test.log |
| 2026-03-04 01:37:57 | scripts/lint.ps1 | lint | Линтер пишет лог в temp/logs/lint.log |
| 2026-03-04 01:37:57 | scripts/typo.ps1 | typo | Проверка typos пишет лог в temp/logs/typo.log |
| 2026-03-04 01:37:57 | scripts/check.ps1 | check | Агрегирующий скрипт сохраняет логи шагов в temp/logs |
| 2026-03-04 01:37:58 | scripts/generate-icon.ps1 | build-assets | Default output иконки изменён на temp/assets/epub2html.ico |
| 2026-03-04 01:37:58 | configs/.typos.toml | config | Исключения typos обновлены: temp вместо build |
| 2026-03-04 01:37:58 | .gitignore | git | Добавлено правило temp/ для исключения промежуточных артефактов |
| 2026-03-04 01:37:58 | README.md | docs | Документация обновлена: все артефакты/логи в temp |
| 2026-03-04 01:37:58 | assets/epub2html.ico | cleanup | Удалён артефакт иконки из корня проекта |
| 2026-03-04 01:38:10 | DEV/plan.md | planning | Live-прогресс обновлён: зафиксирована temp-only политика для артефактов и логов |
| 2026-03-04 01:38:47 | .gitignore | git | Добавлено исключение build/ для предотвращения попадания root-артефактов в git |
| 2026-03-04 01:38:47 | build/epub2html.ico | cleanup | Удалён случайно отслеживаемый build-артефакт, политика temp-only восстановлена |
| 2026-03-04 01:39:40 | DEV/README.md | docs | README перенесён из корня в DEV согласно политике хранения документации |
| 2026-03-04 01:39:40 | DEV/universal_copilot_instructions.md | docs | Файл universal_copilot_instructions перенесён из корня в DEV |
| 2026-03-04 01:39:40 | DEV/plan.md | planning | План обновлён: документация хранится в DEV, путь K3 обновлён на DEV/README.md |
| 2026-03-04 01:40:22 | temp/docs-archive/task_2026-03-04.md | docs-archive | Устаревший DEV/task.md перенесён в temp/docs-archive |
| 2026-03-04 01:40:22 | DEV/README.md | docs | Добавлен раздел жизненного цикла документации и правила архивирования |
| 2026-03-04 01:40:22 | DEV/plan.md | planning | В план добавлено правило переноса устаревшей документации в temp/docs-archive |
| 2026-03-04 01:41:13 | DEV/questionnaire_for_user.txt | docs | Создан текстовый опросник для пользователя с 15 вопросами по уточнению требований |
| 2026-03-04 01:41:46 | DEV/plan.md | planning | Зафиксированы решения: локальный непубличный git, приоритет истории откатов, допустимость хранения чувствительных данных в v1 |
| 2026-03-04 02:06:59 | internal/htmlgen/navbar.go | htmlgen | Added sticky navigation bar (prev/TOC/next) injected into all content HTML pages |
| 2026-03-04 02:07:04 | internal/htmlgen/navbar_test.go | htmlgen | Tests for navbar: relativePath, buildNavBarHTML, InjectNavBars (4 tests) |
| 2026-03-04 02:07:08 | internal/pipeline/pipeline.go | pipeline | Wire InjectNavBars call between index generation and translation steps |
| 2026-03-04 02:08:37 | internal/htmlgen/navbar.go | htmlgen | Adjusted navbar layout: compact adjacent buttons aligned to top-right |
| 2026-03-04 02:16:56 | internal/translator/translator.go | translator | Configured hardcoded Google Translate API key in HARDCODED_API_KEY |
| 2026-03-04 02:22:44 | internal/app/app.go | app | Clarified -register output: HKCU registration done but Windows UserChoice may still override default app |
| 2026-03-04 02:24:48 | internal/config/flags.go | config | No-args launch now enters implicit register mode for first-click UX |
| 2026-03-04 02:24:48 | internal/config/flags_test.go | config | Updated tests for implicit register on empty args and missing-file validation |
| 2026-03-04 02:24:49 | DEV/README.md | docs | Documented first-run no-arg registration behavior |
| 2026-03-04 02:25:15 | internal/pipeline/pipeline.go | pipeline | Implemented -force mode: rebuild output instead of idempotent reopen |
| 2026-03-04 02:25:16 | DEV/README.md | docs | Documented -force flag and behavior in usage notes |
| 2026-03-04 02:36:57 | internal/htmlgen/navbar.go | htmlgen | Added zoom persistence across chapter navigation via injected JS (Ctrl+Wheel + z query/session sync) |
| 2026-03-04 20:07:16 | internal/pdf/extract.go | pdf | Create PDF text extraction package using ledongthuc/pdf. Per-page extraction with GetTextByRow, HTML page generation, epub.Book adapter. |
| 2026-03-04 20:07:23 | internal/pdf/extract_test.go | pdf | Add 8 unit tests for PDF extraction: helpers, valid PDF, empty PDF, invalid file, non-existent file. |
| 2026-03-04 20:07:31 | internal/pipeline/pipeline.go | pipeline | Add PDF format detection (.epub/.pdf by extension). New extractPDF branch. Rename outputDirForEpub->outputDirForFile. Add ExitParse alias. |
| 2026-03-04 20:07:39 | DEV/plan.md | docs | Add Phase 5: PDF support with blocks L, M, N. Architecture decisions R19-R24. |
| 2026-03-04 20:14:53 | go.mod | rename | Module renamed: epub2html -> doc-html-translate |
| 2026-03-04 20:15:01 | **/*.go,scripts/*,DEV/* | rename | Full project rename epub2html -> doc-html-translate: module, imports, binary, scripts, CSS prefix (dht-), ProgID, docs, icon |
| 2026-03-04 23:48:42 | internal/md/extract.go | Markdown extractor | Goldmark-based MD->HTML converter with heading-based pagination |
| 2026-03-04 23:48:44 | internal/fb2/extract.go | FB2 extractor | XML parser for FictionBook2 format with paragraph pagination |
| 2026-03-04 23:48:46 | internal/rtf/extract.go | RTF extractor | Lightweight RTF stripper with CP1251 hex escape decoding |
| 2026-03-04 23:48:48 | internal/htmlconv/extract.go | HTML/HTM extractor | HTML passthrough with body extraction and CSS wrapping |
| 2026-03-04 23:48:50 | internal/pipeline/pipeline.go | Pipeline | Added MD/FB2/RTF/HTML/HTM cases to format router |
| 2026-03-04 23:48:52 | internal/windowsreg/register_windows.go | Registration | Extended SupportedExtensions with .md .fb2 .rtf .html .htm |
| 2026-03-05 03:14:24 | internal/htmlgen/htmlgen.go | TOC | Fix double numbering: ol→ul (auto-numbers were redundant with explicit %d. in toc-label span) |
| 2026-03-05 03:14:24 | internal/pipeline/pipeline.go | Title translation | Translate book.Title via Google API at end of translateContent before GenerateIndex is called |
| 2026-03-05 23:31:07 | internal/htmlgen/navbar.go | navBarCSS | Added img { max-height: 100vh; width: auto; max-width: 100% } to limit image height to one screen |
| 2026-03-07 02:43:26 | internal/translator/ollama.go | ollama.go | Add translateSingle() for echo-back retries; reduce ollamaBatchSize 40->20; add ollamaMaxRetries=2 |
| 2026-03-07 02:43:26 | internal/htmlproc/htmlproc.go | htmlproc.go | ReplaceTexts: skip empty translations (translated=empty -> keep original); fix leading whitespace operator precedence bug |
| 2026-03-07 02:56:37 | internal/logging/log.go | logging | New package: timestamp-prefixed console output (Printf/Println/Errorf/Progress) |
| 2026-03-07 03:06:24 | internal/translator/ollama.go | OllamaClient | Add num_ctx=8192 option to all Ollama requests; concurrent batch goroutines with semaphore; thread-safe firstRequest via sync.Mutex; SetParallelism/SetNumCtx methods |
| 2026-03-07 03:06:24 | internal/config/flags.go | Config | Add OllamaParallel int and OllamaNumCtx int fields; -ollama-parallel and -ollama-ctx flags |
| 2026-03-07 03:06:24 | internal/pipeline/pipeline.go | UseOllama case | Wire SetParallelism + SetNumCtx from config into ollamaWorker |
| 2026-03-07 03:18:07 | internal/htmlsplit/split.go | htmlsplit | New package: SplitIfNeeded splits HTML spine items at paragraph boundaries to stay within maxChars |
| 2026-03-07 03:18:07 | internal/config/flags.go | Config.SplitSize | Add SplitSize int field; -split flag with auto-inject 5000 when used without value |
| 2026-03-07 03:18:07 | internal/pipeline/pipeline.go | Run() | Add split step after extraction using htmlsplit.SplitIfNeeded when SplitSize > 0 |
| 2026-03-07 03:22:49 | internal/config/flags.go | Config.OutputFolder | Add OutputFolder string field; -folder flag for custom output directory |
| 2026-03-07 03:22:49 | internal/pipeline/pipeline.go | outputDirFor() | Replace outputDirForFile with outputDirFor(path, folder); uses -folder when set |
| 2026-03-07 03:44:55 | cmd/doc-html-ui/main.go | DOC-HTML-UI | Created standalone GUI launcher (embedded web UI + Go HTTP server, Edge/Chrome app-mode, native file dialogs via PowerShell, all CLI flags as checkboxes/inputs, streaming log output, heartbeat auto-shutdown) |
| 2026-03-07 03:45:00 | cmd/doc-html-ui/ui.html | DOC-HTML-UI | Dark-themed responsive HTML/CSS/JS interface with all doc-html-translate flags |
| 2026-03-07 03:45:05 | scripts/build-ui.ps1 | DOC-HTML-UI | Build script for doc-html-ui.exe (pure Go, no CGO, -H windowsgui, icon embedding) |
| 2026-03-07 03:50:31 | internal/pdf/extract.go | PDF | Added panic recovery (defer/recover) for malformed PDFs - function-level and per-page level |
| 2026-03-07 03:54:13 | internal/pdf/extract.go | PDF | Added auto-repair fallback via pdfcpu (OptimizeFile + retry extraction), improved no-text diagnostic for scanned PDFs |
| 2026-03-07 03:54:44 | internal/pdf/extract.go | PDF | Mapped repaired-PDF no-text error back to original input path for clearer UX |
| 2026-03-07 03:55:17 | go.mod | Dependencies | Added pdfcpu dependency for PDF repair fallback; removed temporary parser experiment deps |
| 2026-03-07 03:55:17 | go.sum | Dependencies | Updated checksums after introducing pdfcpu fallback and tidy |
| 2026-03-07 03:58:02 | internal/pdf/extract.go | PDF | Implemented no-OCR fallback for image-only PDFs: create single HTML wrapper page with embedded original.pdf instead of failing extraction |
| 2026-03-07 04:05:54 | cmd/doc-html-ui/ui.html | DOC-HTML-UI | Added in-window drag-drop handling, fixed-size window enforcement, and compact horizontally-scrollable log styling |
| 2026-03-07 04:11:49 | internal/epub/epub.go | EPUB | Added XHTML->HTML normalization for content files and intra-book links to improve Chrome Translate compatibility on local files |
| 2026-03-07 17:35:38 | internal/browser/browser.go | Browser | Added normalizeTarget() to redirect XHTML open targets to nearest ancestor index.html |
| 2026-03-07 17:35:38 | internal/browser/browser_windows.go | Browser | Applied normalizeTarget() before Windows browser launch |
| 2026-03-07 17:35:38 | internal/htmlgen/navbar.go | Navigation | Added legacy XHTML guard: direct .xhtml open redirects to index.html; navbar blocks chapter-to-chapter .xhtml navigation |
| 2026-03-07 17:37:00 | internal/htmlgen/navbar.go | Navigation | Added image aspect-ratio guard: when image height changes, width is auto/computed from natural proportions |
| 2026-03-09 23:54:57 | internal/epub/epub.go | epub.Extract | Fix: resolveReservedNames renames EPUB content files named index.html to _content_index.html to avoid overwrite by generated nav TOC |
| 2026-03-10 01:03:46 | internal/config/flags.go | SplitSize | Default SplitSize changed from 0 to 5000; removed injectDefaultSplitValue hack; disable via -split 0 |
| 2026-03-10 01:16:56 | scripts/build.ps1 | goversioninfo | Fix: added -64 flag to goversioninfo to generate amd64-compatible .syso (was: relocation type 7 error) |
| 2026-03-10 01:32:31 | internal/htmlgen/navbar.go | navBarScript | feat: edge-scroll auto-navigation - PageDown/wheel-down at bottom → next page; PageUp/wheel-up at top → prev page (wheel threshold: 3 events) |

| 2026-06-27 16:18:59 | scripts/build-local.ps1, scripts/release.ps1, DEV/RELEASE.md | Build/Release | feat: split local-free build (build-local.ps1: gate+CLI+UI+commit) from paid release flow (release.ps1 checklist-only); /build + /release commands; AGENTS/CLAUDE/README docs |
| 2026-06-27 16:18:59 | .github/workflows/release.yml, publish-extension.yml | CI | perf: move release + extension publish to ubuntu-latest (~half cost); add concurrency guards, npm cache, pinned goversioninfo@v1.7.0; drop WPF icon-gen (commit assets/*.ico) |
| 2026-06-27 16:18:59 | cmd/doc-html-ui/main.go, ui.html | doc-html-ui | feat: Set-as-default-handler button (-register), -toc-depth/-max-cost fields, MSIX-aware hiding + missing-CLI warning via /api/env |
| 2026-06-27 16:18:59 | internal/*, cmd/* | Lint | style: errcheck cleanup (explicit _ = on ignored returns) across internal and cmd; golangci config update |
| 2026-06-27 16:18:59 | configs/.typos.toml | Gate | fix: teach typos config the project proper nouns (SerZhyAle, OPF, PNGs) and exclude data/fixture files so the typo gate runs clean |
| 2026-06-27 16:18:59 | index.html, extension.html, docs.*.html, README.md, extension/ | Site/Docs | docs: typography pass (short hyphens, .. ellipsis) and tone rewrites across site, README and extension copy |

| 2026-07-01 16:35:00 | internal/ocr/overlay.go, extension/src/ocr-overlay.js | OCR overlay | fix: overlay was invisible / oversized in-browser - app container collapsed (inline-block + container-query -> display:block; width:100%; aspect-ratio); extension font-size blew up (79cqw) because block.lines is empty in tesseract.js v5, now reads block.paragraphs[].lines |
| 2026-07-01 16:35:00 | internal/ocr/overlay.go, extension/src/ocr-overlay.js | OCR overlay | feat: 0.85 font fit-factor so plate text fits its block instead of overflowing into the next plate |
| 2026-07-01 16:35:00 | internal/ocr/overlay.go, extension/src/ocr-overlay.js | OCR overlay | feat: adaptive plate colours sampled from the source image (paper background + ink from the first line) so plates blend into the document; best-effort, falls back to white/dark |
| 2026-07-01 16:35:00 | internal/ocr/text.go, extension/src/ocr-text.js | OCR overlay | feat: isTranslatable noise filter - no plate for < 5 letters, vowel-less, address (url/email/domain/path) or mishmash OCR; short CJK kept; unit-tested on both editions |
| 2026-07-01 16:35:00 | docs/PARITY.md | Parity | docs: pin OCR plate geometry / adaptive colours / noise-filter invariants across the Go app and the extension |
